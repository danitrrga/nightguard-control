---
phase: 04-ui-moonlit-indigo
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - crates/mutation-engine/src/lib.rs
  - crates/mutation-engine/src/lock_status.rs
  - crates/mutation-engine/tests/lock_status.rs
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "lock_status correctly answers 'are we locked right now' for inside-window, outside-window, and overnight-wrapping curfews"
    - "lock_status computes the next state-change boundary (curfew end when locked, next lock when open)"
    - "curfew.enabled=false yields not-locked; a missing/malformed config field never silently unlocks"
    - "schedule.<day> precedence over curfew.start/end is applied per the written spec"
  artifacts:
    - path: "crates/mutation-engine/src/lock_status.rs"
      provides: "pure lock_status(config_yaml, now) -> LockStatus evaluator + LockStatus struct"
      contains: "pub fn lock_status"
    - path: "crates/mutation-engine/tests/lock_status.rs"
      provides: "diff/truth-table unit tests covering every window case"
      contains: "#[test]"
  key_links:
    - from: "crates/mutation-engine/src/lib.rs"
      to: "lock_status module"
      via: "pub mod lock_status"
      pattern: "pub mod lock_status"
    - from: "crates/mutation-engine/src/lock_status.rs"
      to: "classify.rs window/time parsers"
      via: "shared parse_hhmm/parse_window (pub(crate) or duplicated identically — no divergent parser)"
      pattern: "parse_hhmm|parse_window"
user_setup: []
---

<objective>
Write the one net-new piece of logic this phase needs that no crate provides: a pure `lock_status(config_yaml, now) -> LockStatus` evaluator in `mutation-engine` that answers "are we locked right now?" and "when is the next state-change boundary?" from `config.yaml`'s curfew fields against advisory time. It must interpret `curfew.enabled`, `curfew.start/end`, `curfew.schedule.<day>`, overnight-wrap windows, and `timezone` faithfully (so the UI never disagrees with the guard's verdict), and degrade defensively (a missing/malformed field is never a silent unlock — D-04).

Purpose: This is the HIGHEST-RISK task in the phase (RESEARCH Open Q1 / Pitfall 1 / Assumption A5). It is carved out as its own pure, exhaustively-tested function so the riskiest logic is verifiable offline and reusable headless. `get_state` (plan 03) depends on it.
Output: A unit-tested `lock_status` function + `LockStatus` result type, registered in the engine's lib.rs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-ui-moonlit-indigo/04-RESEARCH.md
@.planning/phases/04-ui-moonlit-indigo/04-PATTERNS.md

<interfaces>
From crates/mutation-engine/src/classify.rs (window/time parsing to reuse — do NOT introduce a second divergent parser):
  fn parse_hhmm(s: &str) -> Option<i64>          // "HH:MM" -> minutes since midnight; bounds-checked 0..24 / 0..60
  fn parse_window(s: &str) -> Option<(i64, i64)> // "HH:MM-HH:MM" -> (start_min, end_min)
  // read_field (classify.rs:194-220): yamlpath Route + query_exact; missing path -> Ok(None), never panic, never silent loosen

From crates/mutation-engine/src/week.rs:
  fn parse_tz(tz_name: &str) -> Tz               // tz_name.parse().unwrap_or(chrono_tz::UTC) — defensive default

From crates/mutation-engine/src/grace.rs (unix-secs -> in-tz instant pattern, lines 91-96):
  // DateTime::<Utc>::from_timestamp(unix_secs, 0) then .with_timezone(&tz) -> derive local minute-of-day + weekday
</interfaces>
</context>

<feature>
  <name>lock_status curfew-window evaluator</name>
  <files>crates/mutation-engine/src/lock_status.rs, crates/mutation-engine/src/lib.rs, crates/mutation-engine/tests/lock_status.rs</files>
  <read_first>
    - crates/mutation-engine/src/classify.rs lines 194-220 (read_field defensive-absent reader to mirror), 270-279 (parse_hhmm), 362-365 (parse_window), 29-31 (pure-module doc header)
    - crates/mutation-engine/src/week.rs lines 47-60, 102-104 (tz/DST MappedLocalTime discipline + parse_tz default)
    - crates/mutation-engine/src/grace.rs lines 91-96 (unix-secs -> in-tz instant, derive local minute + weekday)
    - crates/mutation-engine/src/lib.rs lines 18-27 (module list — add pub mod lock_status)
    - crates/mutation-engine/tests/quota.rs lines 18-55 (the EXACT diff-table test discipline: utc() helper, fixed now(), per-case asserts)
    - scripts/guard/nightguard_guard.ps1 (the guard's curfew/grace interpretation the evaluator must agree with — note the guard's verdict half checks grace.window_end > trueNow then defaults to curfew/deny; the "is this minute inside the window" logic is the live hook's job, so treat the boundary minute as advisory per D-04)
  </read_first>
  <behavior>
    Write a SHORT precedence spec as a module doc comment FIRST (resolving Open Q1), then test it:
    - Test 1 (inside window, simple): curfew 23:00-07:00 disabled? no — start/end window, now=00:30 -> locked=true, boundary_kind="curfew_end", boundary_unix = today's 07:00 in tz.
    - Test 2 (outside window, simple): same window, now=12:00 -> locked=false, boundary_kind="next_lock", boundary_unix = today's 23:00 in tz.
    - Test 3 (overnight wrap correctness): 23:00-07:00 with now=23:30 -> locked=true, boundary = NEXT day 07:00 (window crosses midnight; never +24h naive — derive from the wrapped end).
    - Test 4 (non-overnight window): 09:00-17:00, now=08:00 -> locked=false boundary=today 09:00; now=10:00 -> locked=true boundary=today 17:00.
    - Test 5 (enabled=false): curfew.enabled=false -> locked=false, grace_active=false, boundary_kind="next_lock" or a documented "no boundary" sentinel; NEVER treated as locked.
    - Test 6 (schedule.<day> precedence): curfew.schedule.monday = "off" while start/end set -> per spec, Monday is unlocked (schedule overrides start/end for that day); curfew.schedule.tuesday = "20:00-06:00" -> Tuesday uses that window not start/end. Document and test the precedence: schedule.<day> when present (off | HH:MM-HH:MM) overrides start/end for that weekday; absent schedule.<day> falls back to start/end.
    - Test 7 (defensive absent/malformed): missing curfew block, or malformed window string -> locked=true (or the documented fail-safe-locked outcome) — a missing/garbage field is NEVER a silent unlock (mirror classify.rs A2: absent != loosen). Document this as the fail-safe rule.
    - Test 8 (bad tz default): timezone = "Not/AZone" -> parse_tz falls back to UTC, no panic; evaluation still produces a deterministic result.
  </behavior>
  <action>
    Define `pub struct LockStatus { pub locked: bool, pub grace_active: bool, pub boundary_unix: i64, pub boundary_kind: String }` (boundary_kind one of "curfew_end" | "next_lock" | "grace_end"; grace_active wiring is filled by get_state which reads guard.json — lock_status itself may set grace_active=false and let the command overlay the grace window, OR accept an optional grace end; choose the simpler seam and document it). Add `pub mod lock_status;` to crates/mutation-engine/src/lib.rs. Implement `pub fn lock_status(config_yaml: &str, now: chrono::DateTime<chrono::Utc>) -> LockStatus` as a 100% pure fn (no I/O) following the classify.rs/week.rs discipline. Read curfew.enabled, curfew.start, curfew.end, curfew.schedule.<day> (×7 weekdays), and timezone via the same defensive yamlpath read_field pattern (missing -> fail-safe-locked, never silent unlock). Convert `now` to the configured tz (grace.rs:91-96 pattern), derive the local minute-of-day + weekday. Resolve the day's effective window: schedule.<day> if present (off => no curfew that day; "HH:MM-HH:MM" => that window) else curfew.start-curfew.end. Determine locked = minute is inside the (possibly overnight-wrapping) window. Compute the next boundary: if locked, the window end instant (handling the overnight wrap so the end lands on the correct calendar day — never naive +24h, mirror week.rs's explicit MappedLocalTime handling); if not locked, the next window start instant. Reuse classify.rs's parse_hhmm/parse_window — either make them `pub(crate)` and import, or duplicate the two tiny parsers verbatim with identical bounds-checking (do NOT write a divergent parser, PATTERNS line 167). Write crates/mutation-engine/tests/lock_status.rs mirroring quota.rs's table discipline (utc() helper, fixed now(), one #[test] per behavior case above) — RED first, then implement to GREEN.
  </action>
  <verify>
    <automated>cargo test -p mutation-engine --test lock_status</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mutation-engine --test lock_status` exits 0 with all 8+ cases passing.
    - crates/mutation-engine/src/lib.rs contains `pub mod lock_status;`.
    - crates/mutation-engine/src/lock_status.rs contains `pub fn lock_status` and `pub struct LockStatus` with fields locked, grace_active, boundary_unix, boundary_kind.
    - The module doc comment contains the written precedence spec (schedule.<day> overrides start/end; enabled=false => not locked; absent/malformed => fail-safe-locked).
    - There is exactly ONE HH:MM parser in scope for lock_status (either imported pub(crate) from classify or a verbatim duplicate) — grep confirms no second parser with different bounds checks.
    - Overnight-wrap test (now=23:30 inside 23:00-07:00) asserts boundary lands on the NEXT calendar day's 07:00, proving no naive +24h.
    - enabled=false case asserts locked=false.
    - A missing curfew block case asserts locked=true (fail-safe), never false.
  </acceptance_criteria>
  <done>lock_status is a pure, unit-tested function in mutation-engine answering locked/next-boundary for all window cases including overnight-wrap, schedule precedence, enabled=false, and defensive-absent, with a written precedence spec.</done>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yaml (possibly tampered) → lock_status | Untrusted/hand-edited curfew fields parsed by the evaluator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Spoofing | lock_status shows OPEN when the guard would lock | mitigate | Fail-safe-locked on any missing/malformed field (Test 7); overnight-wrap handled explicitly (Test 3); written precedence spec confirmed against the guard's curfew interpretation. The boundary minute is treated as advisory (D-04) so a small disagreement degrades to "time unverified", not a false unlock. |
| T-04-06 | Tampering | malformed window string forces a panic / unlock | mitigate | Defensive yamlpath read (classify.rs A2 pattern): missing path -> fail-safe-locked, malformed -> documented fail-safe outcome, never a panic and never a silent unlock. |
| T-04-07 | Denial of Service | bad timezone string panics the evaluator | mitigate | parse_tz defensive default to UTC (week.rs:102-104); Test 8 proves no panic. |
</threat_model>

<verification>
- `cargo test -p mutation-engine --test lock_status` exits 0.
- All eight behavior cases (inside, outside, overnight-wrap, non-overnight, enabled=false, schedule precedence, defensive-absent, bad-tz) pass.
- The precedence spec is documented in the module header.
- No divergent second HH:MM parser exists.
</verification>

<success_criteria>
A pure, offline-unit-tested `lock_status` evaluator exists in mutation-engine that faithfully answers locked / grace_active / next-boundary from config.yaml + advisory time, handling overnight-wrap, schedule-day precedence, disabled curfew, and defensive-absent fields without ever silently unlocking.
</success_criteria>

<output>
Create `.planning/phases/04-ui-moonlit-indigo/04-02-SUMMARY.md` when done.
</output>
