---
phase: 12-launcher-waybar-presence
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [packaging/omarchy/icons/nightguard.svg]
autonomous: true
requirements: [DESK-04]
must_haves:
  truths:
    - "A hand-authored own-brand SVG (crescent moon on a rounded-square tile, Moonlit-Indigo) exists in the repo"
    - "The SVG rasterizes cleanly at 16px via rsvg-convert (valid, non-empty PNG, exit 0)"
    - "The silhouette (tile + crescent) reads at 16px monochrome-first"
    - "Decisions discharged: D-01 (crescent-moon-on-rounded-tile concept), D-02 (Claude hand-authors the SVG this phase), D-06 (canonical brand artifact shipped in-repo)"
  artifacts:
    - path: "packaging/omarchy/icons/nightguard.svg"
      provides: "The canonical brand icon source (source of truth; installer rasterizes from it)"
      contains: "viewBox=\"0 0 512 512\""
  key_links:
    - from: "nightguard.desktop Icon=org.omarchy.ngtui"
      to: "hicolor-installed org.omarchy.ngtui.png/.svg (rasterized from this SVG by the installer)"
      via: "freedesktop icon-name lookup"
      pattern: "org\\.omarchy\\.ngtui"
---

<objective>
Hand-author the own-brand icon (DESK-04 / D-01 / D-02 — both CONFIRMED by the
user). Concept: a **crescent moon on a rounded-square app tile**. Background = a
rounded-corner square tile; foreground = a crescent (a full circle minus a
smaller offset circle cut from its lower part, via an SVG `<mask>`), sitting on
top; Moonlit-Indigo fills. Must read at 16px monochrome-first. The SVG is the
single source of truth — the installer (Plan 05) rasterizes the hicolor PNG sizes
from it at install time, so no stale committed PNGs.

Purpose: a real, final, iterable brand mark — own-authored, no borrowed logo, no
external tool/asset dependency.
Output: `packaging/omarchy/icons/nightguard.svg`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-launcher-waybar-presence/12-CONTEXT.md
@.planning/phases/12-launcher-waybar-presence/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hand-author the crescent-on-tile brand SVG</name>
  <files>packaging/omarchy/icons/nightguard.svg</files>
  <read_first>
    - .planning/phases/12-launcher-waybar-presence/12-RESEARCH.md §Code Examples "Icon — hand-coded crescent SVG" (concrete geometry: tile rect 16,16 480×480 rx 108; moon circle cx256 cy256 r150; mask cut circle cx316 cy212 r126)
    - .planning/phases/12-launcher-waybar-presence/12-CONTEXT.md §Brand icon D-01/D-02 (the CONFIRMED concept + "reads at 16px monochrome first")
    - CLAUDE.md §Constraints Moonlit-Indigo palette tokens (--bg #0c0e14, --surface #161a24, --border #242a38, --text #e8eaf0, --dim #8b91a3, --accent #7aa2ff)
  </read_first>
  <action>
    Author a `viewBox="0 0 512 512"` SVG. Rounded-square tile: `<rect>` x=16 y=16
    width=480 height=480 rx=108 ry=108, fill `#161a24` (--surface), plus a 8px
    stroke rect in `#242a38` (--border). Crescent via `<defs><mask id="crescent">`:
    a black backing rect, a white moon `<circle>` (cx=256 cy=256 r=150), and a
    black offset cut `<circle>` (start at cx=316 cy=212 r=126 — cut ≈84% of moon
    r, offset toward upper-right). Draw the moon circle filled `#7aa2ff` (--accent)
    with `mask="url(#crescent)"`. Keep the crescent sliver THICK so it survives
    16px (iterate the cut radius/offset and eyeball the 16px raster; the silhouette
    must carry meaning even in monochrome). Own-brand only — no borrowed/existing
    logo. No fenced code beyond authoring the file itself.
  </action>
  <acceptance_criteria>
    - `test -f packaging/omarchy/icons/nightguard.svg` and it contains
      `viewBox="0 0 512 512"`, a `<mask` element, and the accent `#7aa2ff` fill
      (source assertions via grep).
    - Rasterizes cleanly at 16px: `rsvg-convert -w 16 -h 16 packaging/omarchy/icons/nightguard.svg -o "$(mktemp --suffix=.png)"` exits 0 and produces a non-empty valid PNG (`file` reports "PNG image data, 16 x 16").
    - Rasterizes at 512px the same way (exit 0, "512 x 512").
    - The crescent + tile silhouette is visible in the 16px raster (recorded in
      SUMMARY; the subjective "looks good" read is confirmed at the Plan 06
      human-verify, but a blank/invisible 16px raster fails here).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo asset → installed icon | The icon is a static, phase-authored asset; no untrusted data flows in |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Information/IP | borrowed-logo contamination | mitigate | Own-brand hand-authored SVG only (D-01); no external asset/logo copied — CLAUDE.md "own brand (no borrowed logos)". |
| T-12-05 | — | static asset, no runtime input | accept | Icon is a build-time asset consumed by rsvg-convert; no injection surface. |
</threat_model>

<verification>
- SVG exists, well-formed, Moonlit-Indigo fills, `<mask>` crescent.
- rsvg-convert produces valid 16px and 512px PNGs (exit 0).
</verification>

<success_criteria>
A real own-brand crescent-on-tile SVG exists and rasterizes crisply from 16→512
via rsvg-convert; it is the source of truth the installer rasterizes.
</success_criteria>

<output>
Create `.planning/phases/12-launcher-waybar-presence/12-03-SUMMARY.md` when done
</output>
