---
phase: 12-launcher-waybar-presence
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packaging/omarchy/nightguard.desktop
  - packaging/omarchy/hypr/nightguard.windowrule.conf
  - packaging/omarchy/waybar/custom-nightguard.jsonc
  - packaging/omarchy/waybar/style-nightguard.css
  - packaging/omarchy/bin/ngtui-menu
autonomous: true
requirements: [DESK-03, BAR-01, BAR-02, BAR-03]
must_haves:
  truths:
    - "A nightguard.desktop with Exec=xdg-terminal-exec --app-id=org.omarchy.ngtui -e ngtui, Terminal=false, Icon=org.omarchy.ngtui"
    - "A Hyprland windowrule snippet floats+centers+sizes match:class ^(org\\.omarchy\\.ngtui)$ (never title), marker-guarded"
    - "A custom/nightguard Waybar module snippet (exec ngtui status --json, signal 11, on-click open-or-focus, on-click-right ngtui-menu), marker-guarded"
    - "A style.css snippet keyed on the six emitted semantic classes, marker-guarded"
    - "An ngtui-menu walker --dmenu read-only script with NO loosening/commit/grace ACTION line"
    - "Decisions discharged: D-04 (walker --dmenu read-only header + open-only actions), D-05 (hover tooltip stays the ngtui status tooltip; the menu is the richer surface), D-06 (canonical .desktop/module/style/menu artifacts shipped in-repo), D-09 (fixed semantic style classes, keyed on the emitted verdict classes)"
  artifacts:
    - path: "packaging/omarchy/nightguard.desktop"
      provides: "DESK-03 launcher entry"
      contains: "app-id=org.omarchy.ngtui"
    - path: "packaging/omarchy/hypr/nightguard.windowrule.conf"
      provides: "float/center/size on the stable app_id"
      contains: "match:class ^(org\\.omarchy\\.ngtui)$"
    - path: "packaging/omarchy/waybar/custom-nightguard.jsonc"
      provides: "BAR-01/02/03 module definition"
      contains: "\"signal\": 11"
    - path: "packaging/omarchy/waybar/style-nightguard.css"
      provides: "D-09 fixed semantic class colors"
      contains: "#custom-nightguard.locked"
    - path: "packaging/omarchy/bin/ngtui-menu"
      provides: "BAR-03 read-only right-click menu"
      contains: "walker --dmenu"
  key_links:
    - from: "nightguard.desktop Exec"
      to: "Hyprland windowrule match:class ^(org.omarchy.ngtui)$"
      via: "xdg-terminal-exec --app-id → Alacritty --class → Wayland app_id"
      pattern: "org\\.omarchy\\.ngtui"
    - from: "custom/nightguard on-click / on-click-right"
      to: "omarchy-launch-or-focus-tui ngtui / ngtui-menu"
      via: "Waybar click handlers"
      pattern: "omarchy-launch-or-focus-tui ngtui"
---

<objective>
Author the repo's canonical omarchy-integration artifacts (D-06) — the files the
installer (Plan 05) merges onto the live box. All five are static, phase-authored
text; each live-config snippet carries the `>>> nightguard (managed) >>>` /
`<<< nightguard <<<` guard markers so the installer's injection is idempotent.
Covers DESK-03 (launcher + float rule), BAR-01 (module + colors), BAR-02
(left-click open-or-focus), BAR-03 (right-click read-only menu).

Load-bearing invariants:
- Float rule matches the stable `app_id`/class `org.omarchy.ngtui`, NEVER title.
- The module is key-less (`ngtui status --json` only — no sudo/NTP/key) and
  declares `"signal": 11` (matches Plan 02's `pkill -RTMIN+11`).
- "A window, never a lever": `ngtui-menu` has read-only header lines + open-only
  actions; NO loosening/commit/grace action anywhere.

Purpose: ship native-omarchy-looking artifacts that mirror scouted idioms.
Output: the five files under `packaging/omarchy/`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-launcher-waybar-presence/12-CONTEXT.md
@.planning/phases/12-launcher-waybar-presence/12-RESEARCH.md

<interfaces>
The status object the module renders and the menu parses (from ngtui/ngtui/status.py):
- Live: `{"text": "<glyph> <n>", "tooltip": "<WORD> · <n> token(s) left", "class": "<verdict>"}`
- Fail-closed: `{"text": "○ —", "class": "unavailable"}` (NOTE: no `tooltip` key on the fail branch — the menu must `.get("tooltip", "")` defensively).
- Emitted classes: `locked`, `grace_active`, `outside_curfew`, `clock_tamper`, `offline_blocked`, `unavailable`.
Reuse (BAR-02): `omarchy-launch-or-focus-tui ngtui` — sets app_id org.omarchy.ngtui, focuses existing or launches. Do NOT roll a custom focus script.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: nightguard.desktop + Hyprland windowrule snippet (DESK-03)</name>
  <files>packaging/omarchy/nightguard.desktop, packaging/omarchy/hypr/nightguard.windowrule.conf</files>
  <read_first>
    - .planning/phases/12-launcher-waybar-presence/12-RESEARCH.md §Code Examples "nightguard.desktop" and "Hyprland windowrule snippet"
    - .planning/phases/12-launcher-waybar-presence/12-RESEARCH.md §Pattern 1 (app-id propagation) and §Pattern 2 (explicit 3-line float rule — NOT the tag one-liner)
    - .planning/ROADMAP.md §Phase 12 SC-1 (floating/centered, match app-id not title, other terminals still tile)
  </read_first>
  <action>
    `nightguard.desktop`: a `[Desktop Entry]` with `Type=Application`,
    `Name=Nightguard Control`, `Exec=xdg-terminal-exec --app-id=org.omarchy.ngtui
    -e ngtui`, `Icon=org.omarchy.ngtui`, `Terminal=false` (xdg-terminal-exec
    supplies the terminal itself), `Categories=Utility;`, `StartupNotify=false`.
    `nightguard.windowrule.conf`: the explicit, order-independent 3-line block
    (float on / center on / size 875 600), each rule
    `match:class ^(org\.omarchy\.ngtui)$` — NEVER `title`. Wrap the block in
    `# >>> nightguard (managed) — do not edit inside markers >>>` … `# <<< nightguard (managed) <<<`.
    Ship the explicit 3-line form (self-contained), not `tag +floating-window`.
  </action>
  <acceptance_criteria>
    - `grep -q 'app-id=org.omarchy.ngtui' packaging/omarchy/nightguard.desktop`
      AND `grep -q 'Terminal=false' packaging/omarchy/nightguard.desktop`
      AND `grep -q 'Icon=org.omarchy.ngtui' packaging/omarchy/nightguard.desktop`
      AND `grep -q -- '-e ngtui' packaging/omarchy/nightguard.desktop`.
    - Windowrule matches class not title: `grep -q 'match:class \^(org\\.omarchy\\.ngtui)\$' packaging/omarchy/hypr/nightguard.windowrule.conf`
      AND `grep -qi 'float on' …` AND `grep -qi 'center on' …` AND `grep -q 'size 875 600' …`.
    - No title match: `! grep -qi 'match:title' packaging/omarchy/hypr/nightguard.windowrule.conf`.
    - Guard markers present: `grep -q '>>> nightguard (managed)' packaging/omarchy/hypr/nightguard.windowrule.conf`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: custom/nightguard Waybar module + style.css snippet (BAR-01/BAR-02)</name>
  <files>packaging/omarchy/waybar/custom-nightguard.jsonc, packaging/omarchy/waybar/style-nightguard.css</files>
  <read_first>
    - .planning/phases/12-launcher-waybar-presence/12-RESEARCH.md §Code Examples "Waybar module snippet" and "Waybar style snippet"
    - .planning/phases/12-launcher-waybar-presence/12-RESEARCH.md §Pattern 3 (custom/* idiom, return-type json) and §Pattern 4 (omarchy-launch-or-focus-tui reuse)
    - .planning/phases/12-launcher-waybar-presence/12-CONTEXT.md §Bar appearance D-09/D-10 (fixed semantic classes; text stays glyph+tokens)
  </read_first>
  <action>
    `custom-nightguard.jsonc`: a marker-guarded (`// >>> nightguard (managed) >>>`
    … `// <<< nightguard (managed) <<<`) `"custom/nightguard"` object with
    `"return-type": "json"`, `"exec": "ngtui status --json"`, `"interval": 30`,
    `"signal": 11`, `"format": "{}"`, `"on-click": "omarchy-launch-or-focus-tui
    ngtui"` (BAR-02 — reuse, don't roll), `"on-click-right": "ngtui-menu"`
    (BAR-03). Key-less: NO sudo/NTP/key in the exec. `style-nightguard.css`: a
    marker-guarded block keyed on the six emitted classes — `#custom-nightguard`
    base rule plus `.locked`/`.clock_tamper`/`.offline_blocked` (danger red),
    `.grace_active` (amber), `.outside_curfew` (open green), `.unavailable` (dim
    #8b91a3). Signal 11 confirmed free (live config uses 7/8/9/10).
  </action>
  <acceptance_criteria>
    - `grep -q '"signal": 11' packaging/omarchy/waybar/custom-nightguard.jsonc`
      AND `grep -q '"exec": "ngtui status --json"' …`
      AND `grep -q 'omarchy-launch-or-focus-tui ngtui' …`
      AND `grep -q '"on-click-right": "ngtui-menu"' …`
      AND `grep -q '"return-type": "json"' …`.
    - Key-less exec: `! grep -Eq 'sudo|--json.*key|guardkey' packaging/omarchy/waybar/custom-nightguard.jsonc`.
    - All six classes styled: `for c in locked grace_active outside_curfew clock_tamper offline_blocked unavailable; do grep -q "#custom-nightguard.$c" packaging/omarchy/waybar/style-nightguard.css || exit 1; done`.
    - Guard markers present in both files.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: ngtui-menu — walker read-only right-click menu (BAR-03)</name>
  <files>packaging/omarchy/bin/ngtui-menu</files>
  <read_first>
    - .planning/phases/12-launcher-waybar-presence/12-RESEARCH.md §Code Examples "ngtui-menu — walker read-only right-click menu" and §Pattern 6
    - ngtui/ngtui/status.py (the `{text,tooltip,class}` shape + the `UNAVAILABLE` fail branch WITHOUT a tooltip key — parse with `.get("tooltip","")`)
    - .planning/phases/12-launcher-waybar-presence/12-CONTEXT.md §Right-click menu D-04 (read-only header + open-only actions; "no loosening action exists")
    - .planning/phases/12-launcher-waybar-presence/12-VALIDATION.md §Per-Task Verification Map (BAR-03 no-loosen grep)
  </read_first>
  <action>
    A `#!/usr/bin/env bash` (`set -euo pipefail`) script: run `ngtui status --json`
    (fallback to the `unavailable` object on error), parse `tooltip`/`text`
    defensively with `python3 -c 'import sys,json;print(json.load(sys.stdin).get("tooltip",""))'`
    (grace remaining and tokens are DATA from the tooltip field, NOT literal
    keywords in the script). Feed `walker --dmenu -p "Nightguard"` newline-
    separated: inert header lines (the tooltip, the status text, a divider) then
    two ACTION lines "Open editor" and "Open status". `case` on the selection:
    only "Open editor"/"Open status" `exec omarchy-launch-or-focus-tui ngtui`;
    every other line (headers/divider) is a no-op `exit 0`. There is NO loosen /
    commit / grace / token-spend ACTION. `chmod +x` the file. The single allowed
    descriptive comment may say "a window, never a lever" but no EXECUTABLE line
    may invoke a mutation.
  </action>
  <acceptance_criteria>
    - `grep -q 'walker --dmenu' packaging/omarchy/bin/ngtui-menu`
      AND `grep -q 'omarchy-launch-or-focus-tui ngtui' packaging/omarchy/bin/ngtui-menu`.
    - No-loosen invariant (comment-filtered per grep hygiene): non-comment lines
      contain no mutation verb —
      `grep -vE '^[[:space:]]*#' packaging/omarchy/bin/ngtui-menu | grep -Eiq 'loosen|token[^s]*spend|commit|use_grace|grace[[:space:]]+(grant|add)' && exit 1 || exit 0`
      exits 0 (i.e. NO match on a non-comment line).
    - Only open actions in the `case`: `grep -Eq 'Open (editor|status)' packaging/omarchy/bin/ngtui-menu`.
    - Script is executable: `test -x packaging/omarchy/bin/ngtui-menu`.
    - `bash -n packaging/omarchy/bin/ngtui-menu` (syntax check) exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bar/menu/launcher surface → curfew state | New outward surfaces must be read-only or open-only |
| window class → float rule | The compositor must key on the stable app_id, not a spoofable title |
| ngtui status --json → menu parse | Menu consumes JSON; must parse defensively (fail-closed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | EoP (defeats self-binding) | any bar/menu/launcher action that loosens the curfew | mitigate | `ngtui-menu` is read-only header + open-only actions; no-loosen grep gate on non-comment lines (BAR-03 invariant). The module's only write reachable is "open the friction-gated editor." |
| T-12-07 | Spoofing | window-title spoof dodges the float rule | mitigate | Float rule matches `class ^(org\.omarchy\.ngtui)$`, never title (SC-1); `! grep match:title` gate. |
| T-12-08 | Input validation | malformed `ngtui status --json` crashes the menu | mitigate | `ngtui status` is fail-closed exit 0 (Phase 11); the menu falls back to the `unavailable` object and `.get("tooltip","")`s defensively. |
| T-12-09 | Tampering | JSON key/sudo leaks into the key-less module exec | mitigate | `! grep sudo|guardkey` gate on the module snippet; exec is `ngtui status --json` only. |
</threat_model>

<verification>
- All grep gates above pass.
- `bash -n packaging/omarchy/bin/ngtui-menu` clean; script executable.
- No `match:title`; no-loosen grep clean on non-comment lines.
</verification>

<success_criteria>
Five canonical artifacts exist, mirror scouted omarchy idioms, carry guard
markers, match the stable app_id (not title), declare signal 11, reuse
`omarchy-launch-or-focus-tui`, and the menu is provably read-only/open-only.
</success_criteria>

<output>
Create `.planning/phases/12-launcher-waybar-presence/12-04-SUMMARY.md` when done
</output>
