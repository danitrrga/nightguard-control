---
phase: 12-launcher-waybar-presence
plan: 06
type: execute
wave: 3
depends_on: [12-01, 12-02, 12-05]
files_modified: []
autonomous: false
requirements: [DESK-03, DESK-04, BAR-01, BAR-02, BAR-03, BAR-04]
must_haves:
  truths:
    - "install.sh has run on the live box; markers present in config.jsonc/hyprland.conf/style.css; backups created"
    - "hicolor org.omarchy.ngtui PNGs (16→512) + scalable SVG installed; icon cache refreshed without relogin"
    - "custom/nightguard renders live in Waybar, class-colored by verdict, from ngtui status --json"
    - "Launching from walker opens ngtui as a floating, centered window with app_id org.omarchy.ngtui; other terminals still tile"
    - "Left-click opens/focuses the TUI; right-click shows the read-only menu (no loosen action)"
    - "A real loosen-with-token commit (PTY intact) refreshes the bar instantly via SIGRTMIN+11"
  artifacts:
    - path: "~/.config/waybar/config.jsonc"
      provides: "live custom/nightguard module (guard-marked, backed up)"
      contains: "custom/nightguard"
    - path: "~/.local/share/applications/nightguard.desktop"
      provides: "live launcher entry"
      contains: "org.omarchy.ngtui"
  key_links:
    - from: "walker / bar on-click"
      to: "floating org.omarchy.ngtui window (real PTY)"
      via: "xdg-terminal-exec --app-id → Alacritty --class → Hyprland float rule"
      pattern: "org\\.omarchy\\.ngtui"
    - from: "successful commit"
      to: "instant bar refresh"
      via: "pkill -RTMIN+11 waybar → module signal 11"
      pattern: "RTMIN\\+11"
---

<objective>
Land the integration on the author's live box and verify every session-bound
behavior the headless tests cannot. Run the proven-idempotent installer against
the real `~/.config/{waybar,hypr}` + `~/.local/share/{applications,icons}`
(backup-first), auto-verify the post-conditions, reload Waybar + Hyprland, then
walk the GUI behaviors at a human-verify checkpoint. This is the phase gate.

These are outward, hard-to-reverse edits to the author's live desktop — the
installer backs up first and is idempotent (proven in Plan 05), but the plan is
`autonomous: false`: a human confirms the window floats, the icon shows, the bar
renders, the menu is read-only, and a real commit refreshes the bar.

Purpose: prove all 5 phase success criteria TRUE on the live system.
Output: a live, working omarchy surface + a completed phase-gate checkpoint.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-launcher-waybar-presence/12-RESEARCH.md
@.planning/phases/12-launcher-waybar-presence/12-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run install.sh on the live box + auto-verify post-conditions</name>
  <files>~/.config/waybar/config.jsonc, ~/.config/waybar/style.css, ~/.config/hypr/hyprland.conf, ~/.local/share/applications/nightguard.desktop, ~/.local/share/icons/hicolor</files>
  <read_first>
    - packaging/omarchy/install.sh (the installer being run — its backup/marker/reload behavior)
    - .planning/phases/12-launcher-waybar-presence/12-RESEARCH.md §Open Questions (Q1 SIGUSR2 new-module reload; Q3 modules-center position) and §Assumptions Log A1/A2/A3
    - ~/.config/waybar/config.jsonc (confirm signal 11 still free before relying on it — live re-check)
  </read_first>
  <action>
    First re-confirm the free signal slot on the live config:
    `grep -o '"signal": [0-9]*' ~/.config/waybar/config.jsonc` — verify 11 is not
    now used (research verified 7/8/9/10 taken; abort/adjust if 11 appeared).
    Then run `bash packaging/omarchy/install.sh` with DEFAULT paths (real box).
    The installer backs up each mutated config to `*.bak.<epoch>` before editing.
    After it completes, reload: `pkill -SIGUSR2 waybar` and confirm the module
    renders; if the newly-added module does NOT appear (Open Q1 / A1), fall back
    to a full waybar restart (`omarchy-restart-waybar` or respawn via uwsm).
    Hyprland auto-reloads on file change; run `hyprctl reload` to be sure (A3).
    This is a live, hard-to-reverse mutation — if any auto-verify below fails,
    STOP and surface it (backups exist for rollback).
  </action>
  <acceptance_criteria>
    - Guard markers present live: `grep -q '>>> nightguard (managed)' ~/.config/waybar/config.jsonc && grep -q '>>> nightguard (managed)' ~/.config/hypr/hyprland.conf && grep -q '>>> nightguard (managed)' ~/.config/waybar/style.css`.
    - Backups created: at least one `~/.config/waybar/config.jsonc.bak.*` exists.
    - Module membership: `"custom/nightguard"` present in a `modules-*` array in config.jsonc (grep).
    - Icon installed: `test -f ~/.local/share/icons/hicolor/16x16/apps/org.omarchy.ngtui.png && test -f ~/.local/share/icons/hicolor/512x512/apps/org.omarchy.ngtui.png && test -f ~/.local/share/icons/hicolor/scalable/apps/org.omarchy.ngtui.svg`.
    - .desktop installed + valid: `test -f ~/.local/share/applications/nightguard.desktop && desktop-file-validate ~/.local/share/applications/nightguard.desktop` (or grep the key fields if the validator is absent).
    - ngtui-menu on PATH: `command -v ngtui-menu` non-empty.
    - `ngtui status --json` still exit 0 + jq-valid (regression).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase-gate human-verify — live desktop behaviors</name>
  <action>
    Pause for the user to walk the five session-bound GUI behaviors below (float,
    icon, bar render, click surfaces, commit refresh). Do NOT auto-approve — this
    is the phase gate confirming the anti-impulse guarantee ("a window, never a
    lever") holds from every new surface, and the real-PTY commit path survives
    launching from the bar.
  </action>
  <what-built>
    The live omarchy integration: a `nightguard.desktop` launcher with the
    crescent-on-tile brand icon; a `custom/nightguard` Waybar module (glyph +
    tokens, class-colored); left-click open-or-focus; a read-only right-click
    menu; and an instant post-commit bar refresh (SIGRTMIN+11). All installed by
    the idempotent, backup-first installer in Task 1.
  </what-built>
  <how-to-verify>
    1. LAUNCH + FLOAT (DESK-03/SC-1): open walker, find "Nightguard Control"
       (crescent-on-tile icon should render WITHOUT relogin — SC-2/DESK-04),
       launch it. Confirm ngtui opens as a FLOATING, CENTERED window. Verify the
       app_id: `hyprctl clients -j | jq '.[]|select(.class=="org.omarchy.ngtui")|{class,floating}'`
       shows `floating:true`. Open a normal terminal — it should still TILE.
    2. BAR RENDER (BAR-01): confirm `custom/nightguard` shows a lock glyph +
       token count in the bar, colored per the current verdict (red locked /
       amber grace / green open / dim unavailable).
    3. LEFT-CLICK (BAR-02): left-click the module → it opens/focuses the floating
       TUI (same window, not a duplicate).
    4. RIGHT-CLICK (BAR-03): right-click the module → a walker menu shows read-only
       header lines (verdict / tokens / grace-in-tooltip) and ONLY "Open editor" /
       "Open status" actions. Confirm there is NO loosen / grant-grace / commit
       action anywhere in the menu.
    5. COMMIT REFRESH (BAR-04/SC-4, PTY intact): from the launched TUI, make a
       real loosen-with-token edit and commit it — the inline `sudo` prompt must
       appear in the terminal (real PTY), the commit must succeed, and the bar's
       token count must update INSTANTLY (before the 30s poll), driven by
       SIGRTMIN+11. Confirm the guard did NOT revert the sanctioned commit.
  </how-to-verify>
  <resume-signal>Type "approved" once all five behaviors are confirmed, or describe which failed (backups from Task 1 allow rollback).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installer → live desktop configs | Live, hard-to-reverse mutation of the author's box |
| launcher/bar/menu → curfew state | Every live surface must be read-only or open-only; only the friction-gated commit mutates |
| window class → float rule | Live float binds to the stable app_id, not a spoofable title |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 (live) | DoS (author locks self out) | live-config merge | mitigate | Installer is proven idempotent + backup-first (Plan 05); Task 1 verifies backups exist and markers are single before declaring success; SIGUSR2/restart + `hyprctl reload` verified. |
| T-12-06 (live) | EoP (defeats self-binding) | right-click menu / bar surfaces | mitigate | Human-verify step 4 confirms NO loosen/grace/commit action in the live menu; only open actions reachable. |
| T-12-07 (live) | Spoofing | float rule dodge via title | mitigate | Human-verify step 1 checks the live window `class == org.omarchy.ngtui` (app_id), never title. |
| T-12-02 (live) | Tampering | signal fakes/perturbs commit | mitigate | Human-verify step 5 confirms the bar refreshes ONLY after a genuine successful commit; a refused commit leaves the bar unchanged (Plan 02 unit-proven, live-confirmed). |
| T-12-01 (live) | Tampering/EoP | rogue `ngtui` on PATH | mitigate | Plan 01 installed the pinned uv-tool ngtui; live re-check `command -v ngtui`/`ngtui-menu`; backend pins NIGHTGUARD_STACK_DIR (CR-02). |
</threat_model>

<verification>
- Task 1 auto-verify: markers present, backups created, icon + .desktop + menu installed, module in modules-*.
- Human-verify checkpoint: all five live behaviors confirmed (float, icon, render, click, commit-refresh).
- Full ngtui suite still green (`cd ngtui && uv run pytest -q`).
</verification>

<success_criteria>
All five Phase 12 success criteria are TRUE on the live box: floating-terminal
launcher matching app_id (others tile), brand icon showing without relogin,
key-less class-colored bar module, left-click-open / right-click-read-only-menu
(no lever), and instant post-commit refresh via SIGRTMIN+11 with the PTY commit
path intact.
</success_criteria>

<output>
Create `.planning/phases/12-launcher-waybar-presence/12-06-SUMMARY.md` when done
</output>
