---
phase: 12-launcher-waybar-presence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [DESK-03, BAR-01]
must_haves:
  truths:
    - "`command -v ngtui` resolves to `~/.local/bin/ngtui`"
    - "`ngtui status --json` emits a single jq-valid Waybar object, exit 0"
    - "The full existing ngtui pytest suite is green (D-10 regression baseline)"
  artifacts:
    - path: "~/.local/bin/ngtui"
      provides: "The global command the .desktop/module/menu all invoke"
      contains: "installed uv-tool shim"
  key_links:
    - from: "waybar module exec / .desktop Exec / ngtui-menu"
      to: "~/.local/bin/ngtui"
      via: "bare `ngtui` on the session PATH (Waybar PATH includes ~/.local/bin)"
      pattern: "command -v ngtui"
---

<objective>
BLOCKING prerequisite gate. `ngtui` is NOT on PATH right now (Phase 11's
`~/.local/bin/ngtui` is absent — verified live). Every runtime surface this phase
wires (the `.desktop` `Exec`, the Waybar module `exec`, the `ngtui-menu` script)
invokes bare `ngtui`. Until it resolves, all live wiring is dead. This plan
(re)installs it, hard-verifies it resolves and answers, and captures the green
pre-change test baseline that later plans must not regress (D-10: no `status.py`
change).

Purpose: unblock every live-wiring task; establish the regression baseline.
Output: a resolvable `ngtui` command + a proven-green baseline.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-launcher-waybar-presence/12-RESEARCH.md
@.planning/phases/12-launcher-waybar-presence/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] (Re)install ngtui and verify it resolves + answers</name>
  <files>~/.local/bin/ngtui</files>
  <read_first>
    - ngtui/pyproject.toml (the `[project.scripts] ngtui = ngtui.__main__:main` entry + `[tool.pytest.ini_options]`)
    - ngtui/ngtui/__main__.py (the `ngtui status [--json]` router — fail-closed, exit 0)
    - .planning/phases/12-launcher-waybar-presence/12-RESEARCH.md §Environment Availability (the MISSING-ngtui row + reinstall command)
  </read_first>
  <action>
    From the `ngtui/` directory run `uv tool install --python 3.14 .` (the exact
    Phase 11 install command — idempotent; re-running upgrades in place). This
    places `~/.local/bin/ngtui`. Do NOT alter PATH — Waybar's PATH already
    includes `~/.local/bin` (verified live). This is the gate: if the install
    fails or `command -v ngtui` is empty afterward, STOP the phase — no later
    live-wiring task can proceed. Do not edit `status.py`, `__main__.py`, or
    `backend.py` in this task.
  </action>
  <acceptance_criteria>
    - `command -v ngtui` prints a non-empty path ending in `/ngtui`.
    - `ngtui status --json` exits 0 and emits exactly one line that parses as
      JSON and contains a `class` key: `ngtui status --json | python3 -c 'import sys,json;o=json.load(sys.stdin);assert "class" in o and "text" in o;print("ok")'` prints `ok`.
    - Exit code of `ngtui status --json` is 0 even on a degraded stack (fail-closed).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Capture the green pre-change test baseline (D-10 guard)</name>
  <files>ngtui/tests/</files>
  <read_first>
    - ngtui/tests/test_status.py and ngtui/tests/test_status_cli.py (the D-10 contract these guard: `status.py` emits `{text,tooltip,class}` unchanged)
    - .planning/phases/12-launcher-waybar-presence/12-VALIDATION.md §Sampling Rate (full-suite guard)
  </read_first>
  <action>
    Run the full existing ngtui suite to record a GREEN baseline BEFORE any
    backend edit lands in later plans. This is the regression anchor for D-10
    (no change to `status.py`). Make no code changes; if the suite is red on a
    clean tree, surface it — a red baseline invalidates the phase's no-change
    contract and must be resolved first.
  </action>
  <acceptance_criteria>
    - `cd ngtui && uv run pytest -q` exits 0 (all tests pass).
    - Record the passing test count in the SUMMARY so later plans can assert the
      count did not drop.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PATH → executed binary | `ngtui` must resolve to the pinned uv-tool install, not an arbitrary PATH entry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering/EoP | `ngtui` resolves to a rogue PATH binary | mitigate | Install from the first-party `ngtui/` source via `uv tool install`; `backend.py` already pins `NIGHTGUARD_STACK_DIR` and refuses an unverified stack (CR-02, existing). Verify `command -v ngtui` points into `~/.local/bin`. |
| T-12-SC | Tampering | package installs | accept | No npm/pip/cargo third-party install — `uv tool install .` builds the first-party wheel only; RESEARCH §Package Legitimacy Audit records "zero external packages." |
</threat_model>

<verification>
- `command -v ngtui` non-empty; `ngtui status --json` jq-valid, exit 0.
- `cd ngtui && uv run pytest -q` green.
</verification>

<success_criteria>
ngtui resolves on PATH and answers `status --json` fail-closed; the ngtui suite
is green (baseline recorded). Later live-wiring plans are unblocked.
</success_criteria>

<output>
Create `.planning/phases/12-launcher-waybar-presence/12-01-SUMMARY.md` when done
(record the passing test count as the D-10 baseline).
</output>
