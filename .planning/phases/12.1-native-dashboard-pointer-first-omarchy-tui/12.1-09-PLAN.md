---
phase: 12.1-native-dashboard-pointer-first-omarchy-tui
plan: 09
type: execute
wave: 7
depends_on: ["12.1-08"]
files_modified:
  - .planning/phases/12.1-native-dashboard-pointer-first-omarchy-tui/12.1-VALIDATION.md
autonomous: false
requirements: [UIX-01, UIX-02, UIX-03, UIX-04, UIX-05, UIX-06]

must_haves:
  truths:
    - "Every control this phase added has been SEEN to fail, and its failure message is on record"
    - "The trust path behaves exactly as before, proven by the existing suite staying green"
    - "Switching the real desktop theme restyles the running TUI, borders and fills included"
    - "It reads as a native Omarchy application rather than a terminal form"
  artifacts:
    - path: ".planning/phases/12.1-native-dashboard-pointer-first-omarchy-tui/12.1-VALIDATION.md"
      provides: "The control ledger closed out — every row with its evidence"
      contains: "nyquist_compliant"
  key_links:
    - from: "the installed ngtui shim"
      to: "ngtui/ngtui/app.tcss"
      via: "uv tool install --python 3.14 . --force — the repo is not what runs"
      pattern: "uv tool install"
---

<objective>
Close the phase: audit that every new control was seen to fail before it was made to pass,
and verify on the real desktop the two things no automated test can reach.

The phase's definition of done is the user's own: *every new check must be seen to fail
before it is made to pass, and its failure message recorded.* A test never seen to fail is
not evidence. Three of these controls were measured failing during research and reuse those
figures; the rest were demonstrated by the plans that added them. This plan collects the
ledger and checks it is complete.

Two verifications are manual because they are not reachable otherwise. The automated
control proves the repaint path with a fixture; only a real `omarchy theme set` proves the
watcher, the file paths and the deployed entry point line up. And whether it reads as a
native app is aesthetic judgement, which is the user's — it is the reason the phase exists.

Purpose: the surface that ships is not the repo. `ngtui` runs from `~/.local/bin/ngtui`,
resolving to an isolated venv separate from `ngtui/.venv`, and `app.tcss` is packaged wheel
data. Editing the repo does not update the installed shim — so without a reinstall the
walkthrough would verify the **old** UI and pass for the wrong reason.
Output: a closed control ledger and a human-verified phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12.1-native-dashboard-pointer-first-omarchy-tui/12.1-CONTEXT.md
@.planning/phases/12.1-native-dashboard-pointer-first-omarchy-tui/12.1-VALIDATION.md
@.planning/phases/12.1-native-dashboard-pointer-first-omarchy-tui/12.1-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close the control ledger and audit the trust path</name>
  <files>.planning/phases/12.1-native-dashboard-pointer-first-omarchy-tui/12.1-VALIDATION.md</files>

  <read_first>
    - `.planning/phases/12.1-native-dashboard-pointer-first-omarchy-tui/12.1-VALIDATION.md` —
      the file being modified: the per-task verification map keyed by control number, the
      negative-controls table, the Wave 0 checklist and the sign-off checklist
    - Every SUMMARY from this phase:
      `12.1-01-SUMMARY.md` through `12.1-08-SUMMARY.md` — the recorded failure messages
    - `.planning/phases/12.1-native-dashboard-pointer-first-omarchy-tui/12.1-UI-SPEC.md`
      §13 — the twelve controls the contract must pass
    - `.planning/phases/12.1-native-dashboard-pointer-first-omarchy-tui/12.1-CONTEXT.md`
      the scope fence, verbatim, and the baseline figure
  </read_first>

  <action>
    **Run the full suite and record the exact line.**
    Baseline entering the phase was **458 passed, 1 failed, 3 skipped in 3.18 s**. The one
    failure is deliberate and named:
    `tests/test_theme_sync.py::test_every_installed_theme_maps_to_a_complete_palette[dos-moos]`
    — `dos-moos.surface is #343f44, which the theme does not declare — it fell back`. It
    stays red because `panel.py:232` feeds the finished bar panel, which the user placed out
    of bounds. "Stays green" means no *new* red, and that is what must hold.

    **Audit the control ledger.** Walk `12.1-VALIDATION.md`'s per-task map, row by row —
    controls 1, 2, 3, 3b, 4, 4b, 4c, 5, 6, 6b, 6c, 7, 8, 9, 10, 11, 11b, 11c, 12, 12b — and
    for each record: which test file and function implements it, that it passes, and **the
    verbatim message it produced when it was made to fail**. Three were measured during
    research and reuse those figures rather than being re-derived: control 3 (the border,
    fill and accent all unchanged after a theme reassignment), control 4 (26 of 28 themes
    collapsing the verdict roles), and control 7 (`content_size` `(130,1)` → `(129,1)` under
    `border-right` and `(130,1)` → `(130,0)` under `border-top`, with `region` failing to
    discriminate under either).

    **If any row lacks a recorded failure message, it is not closed.** Demonstrate it now and
    record it. A control never seen to fail is not evidence, and this is the row where that
    gets caught rather than in six months.

    **Audit the trust path by diff, not by assertion.** Confirm each is byte-identical to its
    pre-phase state:
    `git diff --stat <phase-base> -- ngtui/ngtui/backend.py ngtui/ngtui/panel.py scripts/linux/nightguard_ctl.py packaging/omarchy/ src/ src-tauri/`
    must be empty. And confirm the trust-path suite is green:
    `tests/test_port03_no_crypto.py`, `tests/test_run_sudo_pty.py`,
    `tests/test_port02_commit_argv.py`, `tests/test_edit_window.py`,
    `tests/test_edit_window_seam.py`, `tests/test_edit_window_ui.py`.

    **Audit the two standing greps** that enforce decisions no single test owns:
      - no literal hex in `app.tcss` or `ngtui/ngtui/widgets/*.py` — control 1 owns this and
        must be green with every file the phase added inside its glob
      - `_compositor` appears nowhere under `ngtui/ngtui/` — the private API stays in tests

    Update `12.1-VALIDATION.md`: set `wave_0_complete: true`, set `nyquist_compliant: true`,
    fill the status of every row, and tick the sign-off checklist. Set **Approval** only if
    every box is genuinely ticked.
  </action>

  <verify>
    <automated>cd ngtui && uv run --with pytest --with pytest-asyncio pytest -q</automated>
  </verify>

  <acceptance_criteria>
    - The full suite reports **no new red**: the single failure is the named `[dos-moos]`
      baseline and nothing else. Record the exact summary line and the runtime.
    - Every one of the twenty control rows in `12.1-VALIDATION.md` carries a test reference
      **and** a verbatim failure message. Zero rows blank.
    - `git diff --stat` against the phase base is empty for `ngtui/ngtui/backend.py`,
      `ngtui/ngtui/panel.py`, `scripts/linux/nightguard_ctl.py`, `packaging/omarchy/`,
      `src/` and `src-tauri/`.
    - `tests/test_theme_sync.py` is unmodified and its `[dos-moos]` case is still red and
      still named.
    - `grep -rc '_compositor' ngtui/ngtui/` returns 0 across the package.
    - `tests/test_no_fixed_hex.py` and `tests/test_port01_single_key_bindings.py` pass.
    - `12.1-VALIDATION.md` has `nyquist_compliant: true` and a completed sign-off.
  </acceptance_criteria>

  <done>Every control is implemented, passing, and on record as having been seen to fail; the trust path is provably untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live walkthrough on the real desktop</name>
  <files>none — verification only, no file is written by this task</files>

  <read_first>
    - `.planning/phases/12.1-native-dashboard-pointer-first-omarchy-tui/12.1-VALIDATION.md`
      "Manual-Only Verifications" and "Phase gate" — the two themes to test and why each is
      needed
    - `.planning/phases/12.1-native-dashboard-pointer-first-omarchy-tui/12.1-RESEARCH.md`
      "Runtime State Inventory" → the build-artifacts row: `ngtui` is installed at
      `~/.local/bin/ngtui` via `uv tool install`, resolving to an isolated venv **separate
      from `ngtui/.venv`**, and `app.tcss` is packaged wheel data — so editing the repo does
      not update the installed shim
  </read_first>

  <action>
    Pause for the user. Present the reinstall command and the eight walkthrough steps below,
    wait for their verdict, and record it verbatim. Do not proceed past this task on an
    assumption — the two things checked here are the two no automated control in this phase
    can reach, which is exactly why they are a human gate rather than a test.
  </action>

  <what-built>
    The TUI restyled from the live desktop theme's structural tokens — `[controls]` state
    fills and border alphas, `[spacing]`, `[font]` and Hyprland's live `decoration:rounding`
    — driven by one cursor shared between pointer and keyboard, with the day rendered as a
    full-width ASCII density ramp. Framework header and footer removed; discoverability moved
    to a `help` chip. All twenty automated controls pass, each having been seen to fail.
  </what-built>

  <how-to-verify>
    1. **Reinstall first, or you will be testing the old UI.** In `ngtui/`:
       `uv tool install --python 3.14 . --force`
       The repo is not what runs. Skipping this step makes every check below pass or fail for
       the wrong reason.

    2. Launch the TUI from the launcher (the floating `875 600` window, not a tiled terminal).

    3. **UIX-01, the live restyle.** With the app open, run `omarchy theme set city-783`.
       `city-783` is one of only two themes shipping a hand-written `shell.toml`, and the one
       that also pins a gradient border and `focus-border-width = 2` — so it exercises the
       per-rung colour override, the gradient rejection and the width clamp in one go.
       **Borders and fills must move, not only colours.** Then run `omarchy theme set` with a
       theme whose `shell.toml` is generated from Omarchy's template — any of the other 26 —
       because the two exercise different branches of the variable map.
       Confirm the header's theme name updates too, rather than showing the previous theme.

    4. **UIX-02 and UIX-03, the pointer.** Hover rows with a **real mouse**. A terminal cannot
       change the pointer's shape — that is the ceiling of what Linux gives here, and it is
       named rather than pretended away — so the hover fill has to carry the whole affordance.
       Ask: is it obvious which row is live? Hover a row, then press `j`: the highlight must
       continue from where the pointer left it, not jump to the top. Click a row anywhere
       along its width, including the far right.

    5. **UIX-04, the day.** Hover along the ramp and read the read-out row. Does the hour
       track the pointer? Does the permission channel mark the hours a loosening is accepted?
       Then put the cursor on the ramp and use `h`/`l` — the same string must appear.

    6. **UIX-05, no reflow.** Move the pointer quickly down the rows. Nothing may shift.

    7. **The phase goal.** Open it **beside a first-party Omarchy panel**. Does it read as a
       native application, or still as a terminal form? This is aesthetic judgement and it is
       yours — it is the reason the phase exists.

    8. **The trust path, unchanged.** Confirm the `edit` chip still opens the editor, the
       `sudo` prompt still appears inline, and a change outside the clock window is still
       refused with the signer's own reason. Nothing in this phase touched that path; this
       is the confirmation, not the test.
  </how-to-verify>

  <acceptance_criteria>
    - `uv tool install --python 3.14 . --force` completed before the walkthrough, and the
      launched app is the newly installed shim.
    - Both theme switches were performed with the app **open**, and in both cases borders and
      fills moved — not colours alone. Note which theme was used for the generated-`shell.toml`
      case.
    - The hover-then-`j` continuation was observed with a real mouse.
    - The ramp read-out tracked both the pointer and `h`/`l`, with the same string.
    - No row shifted under fast pointer movement.
    - A verdict on the phase goal, in the user's own words, alongside a first-party panel.
    - If anything in step 8 behaves differently from before this phase: **stop.** That is the
      trust path moving, which this phase was instructed not to do.
  </acceptance_criteria>

  <verify>
    <automated>cd ngtui && uv tool install --python 3.14 . --force && command -v ngtui</automated>
  </verify>

  <done>The user has switched the real desktop theme twice with the app open and seen borders and fills move, has driven the surface with a real mouse, and has given a verdict on whether it reads as a native Omarchy application.</done>

  <resume-signal>Type "approved", or describe what looks wrong — including "it still reads like a terminal form", which is a valid and expected possible outcome.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the repo → the installed shim | what ships is `~/.local/bin/ngtui` and its isolated venv, not `ngtui/.venv` and not the working tree |
| the phase's claims → what the user actually sees | the gap this checkpoint exists to close |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.1-33 | Repudiation | verifying the old UI and calling it green | mitigate | `uv tool install --python 3.14 . --force` is step 1 of the walkthrough and an explicit acceptance criterion. The installed shim resolves to a venv separate from `ngtui/.venv`, and `app.tcss` is packaged wheel data, so a repo edit is invisible to it until reinstall. |
| T-12.1-34 | Repudiation | a control that was never seen to fail | mitigate | Task 1 audits all twenty rows for a recorded verbatim failure message and closes any that lack one before the checkpoint. This is the project's stated definition of done and the reason the ledger exists. |
| T-12.1-35 | Elevation of Privilege | the trust path having moved unnoticed | mitigate | Audited two ways: a `git diff --stat` against the phase base for `backend.py`, `nightguard_ctl.py`, `panel.py`, `packaging/omarchy/`, `src/` and `src-tauri/` must be empty; and the trust-path suite must be green. Step 8 of the walkthrough is the human confirmation on the running system. |
| T-12.1-36 | Spoofing | the restyle working against a fixture but not against the real desktop | mitigate | The automated control proves the repaint path with a fixture. Only a real `omarchy theme set` proves the watcher, the file paths and the deployed entry point line up — which is why two themes are switched, one hand-written and one generated, since they exercise different branches of the variable map. |
| T-12.1-SC | Tampering | package installs | n/a | The phase installs no package. `uv tool install --force` reinstalls **this project** from its own pinned `pyproject.toml` (`textual==8.2.7`); no new package name enters the tree. |
</threat_model>

<verification>
`cd ngtui && uv run --with pytest --with pytest-asyncio pytest -q` — the full suite, green
except the one named baseline red.

**Stop condition, restated because this is the last chance to catch it.** If anything in the
live walkthrough shows the trust path behaving differently — `backend.commit()`, the inline
`sudo` under `App.suspend()` with a real PTY, the direction classifier, or the refusal to
accept a weakening keystroke outside the edit window — **stop and say so.** That is an
instruction, not a preference.
</verification>

<success_criteria>
- Full suite green except `tests/test_theme_sync.py::…[dos-moos]`, which is named and
  deliberate.
- Every control row in `12.1-VALIDATION.md` carries its test reference and its verbatim
  failure message; `nyquist_compliant: true`.
- The trust path is byte-identical and its suite is green.
- The running TUI restyles on a real desktop theme switch, borders and fills included, on
  both a hand-written and a generated `shell.toml` theme.
- The user has given a verdict on whether it reads as a native Omarchy application.
</success_criteria>

<output>
Create `.planning/phases/12.1-native-dashboard-pointer-first-omarchy-tui/12.1-09-SUMMARY.md` when done.
Record the final suite line with its runtime, the closed control ledger, and the user's verdict in their own words.
</output>
