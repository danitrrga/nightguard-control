---
phase: 09-stayfree-desktop
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - .planning/phases/09-stayfree-desktop/09-CONTEXT.md
  - .planning/phases/09-stayfree-desktop/09-NOTES-spike-verdict.md
autonomous: true
requirements: [TRAK-01, TRAK-02]
gated_on: "09-01 verdict == FAIL or PARTIAL"

must_haves:
  truths:
    - "StayFree is recorded as analytics-only (viewed in its own UI); no keep-alive supervisor is built"
    - "The accepted residual is stated explicitly: no native-app hard guarantee (D-05 dropped); the hard floor is the curfew layer"
    - "09-CONTEXT.md is re-opened with the FAIL outcome and the analytics-only resolution, per D-09"
  artifacts:
    - path: ".planning/phases/09-stayfree-desktop/09-NOTES-spike-verdict.md"
      provides: "The fallback resolution appended below the verdict (analytics-only, keep-alive moot, CONTEXT re-opened)"
      contains: "analytics-only"
    - path: ".planning/phases/09-stayfree-desktop/09-CONTEXT.md"
      provides: "A re-opened/resolved banner recording the FAIL branch per D-09"
      contains: "FAIL"
  key_links:
    - from: "09-01 verdict (FAIL/PARTIAL)"
      to: "09-CONTEXT.md resolution"
      via: "the D-09 fallback clause"
      pattern: "analytics-only"
---

<objective>
**GATED: execute this plan ONLY if `09-01-SUMMARY.md` records a spike verdict of FAIL or
PARTIAL.** On PASS, skip this plan and execute 09-02 (keep-alive) instead.

This is the FALLBACK branch RESEARCH predicts with HIGH confidence. StayFree cannot block
(or cannot track) native Wayland apps on Hyprland, so: StayFree = **analytics-only** (viewed
in its own UI per TRAK scope); the keep-alive supervisor is **moot — NOT built** (a tracker
does not need root-enforced uptime); and `09-CONTEXT.md` is re-opened per D-09. No
`nightguard_watchdog.py` change is made.

Purpose: Close Phase 9 cleanly on the predicted FAIL without building enforcement that
would give a false sense of blocking. The hard floor remains the curfew layer (D-05 dropped:
there is NO Phase 8 native-kill backstop — that code was descoped + reverted, `8055b86`).
Output: Recorded fallback resolution + re-opened CONTEXT; TRAK-01/02 satisfied as
analytics-only.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/09-stayfree-desktop/09-RESEARCH.md
@.planning/phases/09-stayfree-desktop/09-CONTEXT.md
@.planning/phases/09-stayfree-desktop/09-NOTES-spike-verdict.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the analytics-only fallback resolution + re-open CONTEXT</name>
  <read_first>
    - .planning/phases/09-stayfree-desktop/09-01-SUMMARY.md (CONFIRM the recorded verdict is FAIL or PARTIAL before doing any work)
    - .planning/phases/09-stayfree-desktop/09-NOTES-spike-verdict.md (the verdict + observations to extend)
    - .planning/phases/09-stayfree-desktop/09-RESEARCH.md (§"The D-09 Gating Spike" -> "FAIL -> fallback", §"TRAK-01 / TRAK-02 — Analytics source")
    - .planning/phases/09-stayfree-desktop/09-CONTEXT.md (D-09 fallback clause + the SUPERSEDED banner: D-05 dropped, hard floor = curfew layer)
  </read_first>
  <action>
    FIRST: confirm `09-01-SUMMARY.md` records verdict FAIL or PARTIAL. If it records PASS, STOP —
    execute 09-02 (keep-alive) instead; this plan does not apply.

    Append a "## Fallback resolution (D-09 FAIL branch)" section to
    `09-NOTES-spike-verdict.md` recording:
    - StayFree's role is now **analytics-only**, viewed in its own UI (per TRAK-02 acceptable-v1;
      cite the actual local-store shape discovered in the spike Step 6 — likely an Electron
      leveldb with no clean export; do NOT write a scraper).
    - For PARTIAL: note which targets blocked (XWayland) vs did not (native Wayland), and that
      PARTIAL is treated as FAIL for the guarantee (the user's escape apps may be native Wayland).
    - The keep-alive supervisor (D-03/D-04) is **moot and NOT built** — a tracker does not need
      root-enforced uptime. (Optionally note that a *lightweight* keep-alive purely to keep
      analytics flowing is a downgrade decision, not a guarantee, and is out of scope here.)
    - The hard floor remains the **curfew layer** (Claude-Code hook block + root config-revert +
      root-locked StayFree *browser* policy). State plainly: with D-05 dropped there is **NO
      native-app hard guarantee** in this phase — accepted, eyes-open, consistent with the
      2026-06-23 descope decision.

    Then re-open `09-CONTEXT.md` per D-09: add a dated banner near the top (or under the existing
    SUPERSEDED banner) recording that the spike returned FAIL/PARTIAL, StayFree is analytics-only,
    the keep-alive is not built, and any future native-app hard-blocking is a NEW phase (not this
    one). Do NOT delete the existing decisions; this is an append/annotate, preserving history.
  </action>
  <verify>
    <automated>grep -q 'analytics-only' .planning/phases/09-stayfree-desktop/09-NOTES-spike-verdict.md && grep -qi 'FAIL' .planning/phases/09-stayfree-desktop/09-CONTEXT.md && grep -qi 'no native-app hard guarantee' .planning/phases/09-stayfree-desktop/09-NOTES-spike-verdict.md</automated>
  </verify>
  <acceptance_criteria>
    - `09-NOTES-spike-verdict.md` has a "Fallback resolution (D-09 FAIL branch)" section stating analytics-only + keep-alive-not-built + the explicit "no native-app hard guarantee" residual.
    - `09-CONTEXT.md` carries a dated re-opened/resolved banner recording the FAIL/PARTIAL outcome and the analytics-only resolution; existing decisions are preserved (append/annotate, not delete).
    - No change is made to `nightguard_watchdog.py` (the keep-alive is NOT built on this branch).
  </acceptance_criteria>
  <done>The FAIL branch is recorded as a first-class resolution: StayFree analytics-only, keep-alive moot, CONTEXT re-opened, residual risk stated; no enforcement code added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| StayFree (user session) → nightguard trust kernel | StayFree as a mere tracker still gets NO `.guardkey`/signing/config role. |
| Late-night user → native desktop apps | On this branch there is NO native-app hard guarantee; only the curfew layer (hook + config-revert + browser policy) holds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09F-01 | Repudiation / false-assurance | Recording a "blocker" role StayFree cannot actually deliver on Wayland | mitigate | The fallback explicitly records StayFree as analytics-only and refuses to build a keep-alive that would imply enforcement; PARTIAL is treated as FAIL for the guarantee. |
| T-09F-02 | DoS (escape) | No native-app hard guarantee with D-05 dropped | accept | Eyes-open, recorded residual: the hard floor is the curfew layer (hook + config-revert + browser policy), which does not cover native desktop apps. Any future native hard-block is a NEW phase. |
| T-09F-03 | Information disclosure | StayFree leveldb / cloud analytics | accept | Local store is an Electron leveldb of usage logs; no clean export; "view in StayFree UI" is the accepted v1. No scraper built. Irrelevant to the trust kernel. |
</threat_model>

<verification>
- `09-NOTES-spike-verdict.md` records the analytics-only fallback + explicit no-native-hard-guarantee residual.
- `09-CONTEXT.md` is re-opened with the FAIL/PARTIAL outcome; history preserved.
- `nightguard_watchdog.py` is unchanged on this branch.
</verification>

<success_criteria>
- Phase 9 closes cleanly on the predicted FAIL without building false enforcement.
- TRAK-01/02 satisfied as analytics-only (StayFree installed as the analytics source; analytics viewed in StayFree's UI).
- The accepted residual (no native-app hard guarantee, D-05 dropped) is documented, not assumed away.
</success_criteria>

<output>
Create `.planning/phases/09-stayfree-desktop/09-03-SUMMARY.md` when done.
</output>
