---
phase: 02-mutation-engine
plan: 05
type: execute
wave: 4
depends_on: ["02-01", "02-04"]
files_modified:
  - crates/mutation-engine/Cargo.toml
  - crates/mutation-engine/src/bin/state_interop_cli.rs
  - scripts/interop/state_interop.ps1
  - scripts/interop/run_state_interop_gate.ps1
autonomous: true
requirements: [RULE-06]
must_haves:
  truths:
    - "The state_hmac computed by Rust over a fixed GuardState equals the state_hmac computed by PowerShell over the identical serialized state (byte-parity, both directions)"
    - "The parity harness runs clean under Windows PowerShell 5.1 (ASCII-only, foreach byte construction, $LASTEXITCODE gating)"
  artifacts:
    - path: "crates/mutation-engine/src/bin/state_interop_cli.rs"
      provides: "Rust side of the state_hmac cross-language gate"
      contains: "state-hmac"
    - path: "scripts/interop/run_state_interop_gate.ps1"
      provides: "PS 5.1 driver that asserts state_hmac parity and exits 0/1"
      contains: "LASTEXITCODE"
  key_links:
    - from: "scripts/interop/run_state_interop_gate.ps1"
      to: "crates/mutation-engine/src/bin/state_interop_cli.rs"
      via: "drives the Rust bin and compares hex tags"
      pattern: "state_interop_cli"
---

<objective>
Close Assumption A3 IN THIS PHASE (not deferred to Phase 3): prove the `state_hmac` recipe is byte-identical across Rust and PowerShell, exactly as Phase 1 proved the config HMAC. A Rust bin emits/verifies a `state_hmac` for a fixed `GuardState`; a Windows-PowerShell-5.1-clean harness re-derives the same tag and asserts byte-equality both directions, exiting 0 only on full parity.

Purpose: guard.json is the Phase 3 interop contract; if the PS guard cannot reproduce the exact state_hmac byte form, it will treat legitimate state as tampered (fail-closed) on every run. This gate de-risks that before Phase 3 builds the guard.
Output: a `[[bin]]` stanza + state_interop_cli.rs + two PS scripts (lib + gate), mirroring Phase 1's interop harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/HANDOFF.md
@.planning/phases/02-mutation-engine/02-RESEARCH.md
@.planning/phases/02-mutation-engine/02-PATTERNS.md
@.planning/phases/02-mutation-engine/02-01-SUMMARY.md
@.planning/phases/02-mutation-engine/02-04-SUMMARY.md
@scripts/interop/run_interop_gate.ps1
@scripts/interop/nightguard_interop.ps1

<interfaces>
<!-- The state_hmac recipe locked in plan 02-01 (A3) — the PS side must reproduce EXACTLY: -->
  serialize GuardState with config_hmac set + state_hmac = "" ->
  trust_kernel::canonicalize_bytes(json_bytes) ->
  sign_bytes(key, &canon) -> tag_to_hex   => state_hmac

<!-- Phase 1 harness analogs (reuse the shape + helpers): -->
  scripts/interop/nightguard_interop.ps1 : Protect-GuardKey, Unprotect-GuardKey, Get-FileBytes,
    Write-RawBytes, ConvertTo-LowerHex, Get-FileHmacHex  (all 5.1-clean .NET [ProtectedData]/[HMACSHA256])
  scripts/interop/run_interop_gate.ps1   : Invoke-Cli pattern, $LASTEXITCODE gating, Add-Check tally, exit 0/1

<!-- Phase 1 Rust bin analog: crates/trust-kernel/src/bin/interop_cli.rs (argv-matched subcommands, exit 0/1/2) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: state_interop_cli.rs — Rust side of the state_hmac gate</name>
  <files>crates/mutation-engine/Cargo.toml, crates/mutation-engine/src/bin/state_interop_cli.rs</files>
  <read_first>
    - crates/trust-kernel/src/bin/interop_cli.rs lines 1-62 (argv-matched subcommands, ExitCode 0/1/2, the //! doc listing invariants — the exact bin shape to mirror)
    - crates/trust-kernel/Cargo.toml lines 6-10 (the [[bin]] stanza shape)
    - 02-RESEARCH.md lines 380-398 (guard.json model + the A3 state_hmac recipe note)
    - 02-PATTERNS.md lines 59-64 (the [[bin]] stanza) + lines 276-280 (state_parity gate assignment; do NOT defer to Phase 3)
    - crates/mutation-engine/src/state.rs (the locked state_hmac recipe from plan 02-01 — REUSE it, do not re-derive a second recipe)
  </read_first>
  <action>
    Add a `[[bin]] name = "state_interop_cli", path = "src/bin/state_interop_cli.rs"` stanza to `crates/mutation-engine/Cargo.toml` (mirror trust-kernel Cargo.toml lines 8-10). Create `src/bin/state_interop_cli.rs` as a tiny argv-matched CLI (no clap — match on args, exit 0/1/2) mirroring interop_cli.rs's structure and //! doc style. Subcommands: `emit-state-hmac <key_path> <out_json_path>` — load the 32-byte key via `trust_kernel::key::load_or_create_key`, construct a FIXED canonical `GuardState` (deterministic literal values: a known config_hmac hex, weekly_spent, week_anchor, an empty/representative ledger, grace=None), compute state_hmac via the plan-01 recipe (the SAME `state.rs` code path — do not inline a second recipe), write the serialized state WITH state_hmac filled to `out_json_path`, and print the state_hmac hex to stdout; `verify-state-hmac <key_path> <json_path> <expected_hex>` — read the JSON, recompute state_hmac via the recipe, exit 0 if it matches `expected_hex`, exit 2 on mismatch. So the PS side can re-derive identically, the bin must ALSO print (or the recipe must guarantee) the exact canonical byte form signed — emit the pre-sign canonical JSON bytes to a sibling file (e.g. `<out_json_path>.signbytes`) so the PS harness signs the identical bytes. Open with a //! doc citing A3 + the locked recipe.
  </action>
  <verify>
    <automated>cargo build -p mutation-engine --bin state_interop_cli</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p mutation-engine --bin state_interop_cli` exits 0.
    - `emit-state-hmac` prints a 64-hex-char state_hmac and writes both the filled-in guard.json and the exact pre-sign canonical bytes (`.signbytes`) the PS side will re-sign.
    - `verify-state-hmac` exits 0 on a matching tag, exit 2 on mismatch (distinct code, mirroring interop_cli).
    - The bin reuses state.rs's recipe — there is no second/duplicate state_hmac computation.
  </acceptance_criteria>
  <done>The Rust side of the state_hmac gate compiles and round-trips its own emit/verify.</done>
</task>

<task type="auto">
  <name>Task 2: PS 5.1 state_hmac parity gate</name>
  <files>scripts/interop/state_interop.ps1, scripts/interop/run_state_interop_gate.ps1</files>
  <read_first>
    - scripts/interop/run_interop_gate.ps1 (the full gate driver: build-the-bin, Invoke-Cli with relaxed $ErrorActionPreference around native calls, Add-Check tally, exit 0/1; the 5.1 byte-list foreach idiom in Check 5)
    - scripts/interop/nightguard_interop.ps1 (Protect/Unprotect-GuardKey, Get-FileBytes, Write-RawBytes, ConvertTo-LowerHex, Get-FileHmacHex — reuse these helpers)
    - .planning/HANDOFF.md lines 40-45 (PS 5.1 env gotcha: ASCII only, foreach byte construction, $LASTEXITCODE gating)
    - 02-RESEARCH.md line 455 + lines 422-425 (keep the parity harness 5.1-clean; A3 recipe)
    - 02-PATTERNS.md lines 276-280 (state_parity gate analog)
  </read_first>
  <action>
    Create `scripts/interop/state_interop.ps1` (a small dot-sourced lib, or reuse nightguard_interop.ps1's helpers) providing a `Get-StateHmacHex` that signs the EXACT canonical `.signbytes` file the Rust bin emitted, using the DPAPI-unprotected 32-byte key and `[HMACSHA256]` (the same .NET type Phase 1 uses). Create `scripts/interop/run_state_interop_gate.ps1` mirroring run_interop_gate.ps1: build `state_interop_cli` (cargo build --bin state_interop_cli, relax $ErrorActionPreference around cargo stderr, gate on $LASTEXITCODE), set up a work dir under the repo's target/ (same volume), then run the parity checks via an Invoke-Cli helper and an Add-Check tally: (1) Rust->PS — Rust `emit-state-hmac` produces a tag + `.signbytes`; PS `Get-StateHmacHex` over the same key + `.signbytes` -> assert byte-identical hex. (2) PS->Rust — PS computes the tag; Rust `verify-state-hmac <key> <json> <ps_tag>` -> assert exit 0. (3) Tamper — flip one byte of the guard.json -> Rust `verify-state-hmac` exits 2 AND PS re-derives a DIFFERENT tag. Exit 0 only if all checks PASS, exit 1 with a per-check failure list otherwise. STRICT 5.1 compatibility: ASCII only (no em-dash literals), build any byte lists with `foreach` (no List[byte].AddRange casts), relax $ErrorActionPreference to 'Continue' around native cargo/CLI calls and gate purely on $LASTEXITCODE.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/interop/run_state_interop_gate.ps1; echo "exit=$LASTEXITCODE"</automated>
  </verify>
  <acceptance_criteria>
    - `run_state_interop_gate.ps1` exits 0 under Windows PowerShell 5.1 with all checks PASS.
    - Check "state_hmac Rust->PS" asserts byte-identical hex tags over the identical canonical signbytes.
    - Check "state_hmac PS->Rust" asserts Rust `verify-state-hmac` exits 0 on the PS-computed tag.
    - Tamper check: a single-byte flip makes Rust `verify-state-hmac` exit 2 AND the PS tag differ.
    - The scripts are ASCII-only and gate on $LASTEXITCODE (no terminating error on cargo stderr) — verified by a clean 5.1 run.
  </acceptance_criteria>
  <done>The Rust<->PowerShell state_hmac parity gate is GREEN, closing Assumption A3 within this phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust app ↔ PowerShell guard | guard.json state_hmac must be byte-reproducible across both languages; any divergence makes the Phase 3 guard fail legitimate state closed |
| disk → guard | a tampered guard.json must be detectable by recomputing state_hmac on the PS side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Tampering | state_hmac byte-divergence Rust vs PS (false-tamper on legitimate state) | mitigate | This gate proves byte-parity both directions over the identical canonical signbytes BEFORE Phase 3 builds the guard (closes A3) |
| T-02-16 | Spoofing | forged guard.json with a hand-computed state_hmac | mitigate | Tamper check proves a single-byte flip breaks verification on both sides; the key is DPAPI-gated (a hand-editor cannot forge a valid tag without scripting DPAPI as this user) |
| T-02-17 | Tampering | PS 5.1 vs pwsh 7 host divergence breaking the harness | mitigate | [HMACSHA256]/[ProtectedData] are identical .NET types across hosts (proven in Phase 1); the harness is ASCII-only, foreach byte-list, $LASTEXITCODE-gated per the env gotcha |
</threat_model>

<verification>
- `cargo build -p mutation-engine --bin state_interop_cli` exits 0.
- `powershell -NoProfile -File scripts/interop/run_state_interop_gate.ps1` exits 0 with all parity + tamper checks PASS.
- The PS scripts are 5.1-clean (ASCII, foreach byte construction, $LASTEXITCODE gating).
</verification>

<success_criteria>
- state_hmac is byte-identical across Rust and PowerShell, both directions (A3 closed in this phase).
- A tampered guard.json fails verification on both sides.
- The harness runs green under Windows PowerShell 5.1.
</success_criteria>

<output>
Create `.planning/phases/02-mutation-engine/02-05-SUMMARY.md` when done.
</output>
