---
phase: 05-instance-wiring
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - scripts/deploy/run_cutover_gate.ps1
  - C:/Users/20252128/.claude/settings.json
files_touched_live:
  - C:/Users/20252128/dev/Projects/LifeOS/hooks/ (deployed scripts via junction)
  - NightguardWatchdog (Scheduled Task re-register)
autonomous: false
requirements: [WIRE-01, WIRE-02]
user_setup: []

must_haves:
  truths:
    - "A scripted D-11 dry-run proves: key round-trips, guard.json verifies (Rust + PS), the curfew gate returns deny/curfew at a simulated in-curfew instant, a hand-edit to config.yaml auto-reverts (reverted=true, HMAC restored), and the adapter emits the correct block per reason"
    - "D-01: the legacy curfew gate (curfew_guard.ps1 / nightguard_curfew_guard.ps1) is retired from settings.json ONLY AFTER the gate is all-green — full replace, no side-by-side, the product guard + adapter become the sole curfew gate AND auto-revert"
    - "D-06: settings.json UserPromptSubmit runs nightguard_adapter.ps1; the NightguardWatchdog Scheduled Task (unchanged delivery mechanism) is re-registered to point at nightguard_watchdog.ps1 and restarted so a fresh process inherits NIGHTGUARD_DIR"
    - "D-05: StayFree is running after cutover (repointing the watchdog past the stale-config early-return restores it — the visible WIRE-01 acceptance signal)"
    - "There was never a moment with no curfew protection: deploy+gate precede retirement; retirement is the last action"
  artifacts:
    - path: "scripts/deploy/run_cutover_gate.ps1"
      provides: "the D-11 prove-then-switch verify gate (run BEFORE retirement)"
      contains: "reverted"
  key_links:
    - from: "run_cutover_gate.ps1"
      to: "nightguard_guard.ps1 + state_interop_cli verify-state-hmac"
      via: "exercise verify/auto-revert/curfew-verdict against the real LifeOS/nightguard instance"
      pattern: "verify-state-hmac"
    - from: "settings.json UserPromptSubmit"
      to: "nightguard_adapter.ps1"
      via: "replace curfew_guard.ps1 with the adapter (only post-green)"
      pattern: "nightguard_adapter"
    - from: "NightguardWatchdog task"
      to: "nightguard_watchdog.ps1"
      via: "Register-ScheduledTask repoint + Start-ScheduledTask (fresh env inherit)"
      pattern: "NightguardWatchdog"
---

<objective>
Execute the live in-place cutover under the D-11 prove-then-switch safety constraint: deploy the Plan-02 scripts, run a scripted verify gate proving the new instance signs/locks/unlocks/auto-reverts correctly, and ONLY on all-green retire the legacy curfew gate (D-01 full replace), swap settings.json to the adapter, re-register + restart the watchdog on its existing Scheduled Task (D-06, restoring StayFree per D-05). Retirement of the old gate is the LAST action — there must never be a night unprotected or a wrongful lockout.

Purpose: WIRE-01 (every live consumer now reads the canonical config; drift gone) + WIRE-02 (the instance is the app's verifying target). This is the only plan that touches live OS-registered state, and it does so behind a passing gate.
Output: run_cutover_gate.ps1, a deployed + cut-over live instance, StayFree restored.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-instance-wiring/05-RESEARCH.md
@.planning/phases/05-instance-wiring/05-PATTERNS.md
@.planning/phases/05-instance-wiring/05-01-SUMMARY.md
@.planning/phases/05-instance-wiring/05-02-SUMMARY.md
@scripts/guard/run_guard_gate.ps1

<interfaces>
Guard test seam (nightguard_guard.ps1): env NIGHTGUARD_TEST_NTP_OVERRIDE=1 + -NtpOverrideUnixSecs feeds a simulated true-time instant (Phase 3 run_guard_gate.ps1 uses this); verdict JSON {decision,reason,reverted,...} + exit 0/1.
state_interop_cli verify-state-hmac <key_path> <json_path> <expected_hex>: exit 0 on match, 2 on mismatch.
Auto-revert assertion: after a hand-edit to config.yaml, firing the guard sets reverted=true and HMAC(config.yaml)==config_hmac again (config.sanctioned.yaml byte-copied back).
Curfew window: 20:45 -> 05:30 Europe/Amsterdam; an in-curfew instant -> deny/curfew, an out-of-curfew instant -> allow (modulo grace).
Pitfall 2: the already-running watchdog process will NOT see NIGHTGUARD_DIR — must Stop/re-register/Start so a fresh powershell.exe inherits the user-scope var.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy scripts and write/run the D-11 prove-then-switch verify gate</name>
  <files>scripts/deploy/run_cutover_gate.ps1</files>
  <read_first>
    - scripts/guard/run_guard_gate.ps1 (the Phase 3 end-to-end gate to mirror: NTP override seam, Rust verify cross-check, auto-revert assertion shape)
    - scripts/deploy/install_nightguard.ps1 (from Plan 02 — the deploy step this task invokes)
    - .planning/phases/05-instance-wiring/05-RESEARCH.md "Pattern 4: Prove-then-switch verify gate (D-11)" (the six-step gate) + "StayFree Process Name"
    - .planning/phases/05-instance-wiring/05-PATTERNS.md deploy + integrity sections
    - scripts/guard/nightguard_guard.ps1 (verdict + NTP override + auto-revert behavior to exercise)
  </read_first>
  <action>First invoke scripts/deploy/install_nightguard.ps1 to byte-deploy guard+verifier+interop+adapter+watchdog into LifeOS/hooks (interop sibling preserved) and re-stamp the integrity baseline. Then create scripts/deploy/run_cutover_gate.ps1 (5.1-safe ASCII, gate on $LASTEXITCODE) mirroring run_guard_gate.ps1, running against the REAL LifeOS/nightguard instance + real .guardkey, asserting D-11 steps 1-5 in order: (1) key round-trips — Rust load_or_create_key and PS Unprotect-GuardKey yield identical 32 bytes; (2) guard.json verifies — PS Get-RuntimeStateHmac re-derive == on-disk state_hmac AND state_interop_cli verify-state-hmac exits 0; (3) curfew verdict correct — fire nightguard_guard.ps1 with NIGHTGUARD_TEST_NTP_OVERRIDE=1 + -NtpOverrideUnixSecs at an in-curfew instant (e.g. 21:30 Europe/Amsterdam) asserting decision=deny reason=curfew exit 1, and at an out-of-curfew instant (e.g. 12:00) asserting it does not deny on curfew; (4) auto-revert works — hand-edit config.yaml (append a byte), fire the guard, assert reverted=true and HMAC(config.yaml) matches config_hmac again (sanctioned copied back); (5) adapter translates — pipe each guard reason (curfew/clock tamper/ntp unreachable) through nightguard_adapter.ps1 and assert the correct butler message + the confirmed block contract, plus the allow-command pass lets /shutdown through. Print a single PASS/FAIL summary and exit non-zero on any failure. This gate must be GREEN before Task 2 runs. Restore config.yaml to its signed canonical bytes at the end of the revert test so the instance is left armed and verifying.</action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/deploy/run_cutover_gate.ps1; if ($LASTEXITCODE -eq 0) { 'GATE GREEN' } else { 'GATE RED' }</automated>
  </verify>
  <acceptance_criteria>
    - install_nightguard.ps1 ran: LifeOS/hooks contains nightguard_guard.ps1, nightguard_adapter.ps1, nightguard_watchdog.ps1, verify_hook_integrity.ps1, guard.baseline.sha256, and interop/{nightguard_interop.ps1,state_interop.ps1}.
    - run_cutover_gate.ps1 exits 0 with an explicit PASS line per D-11 step.
    - The gate output shows: key 32-byte match, verify-state-hmac exit 0, in-curfew -> deny/curfew, auto-revert reverted=true with HMAC restored, adapter block per reason + /shutdown allowed.
    - After the gate, config.yaml HMAC still equals guard.json config_hmac (instance left armed).
    - The legacy curfew_guard.ps1 is STILL the active settings.json gate at this point (retirement has not happened — grep settings.json shows curfew_guard still present).
  </acceptance_criteria>
  <done>Scripts are byte-deployed with a re-stamped baseline, and the D-11 gate proves sign/lock/unlock/auto-revert/adapter-translation all-green against the real instance — with the old gate still protecting.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Retire the legacy gate, swap settings.json to the adapter, restart the watchdog (ONLY post-green)</name>
  <read_first>
    - C:/Users/20252128/.claude/settings.json (the UserPromptSubmit entry to swap from curfew_guard.ps1 to nightguard_adapter.ps1)
    - scripts/deploy/install_nightguard.ps1 (the staged Register-ScheduledTask block for NightguardWatchdog)
    - .planning/phases/05-instance-wiring/05-RESEARCH.md "Runtime State Inventory" (the three runtime holders of the old string) + Pitfall 2
    - .planning/phases/05-instance-wiring/05-PATTERNS.md settings.json + Scheduled-Task section
  </read_first>
  <what-built>GATED ON TASK 1 GREEN. Claude has, in order: (1) edited C:/Users/20252128/.claude/settings.json UserPromptSubmit to replace curfew_guard.ps1 with nightguard_adapter.ps1 (and deleted/ignored the orphan nightguard_curfew_guard.ps1, non-load-bearing) — the D-01 full-replace retirement; (2) re-registered the NightguardWatchdog Scheduled Task to point at nightguard_watchdog.ps1 (D-06 — same task, new script-path argument; editing the file alone is insufficient), then Stop + Start the task so a FRESH powershell.exe inherits the user-scope NIGHTGUARD_DIR (Pitfall 2 — the already-running process spawned before the var existed); (3) waited one watchdog cycle and confirmed StayFree relaunched. This is the LAST step (D-11): retirement happens only because Task 1's gate was green, so there was never an unprotected window.</what-built>
  <how-to-verify>
    1. In a NEW Claude Code session, submit a prompt during the curfew window (or with the NTP override simulating in-curfew) and confirm it is BLOCKED with the Spanish-butler curfew message via the adapter (not the old gate).
    2. Submit `/shutdown` (or the configured allow-command) during curfew and confirm it passes (allow-command pass works).
    3. Confirm settings.json UserPromptSubmit now names nightguard_adapter.ps1 and no longer curfew_guard.ps1.
    4. Run `Get-ScheduledTask NightguardWatchdog` — confirm its action Arguments point at nightguard_watchdog.ps1 and State is Running.
    5. Run `Get-Process -Name StayFree` — confirm StayFree is RUNNING (the WIRE-01 acceptance signal, D-05).
    6. Hand-edit LifeOS/nightguard/config.yaml (change a curfew value), trigger a SessionStart/UserPromptSubmit, and confirm it auto-reverts (the binding is live end-to-end).
  </how-to-verify>
  <resume-signal>Type "approved: cutover live, StayFree running, auto-revert confirmed" or describe what failed (and whether to roll back to curfew_guard.ps1).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-retirement vs post-retirement | The cutover instant — old gate removed, new gate must already be proven and active |
| settings.json UserPromptSubmit | The live enforcement entry point; a wrong edit fail-opens every prompt |
| Scheduled Task action arguments | OS-registered script path; the running process holds the old path until re-register + restart |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Elevation | retiring old gate before new one proven (window unprotected) | mitigate | D-11: Task 1's gate must be GREEN before Task 2 touches settings.json; retirement is the final action, gated behind the verify |
| T-05-13 | Spoofing | adapter does not actually block (wrong CC schema) | mitigate | Gate step 5 + the Task-2 live in-session block test confirm a real prompt is suppressed before trusting the cutover; rollback path to curfew_guard.ps1 documented |
| T-05-14 | Denial of Service | stale running watchdog never sees NIGHTGUARD_DIR -> StayFree stays dead | mitigate | Re-register + Stop/Start the task so a fresh process inherits the user-scope var (Pitfall 2); verified by Get-Process StayFree running |
| T-05-15 | Tampering | hand-edit not reverted post-cutover | mitigate | Task-2 verify step 6 hand-edits config.yaml and confirms auto-revert end-to-end against the live deployed guard |
| T-05-16 | Repudiation | orphan nightguard_curfew_guard.ps1 left as a confusion/attack trap | accept | Deleted/ignored; it is referenced nowhere (research-confirmed orphan), so leaving it is harmless though tidied |
</threat_model>

<verification>
- run_cutover_gate.ps1 exits 0 (all D-11 steps PASS) with the old gate still active.
- settings.json UserPromptSubmit -> nightguard_adapter.ps1 (curfew_guard.ps1 retired) ONLY post-green.
- NightguardWatchdog re-registered to nightguard_watchdog.ps1 and restarted.
- Get-Process -Name StayFree shows StayFree running.
- A live hand-edit to config.yaml auto-reverts.
</verification>

<success_criteria>
The live nightguard runs on the published product: the adapter is the sole curfew gate (legacy gate retired, drift gone), every consumer reads the canonical LifeOS/nightguard config via NIGHTGUARD_DIR, the watchdog has StayFree running again, hand-edits auto-revert — and the cutover happened only after a green prove-then-switch gate, with no unprotected window (D-11).
</success_criteria>

<output>
Create `.planning/phases/05-instance-wiring/05-03-SUMMARY.md` when done
</output>
</content>
