---
phase: 05-instance-wiring
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - C:/Users/20252128/dev/Projects/LifeOS/hooks/nightguard_adapter.ps1
  - C:/Users/20252128/dev/Projects/LifeOS/hooks/nightguard_watchdog.ps1
  - scripts/deploy/install_nightguard.ps1
autonomous: false
requirements: [WIRE-01]
user_setup: []

must_haves:
  truths:
    - "D-02: the adapter resolves NIGHTGUARD_DIR (fail-closed throw on empty), invokes the guard, and translates exit 0/1 + reason into the empirically-confirmed Claude-Code UserPromptSubmit block schema (exit-1 -> {continue:false} carrying the butler message)"
    - "D-02: the adapter reads butler messages (curfew/tamper/offline) from config.yaml, not hardcoded, and implements the /shutdown allow-command pass before curfew logic"
    - "Any guard throw / non-0-non-1 exit / no-JSON makes the adapter emit a fail-closed block"
    - "D-04/D-05: the rewritten watchdog resolves NIGHTGUARD_DIR for the single canonical config + log path (never the stale ~/.claude path), and relaunches StayFree via shell:AppsFolder when absent and watchdog.enabled is true — repointing past the stale-config early-return restores StayFree (the WIRE-01 proof-case)"
    - "D-06: the watchdog remains delivered by the NightguardWatchdog Scheduled Task (only the script + config source change); the install script byte-copies guard+verifier+interop+adapter+watchdog into LifeOS/hooks preserving the hooks/interop/ sibling, re-stamps the integrity baseline, and stages the NightguardWatchdog re-register against nightguard_watchdog.ps1"
  artifacts:
    - path: "C:/Users/20252128/dev/Projects/LifeOS/hooks/nightguard_adapter.ps1"
      provides: "Claude-Code UserPromptSubmit seam translating guard verdict to block/allow"
      contains: "NIGHTGUARD_DIR"
    - path: "C:/Users/20252128/dev/Projects/LifeOS/hooks/nightguard_watchdog.ps1"
      provides: "rewritten StayFree liveness loop reading canonical config"
      contains: "NIGHTGUARD_DIR"
    - path: "scripts/deploy/install_nightguard.ps1"
      provides: "byte-exact deploy + baseline re-stamp + watchdog task re-register helper"
  key_links:
    - from: "nightguard_adapter.ps1"
      to: "nightguard_guard.ps1"
      via: "invoke guard, capture exit code + JSON verdict"
      pattern: "nightguard_guard"
    - from: "nightguard_adapter.ps1"
      to: "config.yaml curfew.message/tamper_message/offline_message"
      via: "read butler strings from canonical config"
      pattern: "offline_message"
    - from: "nightguard_watchdog.ps1"
      to: "StayFree UWP"
      via: "Get-Process StayFree + explorer shell:AppsFolder relaunch"
      pattern: "StayFree"
---

<objective>
Build the three deployable PowerShell artifacts the cutover needs: the Claude-Code adapter hook (D-02 verdict translation + allow-command pass + butler messages), the rewritten StayFree watchdog (D-04/D-05, reads canonical config) delivered by its existing Scheduled Task (D-06), and a byte-exact install/deploy script (D-09 deploy + integrity re-stamp). Empirically confirm the current Claude-Code UserPromptSubmit block schema (open blocker A2) before finalizing the adapter's output contract.

Purpose: WIRE-01 — these are the consumers that must read the single canonical config via NIGHTGUARD_DIR. Built and unit-exercised here; the live settings.json swap and watchdog re-register happen LAST in Plan 03 behind the D-11 verify gate.
Output: nightguard_adapter.ps1, nightguard_watchdog.ps1, scripts/deploy/install_nightguard.ps1, and a confirmed CC block-schema contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-instance-wiring/05-RESEARCH.md
@.planning/phases/05-instance-wiring/05-PATTERNS.md
@.planning/phases/05-instance-wiring/05-01-SUMMARY.md
@scripts/guard/nightguard_guard.ps1
@scripts/guard/verify_hook_integrity.ps1

<interfaces>
Guard verdict vocabulary (nightguard_guard.ps1 lines 572-583): JSON fields {decision: 'allow'|'deny', reason: 'curfew'|'grace active'|'ntp unreachable'|'clock tamper', grace_remaining_secs, reverted, fail_closed, audit_chain_broken, audit_chain_break_index}; exit 0 iff allow else exit 1.
Data-dir resolution stanza (nightguard_guard.ps1 lines 42-48): $DataDir = $env:NIGHTGUARD_DIR; throw on IsNullOrWhiteSpace — fail-closed, never $PSScriptRoot.
Guard dot-source layout (lines 38-40): $here\..\interop\nightguard_interop.ps1 + state_interop.ps1 — deploy must preserve the hooks/interop/ sibling.
verify_hook_integrity.ps1 -Update (lines 57-73): self-registers the four trust-surface scripts; $registered uses repo-relative paths + $repoRoot = $here\..\.. (line 31) — does NOT fit the deployed LifeOS/hooks layout (A5/Pitfall 5).
Legacy curfew_guard.ps1: allow-command pass (lines 72-78: read stdin prompt JSON, match against config['curfew']['allow_commands'], exit 0 on match), butler messages (lines 87-126, n-tilde via [char]0x00F1), block via Console::Error + exit 2.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm the live Claude-Code UserPromptSubmit block schema (resolves blocker A2)</name>
  <read_first>
    - .planning/phases/05-instance-wiring/05-RESEARCH.md "Open Questions" Q1 + Assumption A2
    - .planning/phases/05-instance-wiring/05-PATTERNS.md adapter "OPEN (A2 / blocking the executor)" note
    - C:/Users/20252128/.claude/settings.json (current hooks block — see how UserPromptSubmit hooks are declared and what curfew_guard.ps1 emits today)
    - C:/Users/20252128/.claude/hooks/curfew_guard.ps1 (the active gate: uses Console::Error.WriteLine + exit 2)
  </read_first>
  <what-built>Claude has empirically determined how the installed Claude-Code version blocks a UserPromptSubmit prompt. It either (a) located the installed CC hook documentation/version, or (b) ran a throwaway no-op UserPromptSubmit hook that attempts a block and observed whether the prompt was actually suppressed — testing the candidate mechanisms: a stdout JSON object `{"continue": false, "reason": "..."}`, a specific non-zero exit code (e.g. exit 2), and/or a `decision:block` JSON. Claude recorded the ONE confirmed mechanism that the live CC honors, WITHOUT yet editing the real settings.json UserPromptSubmit entry (that is Plan 03). This is blocker A2 — if guessed wrong, prompts pass through at curfew (fail-open), so it must be confirmed, not assumed.</what-built>
  <how-to-verify>
    1. Claude states the exact confirmed block contract (stdout JSON shape and/or required exit code) and how it was verified (doc reference or empirical no-op-hook test result).
    2. Confirm the test did NOT modify the live curfew_guard.ps1 settings.json entry (the active gate is untouched).
    3. Confirm the contract is fail-closed-compatible: a hook that errors/throws can still produce a block (so an unset NIGHTGUARD_DIR at cutover blocks rather than passes — Pitfall 1).
  </how-to-verify>
  <resume-signal>Type "approved: schema is <describe>" or correct the finding.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write nightguard_adapter.ps1 (verdict translation + allow-command pass + butler messages)</name>
  <files>C:/Users/20252128/dev/Projects/LifeOS/hooks/nightguard_adapter.ps1</files>
  <read_first>
    - C:/Users/20252128/.claude/hooks/curfew_guard.ps1 (legacy gate: allow-command pass lines 72-78, butler messages lines 87-126 incl. [char]0x00F1 n-tilde, block mechanism)
    - scripts/guard/nightguard_guard.ps1 lines 42-48 (data-dir stanza to copy verbatim) + lines 461-583 (verdict vocabulary + exit semantics)
    - .planning/phases/05-instance-wiring/05-PATTERNS.md adapter section (translation table + allow-command excerpt + data-dir stanza)
    - .planning/phases/05-instance-wiring/05-RESEARCH.md "Pattern 2" (translation table) + A4 (guard ignores allow_commands — adapter owns it)
    - Task 1 confirmed CC block schema (use the EXACT confirmed contract for the block output)
  </read_first>
  <action>Create nightguard_adapter.ps1 (Windows PowerShell 5.1-safe: ASCII source, no em-dash literals, gate on $LASTEXITCODE). Structure: (1) resolve $DataDir = $env:NIGHTGUARD_DIR with the verbatim fail-closed throw stanza from nightguard_guard.ps1 lines 42-48 (NEVER $PSScriptRoot for the data dir); $configPath = Join-Path $DataDir 'config.yaml'. (2) Read stdin once ([Console]::In.ReadToEnd()), ConvertFrom-Json to get .prompt; parse config['curfew']['allow_commands'] from config.yaml and if the prompt matches any (regex-escaped) allow_command (e.g. /shutdown), emit allow and exit 0 BEFORE invoking the guard (A4 — the published guard does not read allow_commands). (3) Invoke nightguard_guard.ps1 (resolve it as a sibling under $DataDir-independent hooks dir via $here\nightguard_guard.ps1), capture its stdout JSON and exit code. (4) Translate per the table: exit 0 -> allow (let prompt through, no block object); exit 1 + reason 'curfew' -> block with config curfew.message (substitute {start}/{end} from curfew.start/end); reason 'clock tamper' -> block with curfew.tamper_message; reason 'ntp unreachable' -> block with curfew.offline_message; (guard threw / exit not in {0,1} / no parseable JSON) -> fail-closed block with curfew.offline_message (safest default). Emit the block using the EXACT Task-1-confirmed CC contract (stdout JSON shape and/or exit code). Read all butler strings from config.yaml (do NOT hardcode); preserve the Spanish-butler n-tilde via the [char]0x00F1 handling from the legacy gate. Wrap the whole body in try/catch so ANY exception emits the fail-closed block (Pitfall 1). Implements D-02 + WIRE-01 (single canonical config source).</action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -Command "$null = [scriptblock]::Create((Get-Content -Raw 'C:/Users/20252128/dev/Projects/LifeOS/hooks/nightguard_adapter.ps1')); if($?){'PARSE OK'}else{'PARSE FAIL'}"</automated>
  </verify>
  <acceptance_criteria>
    - The script parses under Windows PowerShell 5.1 (scriptblock-create succeeds; ASCII-only, no em-dash literal).
    - Contains the literal `$env:NIGHTGUARD_DIR` and a fail-closed throw on empty (grep both).
    - Contains references to `curfew.message`, `tamper_message`, and `offline_message` read from config (no hardcoded butler string literals in the adapter — grep shows the messages come from the parsed config object).
    - Implements the allow_commands pass (grep `allow_commands`) that exits 0 on a /shutdown-style match BEFORE the guard invocation.
    - The terminal/error path emits a BLOCK (fail-closed), not an allow — a simulated guard-throw produces the confirmed block contract.
  </acceptance_criteria>
  <done>Adapter parses 5.1-clean, resolves NIGHTGUARD_DIR fail-closed, owns the allow-command pass, translates every guard reason to the confirmed block schema with config-sourced butler messages, and fail-closes on any error.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite nightguard_watchdog.ps1 + write install_nightguard.ps1 deploy script</name>
  <files>C:/Users/20252128/dev/Projects/LifeOS/hooks/nightguard_watchdog.ps1, scripts/deploy/install_nightguard.ps1</files>
  <read_first>
    - C:/Users/20252128/.claude/hooks/nightguard_app_watchdog.ps1 (legacy watchdog: -Loop shape, Get-Process probe + shell:AppsFolder relaunch, minimal YAML apps[] parser, stale config path line 13, log path line 73, early-return lines 78-79)
    - C:/Users/20252128/.claude/hooks/nightguard_install_watchdog.ps1 (Register-ScheduledTask pattern to reuse for re-register)
    - scripts/guard/nightguard_guard.ps1 lines 38-48 (data-dir stanza + the $here\..\interop dot-source the deploy layout must preserve)
    - scripts/guard/verify_hook_integrity.ps1 lines 30-73 ($registered repo-relative paths + $repoRoot=$here\..\.. — the A5/Pitfall 5 mismatch to resolve)
    - .planning/phases/05-instance-wiring/05-PATTERNS.md watchdog section + deploy section + integrity-re-stamp section
    - .planning/phases/05-instance-wiring/05-RESEARCH.md "Deploy / Install Step", "StayFree Process Name", Pitfalls 2/3/4/5
  </read_first>
  <action>TWO artifacts. (A) nightguard_watchdog.ps1 — rewrite the legacy watchdog in place (D-04): KEEP the -Loop param + while/Start-Sleep shape, the per-app `Get-Process -Name StayFree` liveness probe, the UWP relaunch via `explorer.exe shell:AppsFolder\37081StayFreeApps.StayFree3_fqhk48m1tsma0!stayfree`, and the minimal YAML apps[] parser. CHANGE: replace the hardcoded stale `Join-Path $env:USERPROFILE '.claude\nightguard\config.yaml'` (legacy line 13 — the D-05 stale-config early-return root cause) with the NIGHTGUARD_DIR fail-closed stanza ($configPath = Join-Path $DataDir 'config.yaml'); move the log path under $DataDir too; keep the `if watchdog.enabled -ne true { return }` guard but now reading the canonical enabled:true config so it proceeds and relaunches StayFree (the WIRE-01 acceptance signal, D-05). 5.1-safe ASCII. (B) scripts/deploy/install_nightguard.ps1 — a deploy helper that: byte-copies (via [IO.File]::Copy, NEVER Get-Content|Set-Content per Pitfall 3) scripts/guard/nightguard_guard.ps1, scripts/guard/verify_hook_integrity.ps1, scripts/guard/guard.baseline.sha256 into LifeOS/hooks/; copies scripts/interop/nightguard_interop.ps1 + state_interop.ps1 into LifeOS/hooks/interop/ (preserve the sibling so the guard's ..\interop dot-source resolves, Pitfall 4); copies the new nightguard_adapter.ps1 + nightguard_watchdog.ps1 into LifeOS/hooks/. Then RESOLVE the verify_hook_integrity A5/Pitfall-5 mismatch with the chosen disposition: run the integrity verifier from the REPO against the repo copies (source-integrity scope) — i.e. the deploy script re-stamps `verify_hook_integrity.ps1 -Update` against scripts/guard + scripts/interop in the repo (where $repoRoot=$here\..\.. resolves correctly), and the deployed copies are byte-identical to those re-stamped repo bytes (guaranteed by [IO.File]::Copy + the existing .gitattributes -text pins). Include (but do NOT yet invoke) a Register-ScheduledTask block repointing the existing NightguardWatchdog task to the new nightguard_watchdog.ps1 path (D-06 — the Scheduled-Task delivery mechanism is unchanged, only the script path moves), mirroring nightguard_install_watchdog.ps1 — Plan 03 invokes it post-gate. Document inline that the settings.json swap + task Start are deferred to Plan 03 (D-11).</action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -Command "foreach($f in @('C:/Users/20252128/dev/Projects/LifeOS/hooks/nightguard_watchdog.ps1','scripts/deploy/install_nightguard.ps1')){ $null=[scriptblock]::Create((Get-Content -Raw $f)); if(-not $?){throw $f} }; 'PARSE OK'"</automated>
  </verify>
  <acceptance_criteria>
    - Both scripts parse under Windows PowerShell 5.1 (ASCII-only, no em-dash literal).
    - nightguard_watchdog.ps1 contains `$env:NIGHTGUARD_DIR` + fail-closed throw, contains `Get-Process` and `StayFree`, and contains NO `\.claude\nightguard\config.yaml` literal (the stale path is gone — grep returns 0 after stripping comments).
    - install_nightguard.ps1 uses `[IO.File]::Copy` (grep present) and NEVER `Set-Content` for the script bodies; references the hooks/interop/ sibling target.
    - install_nightguard.ps1 contains a Register-ScheduledTask block naming `NightguardWatchdog` and `nightguard_watchdog.ps1`, and an inline note that Start/settings.json swap are deferred to Plan 03.
    - The chosen integrity-scope disposition (repo-source re-stamp) is documented inline (the A5/Pitfall-5 decision is explicit, not silent).
  </acceptance_criteria>
  <done>Watchdog reads canonical config via NIGHTGUARD_DIR (stale path removed) and relaunches StayFree; deploy script byte-copies all five script artifacts preserving the interop sibling, re-stamps the baseline at repo-source scope, and stages (but does not fire) the watchdog re-register.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude-Code -> adapter (stdin prompt) | Untrusted prompt text crosses into the allow-command matcher |
| adapter -> guard (subprocess) | The adapter trusts only the guard's exit code + JSON; any other output is fail-closed |
| repo scripts -> LifeOS/hooks (deploy) | Byte form must survive the copy or the integrity baseline / HMAC breaks |
| integrity baseline scope | The re-stamp must hash the exact bytes that will be verified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Spoofing | CC block contract guessed wrong | mitigate | Task 1 empirically confirms the live UserPromptSubmit block schema BEFORE the adapter is finalized (A2); fail-open is the failure mode being closed |
| T-05-07 | Elevation | unset NIGHTGUARD_DIR -> guard/adapter throw -> fail-open | mitigate | Adapter wraps its whole body in try/catch and emits the confirmed block on ANY throw/non-{0,1}/no-JSON (Pitfall 1); watchdog + adapter both use the fail-closed throw stanza |
| T-05-08 | Tampering | deploy byte-corruption breaks baseline/HMAC | mitigate | [IO.File]::Copy only (never Set-Content); re-stamp AFTER deploy; rely on .gitattributes -text pins (Pitfall 3) |
| T-05-09 | Tampering | guard ..\interop dot-source fails post-deploy | mitigate | Install script preserves the hooks/interop/ sibling dir (Pitfall 4) |
| T-05-10 | Repudiation | integrity baseline hashes wrong (repo vs deployed) bytes | mitigate | A5 disposition fixed: re-stamp at repo-source scope where $repoRoot resolves; deployed copies are byte-identical via [IO.File]::Copy (Pitfall 5) |
| T-05-11 | Information disclosure | butler messages hardcoded in adapter (drift from config) | mitigate | Messages read from config.yaml only; acceptance grep forbids hardcoded literals |
</threat_model>

<verification>
- Adapter + watchdog + install script all parse clean under PowerShell 5.1.
- CC block schema empirically confirmed (Task 1).
- Adapter: NIGHTGUARD_DIR fail-closed, allow-command pass, config-sourced messages, fail-closed on error.
- Watchdog: stale ~/.claude path removed, reads canonical config, StayFree relaunch present.
- Install: byte-exact copy, interop sibling preserved, repo-source baseline re-stamp, watchdog re-register staged (not fired).
</verification>

<success_criteria>
The three deployable artifacts exist and are 5.1-clean: the adapter translates the guard's verdict into the confirmed Claude-Code block schema (fail-closed on any error, config-sourced butler messages, /shutdown allow-pass), the watchdog reads the single canonical config and relaunches StayFree, and the install script byte-deploys everything and re-stamps integrity — all WITHOUT yet swapping the live gate or starting the task.
</success_criteria>

<output>
Create `.planning/phases/05-instance-wiring/05-02-SUMMARY.md` when done
</output>
</content>
