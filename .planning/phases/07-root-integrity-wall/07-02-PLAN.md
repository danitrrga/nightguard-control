---
phase: 07-root-integrity-wall
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
status: blocked
blocked_on: "P0 — Python trust stack absent/untracked. Restore + commit before executing. Also needs 07-01 (root key) done."
files_modified:
  - LifeOS/scripts/nightguard/<control-cli>.py    # run-as-root sign + quota + chown-back
  - /etc/sudoers.d/nightguard                       # narrow rule, password required
autonomous: false
requirements: [ROOT-03, ROOT-04]
must_haves:
  truths:
    - "Allowed edits are signed only by the control-CLI running as root (via sudo); a non-root invocation cannot read the key and refuses"
    - "The control-CLI enforces the weekly token quota in-process and commits sanctioned→guard.json→config.yaml under flock, then restores config.yaml to user ownership"
    - "A hand-forged config (no valid key) is reverted; sudo is the only way to produce a sanctioned change"
  artifacts:
    - path: "/etc/sudoers.d/nightguard"
      provides: "narrow, password-required sudo rule scoped to the control-CLI"
      contains: "danitrrga"
  key_links:
    - from: "the TUI/CLI (Phase 10)"
      to: "LifeOS/scripts/nightguard/<control-cli>.py"
      via: "sudo invocation (password) → root reads key → sign+commit"
      pattern: "sudo"
---

<precondition>
## ⛔ BLOCKED on P0 + 07-01
Needs the restored control-CLI source AND 07-01 (root-owned key). Read the restored
control-CLI first; the tasks specify behavior/contracts.
</precondition>

<objective>
Make the existing control-CLI the **sole signer**, reachable only by elevating via `sudo`,
enforcing the weekly token quota in-process. Closes ROOT-03 (sign-via-sudo, no daemon/socket)
and ROOT-04 (forge infeasible; sudo the sole bypass).

Purpose: with the key root-owned (07-01), only root can sign. Rather than build a daemon, the
user app shells `sudo <control-cli> commit …`; the password prompt is the deliberate
anti-impulse friction. Quota lives in the CLI (one enforcement point).
</objective>

<context>
@.planning/phases/07-root-integrity-wall/07-CONTEXT.md
@.planning/phases/07-root-integrity-wall/07-01-SUMMARY.md
@.planning/REQUIREMENTS.md   (ROOT-03, ROOT-04)

<interfaces>
<!-- control-CLI commit contract (must hold after this plan) -->
  sudo /usr/bin/python3 /home/danitrrga/dev/Projects/LifeOS/scripts/nightguard/<control-cli>.py commit <new_config_path|args>
    1. acquire .nightguard.lock flock
    2. read root .guardkey; classify + apply weekly-token quota (decrement on loosen; refuse at 0 with "available again Monday")
    3. write config.sanctioned.yaml → re-sign guard.json (config_hmac + state_hmac A3) → write config.yaml LAST
    4. chown config.yaml back to $SUDO_UID:$SUDO_GID (D-5) so the user can hand-edit + the watchdog can revert
    5. release flock

<!-- /etc/sudoers.d/nightguard (password REQUIRED — no NOPASSWD; 0440 root:root, validate with visudo -c) -->
  danitrrga ALL=(root) /usr/bin/python3 /home/danitrrga/dev/Projects/LifeOS/scripts/nightguard/<control-cli>.py *
  # NOTE: tighten the arg surface — prefer a fixed subcommand + in-CLI arg validation over a permissive trailing '*' (sudoers arg-injection footgun)
</interfaces>
</context>

<tasks>

<task type="manual" tdd="false">
  <name>Task 1: Control-CLI signs as root + enforces quota + restores config.yaml ownership</name>
  <files>LifeOS/scripts/nightguard/&lt;control-cli&gt;.py</files>
  <read_first>
    - the restored control-CLI commit/sign path (flock, sanctioned→guard.json→config order)
    - 07-CONTEXT.md §D-5 (chown config.yaml back to the invoking user)
    - the Rust quota model for parity (mutation-engine `quota`/`classify`) — the CLI mirrors: loosen costs 1/3, Monday reset, refuse at 0 with the locked reason
  </read_first>
  <action>
    Ensure the control-CLI, when run as root: reads the root key; classifies the proposed change;
    applies the weekly token quota (decrement only on a loosen; free for tighten/noop; refuse a
    0-token loosen with "available again Monday"); commits sanctioned→guard.json→config.yaml under
    flock in that order; then `os.chown(config.yaml, SUDO_UID, SUDO_GID)`. Refuse cleanly if run
    as non-root (cannot read key) with a clear message.
  </action>
  <acceptance_criteria>
    - As root: a tighten commits free; a loosen decrements a token; a 0-token loosen is refused with the locked reason; guard.json verifies after.
    - config.yaml is `danitrrga:danitrrga` again after a commit (not root-owned).
    - As non-root: the CLI refuses (key unreadable), writing nothing.
  </acceptance_criteria>
  <done>The control-CLI is the sole root signer with in-process quota and ownership restore.</done>
</task>

<task type="manual" tdd="false">
  <name>Task 2: Narrow sudoers rule (password required)</name>
  <files>/etc/sudoers.d/nightguard</files>
  <read_first>07-CONTEXT.md §"Sudoers footgun"</read_first>
  <action>
    Add `/etc/sudoers.d/nightguard` (0440 root:root) granting `danitrrga` the right to run the
    control-CLI as root **with a password** (no NOPASSWD). Scope it as tightly as the CLI's arg
    surface allows; validate with `visudo -c`. Document that the password prompt is intentional
    (ROOT-04 friction).
  </action>
  <verify><automated>sudo visudo -cf /etc/sudoers.d/nightguard</automated></verify>
  <acceptance_criteria>
    - `visudo -c` passes; `sudo <control-cli> commit …` prompts for the password and works.
    - The rule does not grant broader root than the control-CLI; no NOPASSWD.
  </acceptance_criteria>
  <done>Signing is reachable only via a password-gated sudo to the control-CLI.</done>
</task>

<task type="manual" tdd="false">
  <name>Task 3: Prove ROOT-03/04 end-to-end</name>
  <files>LifeOS/nightguard/ (verification only)</files>
  <action>
    1. `sudo <control-cli> commit` a tighten → watchdog accepts (no revert); a loosen → token
       decrements, watchdog accepts. 2. Hand-edit config.yaml WITHOUT sudo → watchdog reverts
       (ROOT-04: no valid signature without the root key). 3. Confirm a non-root direct CLI run
       cannot sign. 4. Confirm a 0-token loosen is refused.
  </action>
  <acceptance_criteria>
    - Legit `sudo` commit is accepted (no spurious revert); quota enforced; hand-edit reverts; non-root cannot sign.
  </acceptance_criteria>
  <done>ROOT-03 + ROOT-04 proven: sudo→control-CLI is the only path to a sanctioned change.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-07-05 | Elevation (broaden root via sudoers) | mitigate | Narrow rule scoped to the control-CLI; password required; in-CLI arg validation (no permissive wildcard execution); visudo-checked. |
| T-07-06 | Tampering (config.yaml left root-owned, user can't hand-edit/revert-demo) | mitigate | D-5: CLI chowns config.yaml back to $SUDO_UID after every commit. |
| T-07-07 | Bypass (quota skipped) | mitigate | Quota enforced in the single root signer; non-root cannot sign at all, so there is no un-quota'd signing path. |
| T-07-08 | Repudiation | accept (residual) | The user is a sudoer and can always `sudo` — by design (ROOT-04 friction, not absolute lock); HMAC-chained audit records the elevated commit. |
</threat_model>

<verification>
- `sudo <control-cli> commit` signs + the watchdog accepts; quota decrements on loosen and refuses at 0.
- A non-root invocation cannot read the key / cannot sign; a hand-edit (no sudo) reverts.
- config.yaml ownership returns to the user after each commit; sudoers passes visudo and requires a password.
</verification>

<success_criteria>
- ROOT-03: allowed edits signed by the control-CLI via sudo (no daemon/socket); quota enforced in-CLI under flock.
- ROOT-04: hand-forge infeasible without the root key; sudo is the sole bypass.
</success_criteria>

<output>Create `.planning/phases/07-root-integrity-wall/07-02-SUMMARY.md` when done.</output>
</content>
