---
phase: 07-root-integrity-wall
plan: 01
type: execute
wave: 1
depends_on: []
status: blocked
blocked_on: "P0 — Python trust stack absent/untracked. Restore + commit before executing."
files_modified:
  - LifeOS/scripts/nightguard/nightguard_watchdog.py   # run-as-root + absolute paths
  - LifeOS/scripts/nightguard/ngcommon.py              # NIGHTGUARD_DIR resolution (if needed)
  - /etc/systemd/system/nightguard-watchdog.service    # new system unit
  - /etc/systemd/system/nightguard-watchdog.timer      # new system timer
  - LifeOS/nightguard/.guardkey                         # chown root:root 0600
  - LifeOS/nightguard/config.sanctioned.yaml            # chown root:root 0644
  - LifeOS/nightguard/guard.json                        # chown root:root 0644
autonomous: false
requirements: [ROOT-01, ROOT-02]
must_haves:
  truths:
    - "The watchdog runs as a systemd SYSTEM service (root), reverts a hand-edited config.yaml to sanctioned within one tick, and verifies the browser managed-policy each tick"
    - "After cutover, .guardkey is root:root 0600 and a non-root user cannot read it; config.sanctioned.yaml + guard.json are root:root 0644; config.yaml stays user:user 0644"
    - "There is never a window where no watchdog is verifying the config (prove-then-switch)"
  artifacts:
    - path: "/etc/systemd/system/nightguard-watchdog.timer"
      provides: "root system timer replacing the --user timer (60s tick)"
      contains: "OnUnitActiveSec"
  key_links:
    - from: "/etc/systemd/system/nightguard-watchdog.service"
      to: "LifeOS/scripts/nightguard/nightguard_watchdog.py"
      via: "ExecStart absolute path + Environment=NIGHTGUARD_DIR"
      pattern: "ExecStart"
---

<precondition>
## ⛔ BLOCKED on P0
The Python trust stack (`ngcommon.py`/`guard.py`/`nightguard_watchdog.py`) must be restored
to `LifeOS/scripts/nightguard/` and committed to git before this runs. Read the restored
sources first — the tasks below specify behavior/contracts, not line edits.
</precondition>

<objective>
Make the integrity wall root-backed: run the watchdog as a systemd **system** service (root)
and flip the trust artifacts to root ownership — done as one **prove-then-switch** cutover so
the config is never left unverified. Closes ROOT-01 + ROOT-02.

Purpose: today the key is a user-readable 0600 file, so the impulsive self can re-sign a
loosened config and beat the revert. Root-owning the key turns "reverts" into "can't forge
without `sudo`." A `--user` watchdog cannot read a root key, so the watchdog must become a
system service in the same motion.
</objective>

<context>
@.planning/phases/07-root-integrity-wall/07-CONTEXT.md
@.planning/ROADMAP.md   (§ Phase 7)
@.planning/REQUIREMENTS.md   (ROOT-01, ROOT-02)

<interfaces>
<!-- current --user unit (to retire), verified -->
  ~/.config/systemd/user/nightguard-watchdog.service: ExecStart=/usr/bin/env python3 %h/dev/Projects/LifeOS/scripts/nightguard/nightguard_watchdog.py
  ~/.config/systemd/user/nightguard-watchdog.timer:   OnBootSec=30, OnUnitActiveSec=60, AccuracySec=15

<!-- target system unit (no %h on a system unit; absolute path + env) -->
  /etc/systemd/system/nightguard-watchdog.service:
    [Service] Type=oneshot
    Environment=NIGHTGUARD_DIR=/home/danitrrga/dev/Projects/LifeOS/nightguard
    ExecStart=/usr/bin/python3 /home/danitrrga/dev/Projects/LifeOS/scripts/nightguard/nightguard_watchdog.py
    # runs as root by default
  /etc/systemd/system/nightguard-watchdog.timer: OnBootSec=30, OnUnitActiveSec=60, AccuracySec=15, [Install] WantedBy=timers.target

<!-- target perms (D-1) -->
  .guardkey               root:root 0600
  config.sanctioned.yaml  root:root 0644
  guard.json              root:root 0644
  config.yaml             danitrrga:danitrrga 0644  (unchanged)
</interfaces>
</context>

<tasks>

<task type="manual" tdd="false">
  <name>Task 1: Make the watchdog root-runnable (absolute paths + NIGHTGUARD_DIR)</name>
  <files>LifeOS/scripts/nightguard/nightguard_watchdog.py, LifeOS/scripts/nightguard/ngcommon.py</files>
  <read_first>
    - the restored ngcommon.py path-resolution (how it finds NIGHTGUARD_DIR / the key / config)
    - 07-CONTEXT.md §Decisions D-2 (system unit has no %h)
  </read_first>
  <action>
    Ensure the watchdog resolves all paths from `NIGHTGUARD_DIR` (env), not from `$HOME`/`%h`
    or a path relative to the script, so it behaves identically when launched by a root system
    unit. No behavior change yet — still reads the user-readable key. Confirm it reads the key,
    config.yaml, config.sanctioned.yaml, guard.json, and the policies/ dir by absolute path.
  </action>
  <acceptance_criteria>
    - `sudo NIGHTGUARD_DIR=/home/danitrrga/dev/Projects/LifeOS/nightguard /usr/bin/python3 .../nightguard_watchdog.py` runs one tick cleanly as root (reverts if config mismatched, else `tick: ok`).
    - No path in the watchdog depends on `$HOME`/`%h`/cwd.
  </acceptance_criteria>
  <done>The watchdog is path-clean and runs correctly under root with NIGHTGUARD_DIR.</done>
</task>

<task type="manual" tdd="false">
  <name>Task 2: Install the system unit + PROVE it (key still user-readable)</name>
  <files>/etc/systemd/system/nightguard-watchdog.service, /etc/systemd/system/nightguard-watchdog.timer</files>
  <read_first>07-CONTEXT.md §D-3 (prove-then-switch); the current --user timer cadence</read_first>
  <action>
    Write the system `.service` + `.timer` (interfaces block) to `/etc/systemd/system/`,
    `sudo systemctl daemon-reload`, `enable --now nightguard-watchdog.timer`. Leave the `--user`
    timer running for now (both verify the same config — idempotent reverts, no harm). PROVE the
    root watchdog: hand-edit `config.yaml` (flip a value) and confirm the next root tick reverts
    it to sanctioned (watch `journalctl -u nightguard-watchdog` + `watchdog.log`), and the
    browser-policy check stays green.
  </action>
  <acceptance_criteria>
    - `systemctl status nightguard-watchdog.timer` (system) active; a root tick reverts a hand-edit.
    - Browser managed-policy verification passes under root.
  </acceptance_criteria>
  <done>The root system watchdog is proven to revert + verify before any ownership flip.</done>
</task>

<task type="manual" tdd="false">
  <name>Task 3: Switch — chown to root + retire the --user timer (ROOT-01)</name>
  <files>LifeOS/nightguard/.guardkey, LifeOS/nightguard/config.sanctioned.yaml, LifeOS/nightguard/guard.json</files>
  <read_first>07-CONTEXT.md §"core protection model" (the perms table)</read_first>
  <action>
    Only after Task 2 proves green: `sudo chown root:root .guardkey config.sanctioned.yaml
    guard.json`; `sudo chmod 0600 .guardkey`; `sudo chmod 0644 config.sanctioned.yaml guard.json`.
    Leave `config.yaml` as `danitrrga:danitrrga 0644`. Then retire the user watchdog:
    `systemctl --user disable --now nightguard-watchdog.timer` and remove the `--user` unit files.
  </action>
  <verify>
    <automated>test "$(stat -c '%U:%U %a' /home/danitrrga/dev/Projects/LifeOS/nightguard/.guardkey)" = "root:root 600"</automated>
    <automated>! cat /home/danitrrga/dev/Projects/LifeOS/nightguard/.guardkey >/dev/null 2>&1 && echo "user cannot read key (expected)"</automated>
  </verify>
  <acceptance_criteria>
    - `.guardkey` = root:root 0600; a non-root `cat .guardkey` is Permission denied.
    - `config.sanctioned.yaml` + `guard.json` = root:root 0644 (user can read, not write).
    - `config.yaml` still user-writable; a hand-edit reverts within one tick.
    - Only the system timer remains; the `--user` timer is gone.
  </acceptance_criteria>
  <done>Key + sanctioned + state are root-owned; the root system watchdog is the sole verifier; no protection gap occurred.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-07-01 | Elevation (forge a loosened config) | mitigate | Key root:root 0600 → non-root cannot read it → cannot produce a valid HMAC. The wall. |
| T-07-02 | Tampering (replace the revert target) | mitigate | sanctioned root:root 0644 → only root writes it; the user can't swap in a loosened "sanctioned". |
| T-07-03 | DoS (protection gap during cutover) | mitigate | Prove-then-switch (D-3): root watchdog proven reverting BEFORE the key chown / --user retire. |
| T-07-04 | Tampering (config.yaml becomes root-owned, user locked out of edits) | mitigate | config.yaml stays user:user; the revert model (not prevention) governs the live file. |
</threat_model>

<verification>
- Non-root cannot read `.guardkey`; root system watchdog reverts a hand-edited config.yaml within one tick; browser-policy green.
- Perms exactly match the D-1 table; only the system timer remains.
- No moment in the cutover left the config unverified.
</verification>

<success_criteria>
- ROOT-01: `.guardkey` + sanctioned root-owned; user cannot read the key.
- ROOT-02: the watchdog runs as a systemd system service (true-time + HMAC-revert + browser-policy).
</success_criteria>

<output>Create `.planning/phases/07-root-integrity-wall/07-01-SUMMARY.md` when done.</output>
</content>
