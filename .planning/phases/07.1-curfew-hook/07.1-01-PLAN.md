---
phase: 07.1-curfew-hook
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - /tmp/claude-1000/-home-danitrrga-dev-Projects-nightguard-control/7a930441-7054-4858-aa96-73b5169ae964/scratchpad/probe_ups.py
  - .planning/phases/07.1-curfew-hook/07.1-MATCHER-DECISION.md
autonomous: false
requirements: [CURF-01]
user_setup: []

must_haves:
  truths:
    - "The exact contents of the UserPromptSubmit `prompt` field for /plan, /shutdown, and a plain prompt are captured from the live runtime"
    - "A documented matcher decision (substring vs leading-token) exists, defaulting to substring if the probe is inconclusive"
  artifacts:
    - path: ".planning/phases/07.1-curfew-hook/07.1-MATCHER-DECISION.md"
      provides: "The recorded Q1 probe evidence + chosen matcher strategy that Plan 02 consumes"
      contains: "MATCHER:"
  key_links:
    - from: ".planning/phases/07.1-curfew-hook/07.1-MATCHER-DECISION.md"
      to: "07.1-02-PLAN.md adapter allowlist task"
      via: "MATCHER: line read by the adapter author"
      pattern: "MATCHER:\\s*(substring|leading-token)"
---

<objective>
Resolve RESEARCH Open Question Q1 (top integration risk): determine whether the Claude Code
`UserPromptSubmit` hook's stdin `prompt` field exposes the raw `/plan …` leading token or the
expanded `<command-name>/plan</command-name>…` blob for skill-backed slash commands on this
runtime (Claude Code 2.1.186). The answer dictates the adapter's allowlist matcher (Plan 02).

Purpose: The `/plan` and `/shutdown` curfew escape valve (D-11/D-12/D-13) only works if the
adapter's matcher actually matches what the runtime delivers. A wrong matcher silently breaks
the escape valve (Pitfall 2). This probe is the empirical gate.

Output: A captured `prompt`-field log + a recorded matcher decision (`07.1-MATCHER-DECISION.md`)
that Plan 02 reads. Defaults to **substring** (the proven Windows-adapter behavior) if the
probe is inconclusive — never blocks the adapter port.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-curfew-hook/07.1-RESEARCH.md
@.planning/phases/07.1-curfew-hook/07.1-CONTEXT.md

<interfaces>
<!-- The staged probe (already written) and what it writes. Read it before running. -->
Probe script (staged, ready to run):
  /tmp/claude-1000/-home-danitrrga-dev-Projects-nightguard-control/7a930441-7054-4858-aa96-73b5169ae964/scratchpad/probe_ups.py
  - reads sys.stdin, appends RAW + KEYS + PROMPT_FIELD to probe_capture.log, always exit 0

Capture log it writes:
  /tmp/claude-1000/-home-danitrrga-dev-Projects-nightguard-control/7a930441-7054-4858-aa96-73b5169ae964/scratchpad/probe_capture.log

Live settings file the probe hook must be (temporarily) wired into for global reach:
  ~/.claude/settings.json   (currently keys: enabledPlugins, extraKnownMarketplaces, theme, skipDangerousModePermissionPrompt — NO hooks block yet)

UserPromptSubmit hook entry shape (timeout in SECONDS):
  {"hooks": {"UserPromptSubmit": [{"hooks": [{"type": "command",
     "command": "python3 /abs/path/probe_ups.py", "timeout": 10}]}]}}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the staged probe into ~/.claude/settings.json (temporary, reversible)</name>
  <read_first>
    - /tmp/claude-1000/-home-danitrrga-dev-Projects-nightguard-control/7a930441-7054-4858-aa96-73b5169ae964/scratchpad/probe_ups.py (the probe being wired — confirm it reads stdin and exits 0)
    - ~/.claude/settings.json (the file being modified — confirm current keys, no hooks block)
    - .planning/phases/07.1-curfew-hook/07.1-RESEARCH.md (Open Question Q1, lines 369-385; hook stdin schema Pattern 1)
  </read_first>
  <action>
    Back up `~/.claude/settings.json` to `~/.claude/settings.json.probe-bak` first. Then load it as
    JSON, add a `hooks.UserPromptSubmit` array containing one matcher group with a single command
    hook: `type: "command"`, `command: "python3 <absolute path to probe_ups.py>"`, `timeout: 10`.
    Preserve every existing key (enabledPlugins, extraKnownMarketplaces, theme,
    skipDangerousModePermissionPrompt). Write the file back with 2-space indent, UTF-8,
    ensure_ascii=False. Use an absolute python3 path to the probe (AGENT-SYSTEM Linux rule — relative
    paths broke historically per RESEARCH line 427-430). This is a TEMPORARY instrumentation hook;
    Task 3 removes it.
  </action>
  <verify>
    <automated>python3 -c "import json,os; d=json.load(open(os.path.expanduser('~/.claude/settings.json'))); h=d['hooks']['UserPromptSubmit'][0]['hooks'][0]; assert h['command'].startswith('python3 /') and 'probe_ups.py' in h['command'], h; assert all(k in d for k in ('enabledPlugins','theme','extraKnownMarketplaces','skipDangerousModePermissionPrompt')); print('OK probe wired, keys preserved')"</automated>
  </verify>
  <acceptance_criteria>
    - `~/.claude/settings.json` contains a `hooks.UserPromptSubmit` entry whose `command` is an absolute `python3 …probe_ups.py` path
    - All four pre-existing top-level keys (enabledPlugins, extraKnownMarketplaces, theme, skipDangerousModePermissionPrompt) are still present
    - `~/.claude/settings.json.probe-bak` exists (the rollback copy)
  </acceptance_criteria>
  <done>Probe hook is wired into user-scope settings with the original file backed up.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Probe the live UserPromptSubmit prompt field</name>
  <action>PAUSE for the human: drive an interactive Claude Code session per <how-to-verify>; the agent cannot fire UserPromptSubmit itself. Resume on the reported PROMPT_FIELD values.</action>
  <what-built>The probe hook is wired into `~/.claude/settings.json`. It captures the raw
  `UserPromptSubmit` stdin (including the `prompt` field) to a log on every prompt, then exits 0
  (it blocks nothing — it is pure instrumentation).</what-built>
  <how-to-verify>
    Only an interactive Claude Code session can drive `UserPromptSubmit`, so this step needs you:
    1. Start a NEW Claude Code session (the probe hook loads at session start), in any directory.
    2. Submit exactly these three prompts, one at a time:
       a. `/plan test`
       b. `/shutdown`
       c. `hello world` (a plain, non-slash prompt)
    3. Then read the capture log:
       `cat /tmp/claude-1000/-home-danitrrga-dev-Projects-nightguard-control/7a930441-7054-4858-aa96-73b5169ae964/scratchpad/probe_capture.log`
    4. Look at the `PROMPT_FIELD:` line for each. Report back which form it is:
       - RAW LEADING TOKEN  → the field literally starts with `/plan` / `/shutdown`
       - EXPANDED BLOB      → the field contains `<command-name>/plan</command-name>…`
       - EMPTY/OTHER        → the field is empty or the event did not fire for the slash command
  </how-to-verify>
  <resume-signal>Paste the three `PROMPT_FIELD:` values (or type "raw-token", "expanded-blob", or "empty/inconclusive")</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record the matcher decision and unwire the probe</name>
  <read_first>
    - /tmp/claude-1000/-home-danitrrga-dev-Projects-nightguard-control/7a930441-7054-4858-aa96-73b5169ae964/scratchpad/probe_capture.log (the captured evidence to record)
    - ~/.claude/settings.json (the file being reverted)
    - ~/.claude/settings.json.probe-bak (the rollback copy from Task 1)
    - .planning/phases/07.1-curfew-hook/07.1-RESEARCH.md (Q1 decision rule: default substring unless raw leading token confirmed)
  </read_first>
  <action>
    Write `.planning/phases/07.1-curfew-hook/07.1-MATCHER-DECISION.md` containing: (a) the three
    captured `PROMPT_FIELD:` values verbatim, and (b) a single decisive line `MATCHER: substring`
    or `MATCHER: leading-token`. Decision rule (RESEARCH Q1): choose `leading-token` ONLY if all
    three captures show the raw `/plan`/`/shutdown` token as the first whitespace-delimited token
    (i.e. `prompt.split()[0]` would equal the command); otherwise choose `substring` (the
    fail-safe that mirrors the proven Windows adapter and keeps the escape valve working). Then
    UNWIRE the probe: restore `~/.claude/settings.json` from `~/.claude/settings.json.probe-bak`
    (so the file has NO hooks block again — Plan 03 installs the real curfew hook), and delete the
    `.probe-bak` file. Do NOT leave the probe hook live.
  </action>
  <verify>
    <automated>grep -E '^MATCHER:[[:space:]]+(substring|leading-token)$' .planning/phases/07.1-curfew-hook/07.1-MATCHER-DECISION.md && python3 -c "import json,os; d=json.load(open(os.path.expanduser('~/.claude/settings.json'))); assert 'hooks' not in d or not d.get('hooks'), 'probe hook still wired'; assert not os.path.exists(os.path.expanduser('~/.claude/settings.json.probe-bak')), 'bak not cleaned'; print('OK decision recorded, probe unwired')"</automated>
  </verify>
  <acceptance_criteria>
    - `07.1-MATCHER-DECISION.md` contains exactly one `MATCHER: substring` or `MATCHER: leading-token` line plus the three captured PROMPT_FIELD values
    - The decision is `leading-token` ONLY when all three captures expose the raw leading token; otherwise it is `substring`
    - `~/.claude/settings.json` has no hooks block (probe removed); `settings.json.probe-bak` is deleted
  </acceptance_criteria>
  <done>The matcher strategy is recorded for Plan 02 and the temporary probe instrumentation is fully removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| interactive user → Claude Code runtime | the user submits prompts; the runtime invokes the probe hook with untrusted prompt text on stdin |
| probe hook → scratchpad log | the probe writes captured stdin to a session-local scratchpad file (no trust impact) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-071-01 | Tampering | the probe is left wired and masquerades as enforcement | mitigate | The probe exits 0 (blocks nothing) and Task 2 mandatorily unwires it + restores from .probe-bak; verify gate asserts no hooks block remains |
| T-071-02 | Information disclosure | captured prompt text contains sensitive input | accept | Captures land only in the session-local scratchpad (not git-tracked); ephemeral probe data, deleted with the session |
| T-071-03 | Bypass | wrong matcher chosen → escape valve breaks OR curfew dodgeable | mitigate | Decision rule defaults to substring (fail-safe escape valve); leading-token chosen only on positive raw-token evidence from all three captures |
</threat_model>

<verification>
- The three slash/plain prompts were captured from a live `UserPromptSubmit` fire.
- `07.1-MATCHER-DECISION.md` records a single unambiguous `MATCHER:` line.
- `~/.claude/settings.json` is back to its pre-probe state (no hooks); no `.probe-bak` left behind.
</verification>

<success_criteria>
- Q1 is empirically resolved: the matcher strategy is recorded and consumable by Plan 02.
- This plan does NOT block Plan 02 from starting (Plan 02 defaults to substring if this is inconclusive).
- No live instrumentation hook remains on the runtime.
</success_criteria>

<output>
Create `.planning/phases/07.1-curfew-hook/07.1-01-SUMMARY.md` when done.
Record the chosen `MATCHER:` value prominently so Plan 02 can read it.
</output>
