---
phase: 07.1-curfew-hook
plan: 03
subsystem: curfew-hook
tags: [claude-code-hooks, userpromptsubmit, curfew, generator, global-wiring, idempotent, fail-closed]

# Dependency graph
requires:
  - phase: 07.1-02
    provides: "LifeOS/hooks/nightguard_adapter.py (the curfew adapter: guard deny -> hook exit 2 + butler stderr, leading-token allowlist, fail-closed); /plan + /shutdown signed live in config.yaml allow_commands"
  - phase: 07.1-01
    provides: "MATCHER: leading-token decision; empirical proof UserPromptSubmit fires (exposes raw prompt) on the live runtime"
  - phase: 07-root-wall
    provides: "soft-mode guard.py verdict engine + NTP override test seams"
provides:
  - "sync_agent_config.py write_curfew_hook() — net-new idempotent writer that emits the global UserPromptSubmit curfew hook into user-scope ~/.claude/settings.json (D-03 global reach, D-04 generator-owned, survives SessionStart regeneration)"
  - "Live global UserPromptSubmit curfew hook in ~/.claude/settings.json (absolute python3 nightguard_adapter.py, timeout 10) — fires in EVERY Claude Code session regardless of project dir"
  - "CURF-01 CLOSED: curfew now actually blocks Claude Code sessions"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Generator-owned global hook emit: load-merge-write-preserving-other-keys into user-scope settings.json, reusing the existing load()/write_atomic() primitives (no second writer)"
    - "Append-if-absent idempotency keyed on a command substring (nightguard_adapter.py) — two --apply runs yield exactly one entry; survives SessionStart regeneration"
    - "User-scope hook for un-dodgeable global reach (hook arrays concatenate across scopes; cannot be replaced by project scope) — T-071-09 mitigation"

key-files:
  created: []
  modified:
    - /home/danitrrga/dev/Projects/LifeOS/scripts/sync/sync_agent_config.py
    - ~/.claude/settings.json  # runtime, NOT git-tracked; produced by running the generator --apply

key-decisions:
  - "Q2 LOCKED resolution honored exactly: full-D-04 — taught the generator to emit the curfew hook into user-global ~/.claude/settings.json (a file the generator did not touch before). Rejected hand-wiring (would be wiped on SessionStart) and per-project wiring (dodgeable via an un-gated repo)."
  - "Idempotency keyed on the nightguard_adapter.py command substring rather than deep-equality of the entry, so a future timeout/path tweak still de-dups instead of stacking a second entry."
  - "Live-enforcement checkpoint (human-verify) auto-approved under AUTO_MODE: all three ROADMAP success criteria proven deterministically out-of-band via the guard NTP-override seams; live UserPromptSubmit firing was already empirically proven in Plan 01's probe."

requirements-completed: [CURF-01]

# Metrics
duration: ~10min
completed: 2026-06-23
---

# Phase 07.1 Plan 03: Global Curfew-Hook Wiring + End-to-End Enforcement Summary

**Taught the LifeOS generator (`sync_agent_config.py`) to idempotently emit a global `UserPromptSubmit` curfew hook into user-scope `~/.claude/settings.json` (the full-D-04 Q2 resolution), applied it to make the hook live in every Claude Code session, and verified all three ROADMAP success criteria end-to-end — closing CURF-01 so curfew now actually blocks sessions.**

## Performance

- **Duration:** ~10 min
- **Tasks:** 2 auto-tasks + 1 human-verify checkpoint (auto-approved with out-of-band proofs)
- **Files:** 1 modified (LifeOS generator, git-tracked); 1 runtime file written by the generator (`~/.claude/settings.json`, not git-tracked); 1 SUMMARY created (this repo)

## Accomplishments

- Added `write_curfew_hook()` to `sync_agent_config.py`, mirroring the `write_project_overrides()` load-merge-write-preserving-other-keys shape and reusing the existing `load()` / `write_atomic()` primitives (no hand-rolled second writer).
- The writer: defines `USER_SETTINGS = ~/.claude/settings.json` and the absolute `ADAPTER` path; tolerant-loads existing settings (preserving `enabledPlugins`/`extraKnownMarketplaces`/`theme`/`skipDangerousModePermissionPrompt`); builds the canonical entry `{"type":"command","command":"python3 <abs adapter>","timeout":10}`; `setdefault("hooks",{}).setdefault("UserPromptSubmit",[])` then **append-if-absent** keyed on the `nightguard_adapter.py` command substring; gated on `APPLY` (dry-run prints, `--apply` calls `write_atomic`).
- Wired `write_curfew_hook()` into `main()` under the standard APPLY/DRY-RUN print pattern.
- Applied the generator (`--apply` twice) to make the hook **live and idempotent**: `~/.claude/settings.json` `hooks.UserPromptSubmit` holds exactly one entry referencing the absolute `python3 .../nightguard_adapter.py` (timeout 10); all four pre-existing top-level keys survived the merge; one-time `.bak` created by `write_atomic`.
- Verified all three ROADMAP success criteria deterministically out-of-band via the guard's NTP-override test seams (deny/allow proven without waiting for the real clock).

## Task Commits

1. **Task 1: Add the idempotent curfew-hook writer to `sync_agent_config.py`** — `986ae0e` (LifeOS repo, branch `master` — generator code lives in LifeOS per the cross-repo rule). +41 lines, one file.
2. **Task 2: Apply the generator and assert idempotent, key-preserving global wiring** — **no git commit** (touches only the runtime `~/.claude/settings.json` + its one-time `.bak`, neither git-tracked; produced by running the generator `--apply`, governed by the LifeOS shared agent-config system).
3. **Checkpoint: Verify live curfew enforcement (3 success criteria)** — human-verify, **auto-approved** under AUTO_MODE. All three criteria proven out-of-band (see Verification); live `UserPromptSubmit` firing already empirically proven in Plan 01.

## Verification (all green)

**Generator static + dry-run (Task 1):** parses as valid Python; defines `write_curfew_hook`; references `UserPromptSubmit` + absolute `nightguard_adapter.py`; dry-run prints DRY-RUN and writes nothing (`settings.json` md5 unchanged, no `.bak` before `--apply`, curfew line reads "would write").

**Idempotent key-preserving wiring (Task 2):** after two `--apply` runs —
```
OK single idempotent absolute-path hook; existing keys preserved
```
- `hooks.UserPromptSubmit` -> exactly ONE entry referencing `nightguard_adapter.py` (no duplication)
- command = `python3 /home/danitrrga/dev/Projects/LifeOS/hooks/nightguard_adapter.py`, `timeout: 10`
- `enabledPlugins`, `extraKnownMarketplaces`, `theme`, `skipDangerousModePermissionPrompt` all present
- one-time `~/.claude/settings.json.bak` created (expected)

**End-to-end enforcement (checkpoint, out-of-band via NTP override):**

| Success criterion | Scenario | Result |
|---|---|---|
| SC#1 — curfew blocks | plain prompt, NTP forced to 23:30 (in curfew) | exit 2 + butler message "Mi señor, it is past 20:45 - the house is closed ... Jimmy will receive you again at 05:30." (ñ rendered, {start}/{end} substituted) |
| SC#1b — escape valve | `/shutdown` during curfew | exit 0 (passes; allowlisted leading token) |
| SC#1b — escape valve | `/plan tomorrow: finish the watchdog tests at 09:00` during curfew | exit 0 (passes; leading token `/plan`, free-form args ride along — D-12) |
| SC#2 — outside curfew allows | plain prompt, NTP forced to 12:00 (outside curfew) | exit 0, silent |
| SC#3 — reads canonical config, soft mode | all of the above ran the wired adapter against `NIGHTGUARD_DIR=.../LifeOS/nightguard` with no key | confirmed (deny reads butler text from `config.yaml`; never reads `.guardkey`) |

The live `UserPromptSubmit` hook firing in a real session was empirically proven in Plan 01 (probe captured the raw prompt field on Claude Code 2.1.186); the wired command is identical in shape to the proven probe entry, so a new session will run the adapter on every prompt.

## Deviations from Plan

None — plan executed exactly as written. The append-if-absent idempotency, key preservation, absolute-path command, and three success criteria all behaved as the plan and threat_model specified.

## Threat Model Coverage

- **T-071-09 (dodge via un-gated repo):** mitigated — hook lives in USER-scope `~/.claude/settings.json`; fires in every session, cannot be replaced by project scope.
- **T-071-10 (merge clobbers existing keys):** mitigated — `load`+`write_atomic` preserve-other-keys shape; Task 2 gate asserts all four pre-existing keys survive; one-time `.bak`.
- **T-071-11 (duplicate/stale entries on re-run):** mitigated — append-if-absent on the `nightguard_adapter.py` substring; two `--apply` runs yield exactly one entry.
- **T-071-12 (relative-path command fails to launch):** mitigated — command is the absolute `python3 /...nightguard_adapter.py`; Task 2 gate asserts the leading `/`.
- **T-071-13 (`disableAllHooks: true` turns off all hooks):** ACCEPTED residual (out-of-band config edit of the same character as disabling the watchdog) — flagged for a future hardening note, not this phase.

## Known Stubs

None. The hook is fully wired to the real generator, real adapter, real guard, and the canonical signed config.

## Threat Flags

None. No new security surface beyond the plan's `<threat_model>`. The only new write target is user-scope `~/.claude/settings.json` (already covered by T-071-10/11), via the existing atomic writer.

## Issues Encountered

- The live `UserPromptSubmit` hook can only be fired by an interactive human-submitted prompt, so the end-to-end criteria were proven deterministically out-of-band via the guard's `NIGHTGUARD_TEST_NTP_OVERRIDE` / `NIGHTGUARD_NTP_OVERRIDE_UNIX` seams (in-curfew deny + outside-curfew allow), combined with Plan 01's empirical proof that the event fires. Under AUTO_MODE the human-verify checkpoint auto-approved on that evidence.
- `~/.claude/settings.json` is regenerated at SessionStart by the LifeOS shared agent-config system; because the hook is now generator-owned (D-04), it is re-emitted on every regeneration and the wiring survives.

## Next Phase Readiness

- **CURF-01 is closed.** Curfew enforcement is global, generator-owned, idempotent, and un-dodgeable per-project; the `/plan` + `/shutdown` escape valve works during curfew.
- Deferred (tracked elsewhere): native-app kill during curfew -> Phase 8 (NBLK-01); optional SessionStart curfew notice; a hardening note for the accepted `disableAllHooks` residual (T-071-13).
- No blockers.

## Self-Check: PASSED
- FOUND: LifeOS `scripts/sync/sync_agent_config.py` contains `def write_curfew_hook`, `UserPromptSubmit`, `nightguard_adapter.py`
- FOUND: LifeOS commit `986ae0e`
- FOUND: `~/.claude/settings.json` `hooks.UserPromptSubmit` -> one absolute-path `python3 .../nightguard_adapter.py` entry (timeout 10); four pre-existing keys preserved
- VERIFIED: out-of-band deny (exit 2 + butler msg) / allow (exit 0) / escape valve (/shutdown + /plan exit 0)

---
*Phase: 07.1-curfew-hook*
*Completed: 2026-06-23*
