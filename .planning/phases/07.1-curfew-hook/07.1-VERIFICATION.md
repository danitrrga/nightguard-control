---
phase: 07.1-curfew-hook
verified: 2026-06-23T00:00:00Z
status: human_needed
score: 3/3 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Live interactive Claude Code prompt is blocked during curfew with the Spanish-butler message"
    expected: "Submitting a plain prompt (e.g. 'what time is it') inside a real Claude Code session during curfew hours (20:45-05:30) erases the prompt and displays the Mi señor butler message, exit 2 fires in the hook."
    why_human: "UserPromptSubmit can only be driven by an interactive human-submitted prompt; the NTP-override deterministic proof confirms the adapter and guard produce the correct outputs but cannot confirm the Claude Code hook contract fires end-to-end in a live session. Plan 01's empirical probe proved the event fires; the live wired command is identical in shape to the proven probe entry. Treat as optional confirmation, not a blocker."
---

# Phase 07.1: Curfew Hook Verification Report

**Phase Goal:** Make curfew actually enforce on Linux. guard.py already returns the correct verdict (deny during curfew, allow outside / in grace) but no Claude Code hook fires it. Wire the guard verdict into the Claude Code hook contract — the Linux equivalent of the retired Windows nightguard_adapter.ps1.
**Verified:** 2026-06-23
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | During curfew, a Claude Code session/prompt is blocked with the Spanish-butler deny message (guard.py exit 1 → hook exit 2) | VERIFIED | Behavioral spot-check: `printf '{"prompt":"hi"}' \| NIGHTGUARD_TEST_MODE=1 ... NIGHTGUARD_NTP_OVERRIDE_UNIX=$(date -d 'today 23:30' +%s) python3 nightguard_adapter.py` → stderr `"Mi señor, it is past 20:45 - the house is closed..."` + exit=2. Adapter code: guard_exit==1 → send_block(msg) → sys.exit(2). No sys.exit(guard_exit) path exists. |
| 2 | Outside curfew — and during an active daily grace window — sessions are allowed | VERIFIED | Behavioral spot-check: same adapter with `NIGHTGUARD_NTP_OVERRIDE_UNIX=$(date -d 'today 12:00' +%s)` → empty stderr + exit=0. Adapter code: guard_exit==0 AND verdict.decision=="allow" → sys.exit(0). |
| 3 | The hook reads the canonical LifeOS config/guard (no ~/.claude drift) and runs guard.py in user soft-mode (no key) | VERIFIED | CR-03 fix: `resolve_data_dir()` always returns `CANONICAL=/home/danitrrga/dev/Projects/LifeOS/nightguard` in production (env override only under NIGHTGUARD_TEST_MODE=1). Adapter never calls read_key() — confirmed by grep: zero hits for read_key/guardkey in the file. |

**Score:** 3/3 truths verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `/home/danitrrga/dev/Projects/LifeOS/hooks/nightguard_adapter.py` | Curfew adapter: guard exit 1 → exit 2 + butler stderr, leading-token allowlist, fail-closed | VERIFIED | 164 lines (>80 required). Parses as valid Python. Contains sys.exit(2), guard_exit==1, import ngcommon, subprocess.run. No sys.exit(guard_exit), no sys.exit(1). CR-02 fix present (verdict.decision=="allow" required for allow path). CR-03 fix present (NIGHTGUARD_TEST_MODE gate). LifeOS commit bb71e244. |
| `/home/danitrrga/dev/Projects/LifeOS/nightguard/config.yaml` | curfew.allow_commands contains /shutdown AND /plan (signed) | VERIFIED | `grep -A 10 allow_commands config.yaml` shows both `/shutdown` and `/plan` present. Signed commit reported in 07.1-02-SUMMARY.md (config_hmac=1a18fc24…, verify all-green). |
| `/home/danitrrga/dev/Projects/LifeOS/scripts/sync/sync_agent_config.py` | Generator with idempotent write_curfew_hook() | VERIFIED | `def write_curfew_hook` defined. References UserPromptSubmit and absolute nightguard_adapter.py path. WR-05 fix (exact command dedup). WR-06 fix (atomic OSError fallback). Called from main(). LifeOS commit 986ae0e. |
| `~/.claude/settings.json` | Live global UserPromptSubmit curfew hook | VERIFIED | Confirmed on disk: hooks.UserPromptSubmit[0].hooks[0] = {type:"command", command:"python3 /home/danitrrga/dev/Projects/LifeOS/hooks/nightguard_adapter.py", timeout:10}. Exactly one entry. All four pre-existing keys preserved (enabledPlugins, extraKnownMarketplaces, theme, skipDangerousModePermissionPrompt). |
| `.planning/phases/07.1-curfew-hook/07.1-MATCHER-DECISION.md` | Recorded matcher decision (MATCHER: leading-token) | VERIFIED | File exists. Contains `MATCHER: leading-token` on its own line (grep -E confirms the format). Four captured PROMPT_FIELD values document the live probe evidence. |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `~/.claude/settings.json UserPromptSubmit` | `nightguard_adapter.py` | `command: python3 <abs path>` | WIRED | Confirmed on disk: exact absolute path python3 /home/danitrrga/dev/Projects/LifeOS/hooks/nightguard_adapter.py |
| `nightguard_adapter.py` | `guard.py` | `subprocess.run([sys.executable, guard_py], env={NIGHTGUARD_DIR})` | WIRED | subprocess.run present and wired to os.path.join(ROOT, "scripts", "nightguard", "guard.py"); env["NIGHTGUARD_DIR"] = data_dir passed |
| `nightguard_adapter.py` | `ngcommon.py` | `sys.path.insert + import ngcommon` | WIRED | sys.path.insert(0, os.path.join(ROOT, "scripts", "nightguard")) then `import ngcommon as ng` present |
| `guard exit 1` | `adapter exit 2` | `explicit translation (guard_exit == 1 → send_block → sys.exit(2))` | WIRED | guard_exit == 1 block present. No sys.exit(guard_exit) path. sys.exit(1) absent. |
| `sync_agent_config.py write_curfew_hook()` | `~/.claude/settings.json` | `load then setdefault(hooks).setdefault(UserPromptSubmit) then append-if-absent then write_atomic` | WIRED | All logic confirmed in code. write_curfew_hook() called from main(). Dry-run leaves file unchanged (md5 confirmed). |

---

## Data-Flow Trace (Level 4)

The adapter renders dynamic data (butler messages) from config.yaml and the guard verdict JSON.

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `nightguard_adapter.py` | `cfg` (butler strings) | `ng.yaml_load(open(config_path).read())` → curfew section | YES — reads live config.yaml from canonical dir | FLOWING |
| `nightguard_adapter.py` | `verdict` (decision/reason) | `subprocess.run([guard_py], capture_output=True)` → last stdout line JSON | YES — real guard subprocess invocation; spot-checks confirm real deny/allow output | FLOWING |
| `nightguard_adapter.py` | `prompt` | `json.loads(sys.stdin.read()).get("prompt")` | YES — reads from Claude Code hook's stdin payload | FLOWING |

---

## Behavioral Spot-Checks

| Behavior | Command (abbreviated) | Result | Status |
|----------|----------------------|--------|--------|
| SC#1: In-curfew plain prompt blocked | `printf '{"prompt":"hi"}' \| NIGHTGUARD_TEST_MODE=1 ... NTP=23:30 python3 nightguard_adapter.py` | stderr: "Mi señor, it is past 20:45..." + exit=2 | PASS |
| SC#2: Outside-curfew plain prompt allowed | Same with NTP=12:00 | empty stderr + exit=0 | PASS |
| SC#3: /shutdown allowlisted during curfew | `{"prompt":"/shutdown"}` with NTP=23:30 | exit=0 (no guard call) | PASS |
| SC#4: /plan with free-form args (D-12) | `{"prompt":"/plan tomorrow: finish tests at 09:00"}` with NTP=23:30 | exit=0 | PASS |
| SC#5: Substring bypass closed (leading-token) | `{"prompt":"please run /shutdown later"}` with NTP=23:30 | exit=2 (blocked) | PASS |
| SC#6: Fail-closed on missing config | `NIGHTGUARD_TEST_MODE=1 NIGHTGUARD_DIR=/tmp/nonexistent` | stderr: "Nightguard is unavailable..." + exit=2 | PASS |
| SC#7 (idempotency): Generator dry-run | `python3 sync_agent_config.py` (no --apply) | "already present (no change)", file md5 unchanged | PASS |

---

## Probe Execution

No formal probe scripts (scripts/*/tests/probe-*.sh) declared or present for this phase. Behavioral spot-checks above serve as the automated evidence. The plan's `<verification>` sections used inline `python3 -c` assertions and NTP-override shell commands as their verification gates.

---

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| CURF-01 | 07.1-01, 07.1-02, 07.1-03 | guard.py wired as Claude Code curfew hook; deny blocked with butler message; outside curfew/grace allowed; reads canonical config in soft mode | SATISFIED | REQUIREMENTS.md marks `[x]` CURF-01 as Complete for Phase 7.1. All three success criteria proven by behavioral spot-checks and adapter code review. LifeOS commits bb71e244 + 986ae0e + 3b470b9. |

No orphaned requirements: CURF-01 is the sole requirement mapped to this phase in REQUIREMENTS.md and all three plans claim it.

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| No TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER markers found in any modified file | | | | |

Zero debt markers. No stub patterns. No hardcoded empty data on any rendering path.

---

## Code Review Findings (07.1-REVIEW.md)

Three critical issues (CR-01, CR-02, CR-03) were identified in the code review pass. Their resolution status against the actual code on disk:

| Finding | Severity | Resolution | Evidence in Code |
|---------|----------|------------|------------------|
| CR-01: Allowlist passthrough with free-form args is an open channel during curfew | Critical | ACCEPTED BY DESIGN | D-11/D-12 — free-form /plan escape valve is the locked design decision; documented in CONTEXT.md and REVIEW.md. |
| CR-02: ALLOW decided from guard exit code alone (no verdict.decision check) | Critical | FIXED (bb71e244) | Line 139: `if verdict.get("decision") == "allow": sys.exit(0)` then `send_block(...)`. |
| CR-03: NIGHTGUARD_DIR env-injectable (same-user redirect bypass) | Critical | FIXED (bb71e244) | Lines 64-69: `resolve_data_dir()` — CANONICAL pinned in production; env override only under NIGHTGUARD_TEST_MODE=1. |
| WR-05: Substring dedup lets relocated adapter path accumulate stale hook | Warning | FIXED (bb71e244) | Line 219: `any(c == entry["command"] for c in existing)` — exact full-command dedup + prune stale paths. |
| WR-06: OSError fallback writes non-atomically, can truncate settings.json | Warning | FIXED (bb71e244) | Lines 69-92: retry loop with second temp + raises on failure. |
| WR-01..WR-04, IN-01..IN-04 | Warning/Info | DEFERRED | Not required for the phase goal; candidates for future polish. |

---

## Human Verification Required

### 1. Live Interactive Claude Code Session Enforcement (Optional Confirmation)

**Test:** Start a new Claude Code session. During curfew hours (20:45-05:30), submit a plain prompt such as "what time is it". Then submit "/shutdown" and "/plan tomorrow: finish the watchdog tests at 09:00".
**Expected:**
- The plain prompt is erased and the Spanish-butler message appears: "Mi señor, it is past 20:45 - the house is closed for the night..."
- "/shutdown" and "/plan ..." pass through without being erased.
**Why human:** UserPromptSubmit can only be fired by an interactive human-submitted prompt; it cannot be driven programmatically. The out-of-band NTP-override spot-checks above confirm the adapter + guard produce the correct exit codes and stderr output. Plan 01's live probe empirically proved UserPromptSubmit fires and exposes the raw prompt text on Claude Code 2.1.186. The wired hook entry is structurally identical to the proven probe entry. This human test is a belt-and-suspenders confirmation rather than a blocker.

---

## Gaps Summary

No gaps found. All three phase success criteria are satisfied by verified codebase evidence:

1. **SC#1 (curfew blocks):** Adapter translates guard exit 1 → hook exit 2 + butler message. Proven by behavioral spot-check (in-curfew NTP override, exit=2, correct Spanish-butler stderr). No sys.exit(guard_exit) or sys.exit(1) path exists. CR-02 defense-in-depth (verdict.decision check) added and confirmed on line 139.

2. **SC#2 (outside curfew/grace allows):** Adapter exits 0 silently on guard exit 0 + verdict.decision=="allow". Proven by behavioral spot-check (outside-curfew NTP override, exit=0, empty stderr). /shutdown and /plan pass during curfew (leading-token allowlist confirmed).

3. **SC#3 (canonical config, soft mode):** CR-03 fix pins CANONICAL in production. Adapter reads config.yaml from /home/danitrrga/dev/Projects/LifeOS/nightguard (the root watchdog's protected dir). Never reads the signing key. All confirmed by code review and grep.

**Live wiring confirmed:** ~/.claude/settings.json contains exactly one UserPromptSubmit hook entry pointing to the absolute python3 path of nightguard_adapter.py with timeout 10.

**Generator ownership confirmed:** sync_agent_config.py write_curfew_hook() is idempotent (dry-run shows "already present (no change)"), keyed on exact command match, and self-heals stale relocated paths.

**CURF-01:** Marked [x] Complete in REQUIREMENTS.md. All three plans claim it. Traceability is complete.

---

_Verified: 2026-06-23_
_Verifier: Claude (gsd-verifier)_
