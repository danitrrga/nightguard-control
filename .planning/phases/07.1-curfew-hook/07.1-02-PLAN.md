---
phase: 07.1-curfew-hook
plan: 02
type: execute
wave: 1
depends_on: ["07.1-01"]
files_modified:
  - /home/danitrrga/dev/Projects/LifeOS/hooks/nightguard_adapter.py
  - /home/danitrrga/dev/Projects/LifeOS/nightguard/config.yaml
autonomous: false
requirements: [CURF-01]
user_setup: []

must_haves:
  truths:
    - "During curfew, running the adapter (no allowlisted prompt) exits 2 and writes the Spanish-butler curfew message to stderr"
    - "Outside curfew, running the adapter exits 0 and emits nothing"
    - "A prompt whose allowlist match succeeds exits 0 without invoking the guard"
    - "Any adapter error/exception (unresolvable dir, guard crash, non-{0,1} exit, unparseable verdict) exits 2 (fail-closed), never 0 or 1"
    - "config.yaml curfew.allow_commands contains both /shutdown and /plan after a signed control-CLI commit, and `verify` passes"
  artifacts:
    - path: "/home/danitrrga/dev/Projects/LifeOS/hooks/nightguard_adapter.py"
      provides: "The Linux UserPromptSubmit curfew adapter (verdict→exit-2 translation + allowlist + butler messages)"
      min_lines: 80
      contains: "sys.exit(2)"
    - path: "/home/danitrrga/dev/Projects/LifeOS/nightguard/config.yaml"
      provides: "curfew.allow_commands with /plan added (signed)"
      contains: "/plan"
  key_links:
    - from: "/home/danitrrga/dev/Projects/LifeOS/hooks/nightguard_adapter.py"
      to: "/home/danitrrga/dev/Projects/LifeOS/scripts/nightguard/guard.py"
      via: "subprocess.run([sys.executable, guard_py], env={NIGHTGUARD_DIR})"
      pattern: "subprocess\\.run"
    - from: "/home/danitrrga/dev/Projects/LifeOS/hooks/nightguard_adapter.py"
      to: "/home/danitrrga/dev/Projects/LifeOS/scripts/nightguard/ngcommon.py"
      via: "sys.path.insert + import ngcommon (yaml_load)"
      pattern: "import ngcommon"
    - from: "guard exit 1"
      to: "adapter exit 2"
      via: "explicit exit-code translation (NEVER propagate)"
      pattern: "guard_exit == 1"
---

<objective>
Build the Linux curfew adapter (`nightguard_adapter.py`) — a near-1:1 Python port of the retired
Windows `nightguard_adapter.ps1` — and add `/plan` to the canonical `config.yaml`
`curfew.allow_commands` via the SIGNED control-CLI path. This closes CURF-01's core: a `deny`
verdict from `guard.py` now translates into a Claude Code hard-block (exit 2 + butler message),
and the `/plan`/`/shutdown` escape valve is configured.

Purpose: The verdict engine already returns the correct curfew/grace verdict; this is the missing
translator that turns it into an enforceable hook contract. Fail-closed is load-bearing: every
error path blocks (exit 2), never allows.

Output: `LifeOS/hooks/nightguard_adapter.py` (git-tracked) + a signed `config.yaml` with `/plan`
in the allowlist. Plan 03 wires this adapter into the global hook.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-curfew-hook/07.1-RESEARCH.md
@.planning/phases/07.1-curfew-hook/07.1-PATTERNS.md
@.planning/phases/07.1-curfew-hook/07.1-CONTEXT.md
@.planning/phases/07.1-curfew-hook/07.1-MATCHER-DECISION.md

<interfaces>
<!-- Contracts the adapter consumes. Read the analogs in <read_first>; do not re-explore. -->

guard.py (subprocess oracle — DO NOT modify):
  invoked: python3 /home/danitrrga/dev/Projects/LifeOS/scripts/nightguard/guard.py
  env: NIGHTGUARD_DIR must point at the canonical instance dir
  stdout: one JSON line {"decision","reason","grace_remaining_secs",...}
  exit: 0 = allow (outside curfew / grace active) ; 1 = deny ; (anything else = crash)
  deny reasons seen: "curfew", "clock tamper", "ntp unreachable"
  soft mode: read_key()==None → no key needed, no audit written

ngcommon.py (reused parser — DO NOT modify):
  /home/danitrrga/dev/Projects/LifeOS/scripts/nightguard/ngcommon.py
  ngcommon.yaml_load(text) -> dict   (the SAME parser guard.py + ctl use)
  NIGHTGUARD_DIR default (ngcommon line 20) = os.path.join(ROOT, "nightguard")

config.yaml curfew block (the single source of truth for surfaced text, D-10):
  curfew.start / curfew.end                    ("20:45" / "05:30")
  curfew.message    (uses {start}/{end} placeholders)   → reason "curfew"
  curfew.tamper_message                                 → reason "clock tamper"
  curfew.offline_message  (fail-closed default)         → "ntp unreachable" / any other
  curfew.allow_commands  (list; currently ["/shutdown"]; D-12 adds "/plan")

control-CLI signed commit (the ONLY sanctioned writer — config.yaml hand-edits revert ≤60s):
  sudo python3 /home/danitrrga/dev/Projects/LifeOS/scripts/nightguard/nightguard_ctl.py commit --from <proposed.yaml>
  python3 /home/danitrrga/dev/Projects/LifeOS/scripts/nightguard/nightguard_ctl.py verify
  field-table (nightguard_ctl.py line 46): curfew.allow_commands is "list_add" → adding an entry is FREE (costs_token=False, no weekly token spent)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port nightguard_adapter.py (verdict→exit-2 + allowlist + butler messages, fail-closed)</name>
  <read_first>
    - /home/danitrrga/dev/Projects/LifeOS/hooks/nightguard_adapter.py (the file being created — confirm it does not exist yet)
    - /home/danitrrga/dev/Projects/LifeOS/.agents/hooks/nightguard_adapter.ps1 (the contract to port 1:1 — exit mapping, Send-Block, Read-AdapterConfig, allowlist, reason switch, terminal defensive block)
    - /home/danitrrga/dev/Projects/LifeOS/hooks/guard_tool_use.py (shape analog: shebang+docstring lines 1-22, ROOT-from-__file__ line 24, stdin read+json.loads lines ~105-111, deny()/exit-2 lines 84-86, log_alert lines 89-99)
    - /home/danitrrga/dev/Projects/LifeOS/scripts/nightguard/ngcommon.py (yaml_load + NIGHTGUARD_DIR default line 20, read_key soft-mode)
    - /home/danitrrga/dev/Projects/LifeOS/scripts/nightguard/guard.py (verdict JSON shape + exit codes — read enough to confirm stdout/exit contract; DO NOT modify)
    - .planning/phases/07.1-curfew-hook/07.1-PATTERNS.md (the per-stanza port map, lines 21-145 — copy these shapes verbatim)
    - .planning/phases/07.1-curfew-hook/07.1-MATCHER-DECISION.md (the MATCHER: line from Plan 01 — substring or leading-token)
  </read_first>
  <behavior>
    - Outside curfew (guard exit 0): adapter exits 0, emits nothing on stderr.
    - In curfew, reason "curfew" (guard exit 1): adapter exits 2, stderr = curfew.message with {start}/{end} substituted.
    - In curfew, reason "clock tamper" (guard exit 1): adapter exits 2, stderr = curfew.tamper_message.
    - In curfew, reason "ntp unreachable" or any other deny reason (guard exit 1): adapter exits 2, stderr = curfew.offline_message.
    - Prompt matches an allow_commands entry (per the MATCHER decision): adapter exits 0 WITHOUT invoking guard.
    - Fail-closed: unresolvable NIGHTGUARD_DIR / unreadable config / guard non-{0,1} exit / unparseable verdict JSON / any uncaught exception → exit 2 with offline_message (or hardcoded FAIL_CLOSED fallback if config unreadable). NEVER exit 0 and NEVER exit 1 on error.
    - The ñ/accented butler text renders (UTF-8 stderr).
  </behavior>
  <action>
    Create `/home/danitrrga/dev/Projects/LifeOS/hooks/nightguard_adapter.py` as a stdlib-only
    (`json`, `os`, `sys`, `subprocess`) Python port following 07.1-PATTERNS.md stanza-by-stanza:
    (1) shebang + docstring + imports; (2) `ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))`
    then `sys.path.insert(0, os.path.join(ROOT, "scripts", "nightguard"))` and `import ngcommon as ng`;
    (3) `CANONICAL = "/home/danitrrga/dev/Projects/LifeOS/nightguard"`, `data_dir = os.environ.get("NIGHTGUARD_DIR") or CANONICAL`;
    (4) `FAIL_CLOSED = "Nightguard is unavailable. For your own good, the house remains closed."`
    and `send_block(msg, fallback=FAIL_CLOSED)` that writes `(msg or fallback)+"\n"` to stderr and
    `sys.exit(2)`; (5) read+`json.loads` stdin → `prompt` (unparseable stdin is NOT allow — fall
    through to guard, fail-closed downstream, per PATTERNS lines 45-58); (6) load config via
    `ng.yaml_load(open(config_path, encoding="utf-8").read())` → `cfg = (...).get("curfew") or {}`,
    `fallback = cfg.get("offline_message") or FAIL_CLOSED`; (7) allowlist check BEFORE the guard —
    use the matcher from 07.1-MATCHER-DECISION.md: substring (`if cmd and cmd in prompt: sys.exit(0)`)
    OR leading-token (`if prompt.split() and prompt.split()[0] in (cfg.get("allow_commands") or []): sys.exit(0)`),
    iterating `cfg.get("allow_commands") or []`; (8) subprocess `guard.py` with
    `env = dict(os.environ); env["NIGHTGUARD_DIR"] = data_dir`,
    `guard_py = os.path.join(ROOT, "scripts", "nightguard", "guard.py")`,
    `subprocess.run([sys.executable, guard_py], capture_output=True, text=True, env=env, timeout=8)`,
    capture `returncode`, parse last stdout line as the verdict JSON (unparseable → `{}`);
    (9) translate: `guard_exit == 0 → sys.exit(0)`; `guard_exit == 1 →` reason switch
    (curfew→message with {start}/{end} sub, "clock tamper"→tamper_message, else→offline_message)
    then `send_block(msg, fallback)`; (10) after the switch, a `send_block(cfg.get("offline_message"), fallback)`
    fall-through for any non-{0,1} exit; (11) `try: sys.stderr.reconfigure(encoding="utf-8")
    except Exception: pass` for the ñ; (12) `if __name__ == "__main__":` wrap `main()` in
    `try/except Exception: send_block(None)` then a TERMINAL `send_block(None)` defensive
    fall-through (port ps1 line 193). CRITICAL invariant (D-07): NEVER `sys.exit(guard_exit)`,
    NEVER exit 1 on any path. Also write a small companion test/harness file or use the verify
    command below to prove the four behaviors (the guard exposes test seams
    `NIGHTGUARD_TEST_NTP_OVERRIDE`/`NIGHTGUARD_NTP_OVERRIDE_UNIX` to force in-curfew vs outside).
  </action>
  <verify>
    <automated>cd /home/danitrrga/dev/Projects/LifeOS && python3 -c "import ast,sys; src=open('hooks/nightguard_adapter.py').read(); ast.parse(src); assert 'sys.exit(guard_exit)' not in src, 'FAIL-OPEN: propagates guard exit'; b=[l for l in src.splitlines() if l.strip().startswith('#') is False]; assert 'guard_exit == 1' in src and 'sys.exit(2)' in src; assert 'import ngcommon' in src and 'subprocess.run' in src; print('OK adapter structure + no exit-propagation')" && printf '{"prompt":"hello"}' | NIGHTGUARD_DIR=/home/danitrrga/dev/Projects/LifeOS/nightguard NIGHTGUARD_TEST_NTP_OVERRIDE=1 NIGHTGUARD_NTP_OVERRIDE_UNIX=$(date -d 'today 23:30' +%s) python3 hooks/nightguard_adapter.py; ec=$?; echo "in-curfew exit=$ec (expect 2)"; test $ec -eq 2</automated>
  </verify>
  <acceptance_criteria>
    - `nightguard_adapter.py` parses as valid Python, stdlib-only imports, `import ngcommon` present
    - Source contains NO `sys.exit(guard_exit)` and NO path that can exit 1 (assert via grep: no `sys.exit(1)`, no bare `exit(guard_exit)`)
    - Running with a forced-in-curfew NTP override and a plain prompt exits 2 and writes a non-empty stderr message
    - Running with a forced-outside-curfew NTP override and a plain prompt exits 0 with empty stderr
    - Running with `{"prompt":"/shutdown"}` (an allowlisted command) exits 0 without invoking the guard
    - Running with an unset/garbage NIGHTGUARD_DIR pointing at a missing config exits 2 (fail-closed), never 0/1
  </acceptance_criteria>
  <done>The adapter translates guard verdicts into the Claude Code block contract, honors the allowlist, and fails closed on every error branch.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Sign /plan into config.yaml via sudo control-CLI</name>
  <action>PAUSE for the human: only a human can supply the sudo password the root-signer requires. Stage the proposed config.yaml (with /plan added), then have the human run the commit + verify per <how-to-verify>. Resume on verify-green.</action>
  <what-built>The adapter is built and unit-verified. Adding `/plan` to `config.yaml`
  `curfew.allow_commands` is a SIGNED config change — the root watchdog reverts unsigned edits
  within ≤60s, so it must go through `sudo`→control-CLI, which prompts for the sudo password
  (deliberate anti-impulse friction; only a human can supply it). This is a FREE change (list_add
  is tighten/neutral — no weekly token spent).</what-built>
  <how-to-verify>
    Claude has staged a proposed `config.yaml` (a copy of the canonical config with `/plan` added to
    `curfew.allow_commands`, comments/format preserved). To commit it under the root key, run — and
    enter your sudo password when prompted:
    1. `sudo python3 /home/danitrrga/dev/Projects/LifeOS/scripts/nightguard/nightguard_ctl.py commit --from <staged-proposed.yaml>`
       (Claude will give you the exact staged path.) Confirm the output reports a FREE / no-token
       commit (direction tighten/neutral), NOT a token spend.
    2. Verify the signature holds:
       `python3 /home/danitrrga/dev/Projects/LifeOS/scripts/nightguard/nightguard_ctl.py verify`
       → expect config.yaml hmac == guard.json (verify passes).
    3. Confirm `/plan` is now live and signed:
       `grep -A3 allow_commands /home/danitrrga/dev/Projects/LifeOS/nightguard/config.yaml`
       → should list both `/shutdown` and `/plan`.
    Report the commit output (free vs token) and the verify result.
  </how-to-verify>
  <resume-signal>Paste the commit output + verify result, or type "committed, /plan live, verify green"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code runtime → adapter | untrusted `prompt` text arrives on stdin; treated as opaque (allowlist membership only, never eval/shell-interpolation) |
| adapter → guard.py subprocess | adapter sets NIGHTGUARD_DIR and trusts guard's exit code + JSON verdict (black-box oracle) |
| user → config.yaml | only the root-key control-CLI may produce a signed config; user-space hand-edits revert ≤60s |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-071-04 | Tampering/Bypass (FAIL-OPEN — cardinal) | adapter exit-code path | mitigate | Explicit translation guard exit 1→2; NO `sys.exit(guard_exit)`; verify gate greps for propagation; outside-curfew=0 / in-curfew=2 asserted |
| T-071-05 | Tampering/Bypass | adapter error/exception branches | mitigate | Top-level try/except→send_block(exit 2) + terminal defensive send_block; non-{0,1} guard exit→exit 2; unparseable verdict→offline_message exit 2 |
| T-071-06 | Tampering | hand-edit config.yaml to add a bypass command | mitigate | `/plan` added ONLY via signed `sudo`→control-CLI commit; verify confirms config_hmac==guard.json; root watchdog reverts any unsigned edit ≤60s |
| T-071-07 | Bypass | substring-allowlist abuse (embed `/shutdown` mid-prompt) | accept | Known v1 tradeoff (Windows adapter shipped with substring); if Plan 01 probe yielded leading-token, the safer matcher is used instead |
| T-071-08 | Information disclosure | adapter reads the signing key | mitigate | Soft mode — adapter never calls read_key(); no key material crosses the hook boundary (integrity stays the root watchdog's job) |
</threat_model>

<verification>
- `nightguard_adapter.py` exists, is stdlib-only, and contains no exit-code-propagation path.
- Forced in-curfew → exit 2 + butler stderr; forced outside-curfew → exit 0 silent; allowlisted prompt → exit 0 (no guard call); missing-config → exit 2.
- `config.yaml curfew.allow_commands` lists `/shutdown` AND `/plan`; `nightguard_ctl.py verify` passes (signed, watchdog-stable).
</verification>

<success_criteria>
- CURF-01 core is built: the deny verdict translates to a hard-block, the allow/grace verdict passes, the escape valve is configured.
- Every error/exception branch in the adapter exits 2 (fail-closed), proven by the verify gate.
- The `/plan` allowlist entry is live AND signed (survives the root watchdog).
</success_criteria>

<output>
Create `.planning/phases/07.1-curfew-hook/07.1-02-SUMMARY.md` when done.
Note the absolute adapter path and the confirmed verify-green state for Plan 03.
</output>
