---
phase: 07.1-curfew-hook
plan: 03
type: execute
wave: 2
depends_on: ["07.1-02"]
files_modified:
  - /home/danitrrga/dev/Projects/LifeOS/scripts/sync/sync_agent_config.py
  - ~/.claude/settings.json
autonomous: false
requirements: [CURF-01]
user_setup: []

must_haves:
  truths:
    - "The generator (sync_agent_config.py --apply) idempotently merges a curfew UserPromptSubmit hook into ~/.claude/settings.json"
    - "Re-running the generator does NOT duplicate the hook entry"
    - "The merge preserves enabledPlugins/theme/extraKnownMarketplaces/skipDangerousModePermissionPrompt"
    - "The wired hook command is an absolute python3 path to nightguard_adapter.py"
    - "During curfew a real Claude Code prompt is blocked with the butler message; outside curfew (and in grace) prompts pass"
  artifacts:
    - path: "/home/danitrrga/dev/Projects/LifeOS/scripts/sync/sync_agent_config.py"
      provides: "Net-new idempotent writer that emits the curfew hook into user-global settings"
      contains: "UserPromptSubmit"
    - path: "~/.claude/settings.json"
      provides: "Live global UserPromptSubmit curfew hook"
      contains: "nightguard_adapter.py"
  key_links:
    - from: "/home/danitrrga/dev/Projects/LifeOS/scripts/sync/sync_agent_config.py"
      to: "~/.claude/settings.json"
      via: "load then setdefault(hooks).setdefault(UserPromptSubmit) then append-if-absent then write_atomic"
      pattern: "settings\\.json"
    - from: "~/.claude/settings.json UserPromptSubmit hook"
      to: "/home/danitrrga/dev/Projects/LifeOS/hooks/nightguard_adapter.py"
      via: "command: python3 <abs path>"
      pattern: "nightguard_adapter\\.py"
---

<objective>
Wire the curfew adapter globally via the LOCKED Q2 resolution — "teach the generator (full
D-04)". Add net-new idempotent code to `sync_agent_config.py` that merges a `UserPromptSubmit`
curfew hook into user-global `~/.claude/settings.json` (preserving all existing keys, never
duplicating on re-run), then run it `--apply` to make the hook live. Finally, verify the three
ROADMAP success criteria end-to-end: curfew blocks, outside-curfew/grace allows, and the hook
reads the canonical config in soft mode.

Purpose: `~/.claude/settings.json` (user scope) is read for EVERY Claude Code session regardless
of project dir, and hook arrays concatenate across scopes — so a curfew hook there cannot be
dodged by opening an un-gated repo (D-03). Emitting it from the generator makes it reproducible
on a fresh machine (D-04) and the adapter script + emitted hook JSON stay git-tracked in LifeOS.

Output: a generator that owns the curfew hook + a live global hook + verified enforcement. This
closes CURF-01.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-curfew-hook/07.1-RESEARCH.md
@.planning/phases/07.1-curfew-hook/07.1-PATTERNS.md
@.planning/phases/07.1-curfew-hook/07.1-CONTEXT.md

<interfaces>
<!-- Generator machinery to REUSE verbatim (do NOT hand-roll a second writer). -->

sync_agent_config.py (the file being extended):
  ROOT  = LifeOS root (line 26)            HOME = os.path.expanduser("~") (line 27)
  APPLY = "--apply" in sys.argv (line 29)  -> DRY-RUN by default
  load(p, default=None)  (lines 38-44)     -> tolerant read, returns {} on missing, never throws
  write_atomic(path, data) (lines 47-67)   -> tmp->validate->os.replace, one-time .bak, ensure_ascii=False
  write_project_overrides() (lines 106-150) -> the load-merge-write-preserving-other-keys SHAPE to mirror:
        sl = load(slp); sl["<managed key>"] = ...; if APPLY: write_atomic(slp, sl)
  main() (line 151+)                        -> call the new writer here (under the APPLY/DRY-RUN print pattern)

Target file (global reach, D-03/D-04):
  ~/.claude/settings.json
  current keys (preserve ALL): enabledPlugins, extraKnownMarketplaces, theme, skipDangerousModePermissionPrompt
  has NO hooks block today -> the writer creates it

The hook JSON entry to emit (schema from RESEARCH; timeout in SECONDS; absolute python3 path):
  {"type": "command",
   "command": "python3 /home/danitrrga/dev/Projects/LifeOS/hooks/nightguard_adapter.py",
   "timeout": 10}
  nested under: settings["hooks"]["UserPromptSubmit"] = [ {"hooks": [ <the entry above> ]} ]
  (UserPromptSubmit has no matcher -- one group whose inner "hooks" list holds the command.)

guard.py test seams (for the end-to-end verification task):
  NIGHTGUARD_TEST_NTP_OVERRIDE=1 + NIGHTGUARD_NTP_OVERRIDE_UNIX=<unix secs> -> force a true-time instant
  (use an in-curfew time and an outside-curfew time to drive deny/allow without waiting for the clock)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the idempotent curfew-hook writer to sync_agent_config.py</name>
  <read_first>
    - /home/danitrrga/dev/Projects/LifeOS/scripts/sync/sync_agent_config.py (the file being modified -- read load() 38-44, write_atomic() 47-67, write_project_overrides() 106-150, main() 151+)
    - ~/.claude/settings.json (the target -- confirm current keys + no hooks block)
    - .planning/phases/07.1-curfew-hook/07.1-PATTERNS.md (the generator stanza, lines 149-192 -- the exact merge shape + hook JSON)
    - .planning/phases/07.1-curfew-hook/07.1-RESEARCH.md (Q2 LOCKED to full-D-04; array-concatenation semantics lines 31-39, 294-297, 387-403)
    - /home/danitrrga/dev/Projects/nightguard-control/.claude/AGENT-SYSTEM.md (Linux interpreter rule: .py->python3, ABSOLUTE paths only; generated-files caveat)
  </read_first>
  <action>
    Add a new function `write_curfew_hook()` to `sync_agent_config.py` (mirror the
    `write_project_overrides()` load-merge-write shape, lines 106-150). It must: (1) define
    `USER_SETTINGS = os.path.join(HOME, ".claude", "settings.json")` and
    `ADAPTER = "/home/danitrrga/dev/Projects/LifeOS/hooks/nightguard_adapter.py"`; (2)
    `s = load(USER_SETTINGS)` (tolerant read -- preserves enabledPlugins/theme/etc.); (3) build the
    canonical entry `{"type":"command","command":"python3 "+ADAPTER,"timeout":10}`; (4) idempotent
    merge: `groups = s.setdefault("hooks", {}).setdefault("UserPromptSubmit", [])`; scan every
    inner `hooks` command string already present; ONLY append a new group `{"hooks":[entry]}` if no
    existing command already references `nightguard_adapter.py` (append-if-absent -- a re-run must
    NOT duplicate); (5) gate on `APPLY`: in DRY-RUN print the would-write summary, under `--apply`
    call `write_atomic(USER_SETTINGS, s)`; return a one-line summary string. Then CALL
    `write_curfew_hook()` from `main()` under the same print pattern as the other writers. Use the
    existing `write_atomic`/`load` (do NOT hand-roll a second writer). The command MUST be the
    absolute `python3 /home/danitrrga/.../nightguard_adapter.py` (Linux interpreter rule -- relative
    paths broke historically; RESEARCH line 427-430). This code + the resulting hook JSON are
    git-tracked in LifeOS.
  </action>
  <verify>
    <automated>cd /home/danitrrga/dev/Projects/LifeOS && python3 -c "import ast; ast.parse(open('scripts/sync/sync_agent_config.py').read())" && grep -q "def write_curfew_hook" scripts/sync/sync_agent_config.py && grep -q "UserPromptSubmit" scripts/sync/sync_agent_config.py && grep -q "nightguard_adapter.py" scripts/sync/sync_agent_config.py && python3 scripts/sync/sync_agent_config.py 2>&1 | grep -qi "DRY-RUN" && echo "OK writer added, dry-run clean (no file written)"</automated>
  </verify>
  <acceptance_criteria>
    - `sync_agent_config.py` parses as valid Python and defines `write_curfew_hook`
    - The function references `UserPromptSubmit` and the absolute `nightguard_adapter.py` python3 command
    - Running the generator with NO `--apply` prints DRY-RUN and writes nothing (settings.json unchanged)
    - The append-if-absent logic keys on the `nightguard_adapter.py` command substring (idempotent across re-runs)
  </acceptance_criteria>
  <done>The generator owns a net-new idempotent writer for the global curfew hook; dry-run is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the generator and assert idempotent, key-preserving global wiring</name>
  <read_first>
    - /home/danitrrga/dev/Projects/LifeOS/scripts/sync/sync_agent_config.py (the writer just added)
    - ~/.claude/settings.json (the file about to be written -- record its current keys first)
  </read_first>
  <action>
    Run `python3 /home/danitrrga/dev/Projects/LifeOS/scripts/sync/sync_agent_config.py --apply`
    ONCE to write the curfew hook into `~/.claude/settings.json`. Then run it `--apply` a SECOND
    time and assert the `UserPromptSubmit` array did NOT grow (idempotent -- exactly one entry
    referencing `nightguard_adapter.py`). Assert all four pre-existing top-level keys
    (enabledPlugins, extraKnownMarketplaces, theme, skipDangerousModePermissionPrompt) survive the
    merge. Confirm the wired command is the absolute `python3 .../nightguard_adapter.py` with
    `timeout: 10`. (The generator's `write_atomic` leaves a one-time `~/.claude/settings.json.bak`
    -- that is expected.)
  </action>
  <verify>
    <automated>python3 /home/danitrrga/dev/Projects/LifeOS/scripts/sync/sync_agent_config.py --apply >/dev/null 2>&1 && python3 /home/danitrrga/dev/Projects/LifeOS/scripts/sync/sync_agent_config.py --apply >/dev/null 2>&1 && python3 -c "import json,os; d=json.load(open(os.path.expanduser('~/.claude/settings.json'))); ups=d['hooks']['UserPromptSubmit']; cmds=[h['command'] for g in ups for h in g.get('hooks',[])]; ng=[c for c in cmds if 'nightguard_adapter.py' in c]; assert len(ng)==1, ('dup or missing', cmds); assert ng[0].startswith('python3 /') and ng[0].endswith('nightguard_adapter.py'), ng[0]; assert all(k in d for k in ('enabledPlugins','extraKnownMarketplaces','theme','skipDangerousModePermissionPrompt')); print('OK single idempotent absolute-path hook; existing keys preserved')"</automated>
  </verify>
  <acceptance_criteria>
    - After two `--apply` runs, `~/.claude/settings.json` `hooks.UserPromptSubmit` has exactly ONE entry referencing `nightguard_adapter.py` (no duplication)
    - The wired command is an absolute `python3 /...nightguard_adapter.py` path
    - All four pre-existing top-level keys are still present after the merge
  </acceptance_criteria>
  <done>The global curfew hook is live in user-scope settings, idempotent, and preserves unmanaged keys.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify live curfew enforcement (3 success criteria)</name>
  <action>PAUSE for the human: only an interactive Claude Code session exercises the live UserPromptSubmit hook. Have the human verify the three success criteria per <how-to-verify> (deny/allow also provable out-of-band via the NTP override). Resume on "approved".</action>
  <what-built>The curfew adapter is wired globally via the generator into `~/.claude/settings.json`
  `UserPromptSubmit`. End-to-end, a new Claude Code session in ANY directory now runs
  `nightguard_adapter.py` on every prompt, which invokes `guard.py` in soft mode against the
  canonical LifeOS config and translates a `deny` verdict into a hard block (exit 2 + Spanish-butler
  message). This is the final verification of the three ROADMAP success criteria.</what-built>
  <how-to-verify>
    Only an interactive Claude Code session exercises the live `UserPromptSubmit` hook, so this
    needs you. Start a NEW Claude Code session (the hook loads at session start) and verify all three:

    1. SC#1 -- curfew blocks. The current real curfew is 20:45-05:30. If it is currently inside
       curfew, just submit a normal prompt (e.g. `what time is it`) -- it should be ERASED and you
       should see the butler message ("Mi senor, it is past 20:45 - the house is closed ..."). If it
       is NOT currently curfew time, Claude can prove the deny path deterministically out-of-band by
       running the adapter with an in-curfew NTP override:
         `printf '{"prompt":"hi"}' | NIGHTGUARD_DIR=/home/danitrrga/dev/Projects/LifeOS/nightguard NIGHTGUARD_TEST_NTP_OVERRIDE=1 NIGHTGUARD_NTP_OVERRIDE_UNIX=$(date -d 'today 23:30' +%s) python3 /home/danitrrga/dev/Projects/LifeOS/hooks/nightguard_adapter.py; echo "exit=$?"`
       -> expect the butler curfew message on stderr and `exit=2`.

    2. SC#1b -- the escape valve. During curfew, submit `/shutdown` and `/plan tomorrow: finish the
       watchdog tests at 09:00` -- BOTH should pass through (the prompt is NOT erased; the leading
       token is allowlisted, free-form params ride along).

    3. SC#2 -- outside curfew / grace allows. Either submit a normal prompt outside curfew (it passes
       silently), OR Claude proves it out-of-band with an outside-curfew override:
         `printf '{"prompt":"hi"}' | NIGHTGUARD_DIR=/home/danitrrga/dev/Projects/LifeOS/nightguard NIGHTGUARD_TEST_NTP_OVERRIDE=1 NIGHTGUARD_NTP_OVERRIDE_UNIX=$(date -d 'today 12:00' +%s) python3 /home/danitrrga/dev/Projects/LifeOS/hooks/nightguard_adapter.py; echo "exit=$?"`
       -> expect empty stderr and `exit=0`.

    Report: did a normal prompt block during curfew with the butler message? Did `/shutdown` + `/plan`
    pass during curfew? Did a normal prompt pass outside curfew?
  </how-to-verify>
  <resume-signal>Type "approved" (curfew blocks, /plan + /shutdown pass, outside-curfew allows) or describe what did not behave as expected</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator -> ~/.claude/settings.json | the generator writes a managed hooks block into a user-global file that holds unmanaged keys |
| ~/.claude/settings.json -> every Claude Code session | user-scope hooks fire in all sessions and concatenate across scopes (the global-reach surface) |
| project settings -> the curfew hook | a per-project settings file could attempt to dodge curfew |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-071-09 | Bypass/Elevation | dodge curfew via an un-gated repo | mitigate | Hook lives in USER-scope `~/.claude/settings.json`; arrays concatenate across scopes so the curfew hook fires in every session and cannot be replaced by project scope [VERIFIED RESEARCH Pitfall 4] |
| T-071-10 | Tampering | the merge clobbers enabledPlugins/theme | mitigate | Reuse `load`+`write_atomic` load-merge-write-preserving-other-keys shape; verify gate asserts all four pre-existing keys survive; one-time .bak from write_atomic |
| T-071-11 | Tampering | duplicate/stale hook entries accumulate on re-run | mitigate | Append-if-absent keyed on the `nightguard_adapter.py` command substring; two `--apply` runs asserted to yield exactly one entry |
| T-071-12 | Bypass | relative-path hook command fails to launch (historic breakage) -> no enforcement | mitigate | Command is an ABSOLUTE `python3 /...nightguard_adapter.py` path (AGENT-SYSTEM Linux rule); verify gate asserts the leading `/` |
| T-071-13 | Bypass | `disableAllHooks: true` turns off ALL hooks | accept | Residual, out-of-band config edit of the same character as disabling the watchdog; flagged for a future hardening note, not this phase (RESEARCH Pitfall 4) |
</threat_model>

<verification>
- `write_curfew_hook` exists in the generator; dry-run writes nothing; two `--apply` runs are idempotent (one entry).
- `~/.claude/settings.json` `hooks.UserPromptSubmit` references the absolute `nightguard_adapter.py`; all pre-existing keys preserved.
- Live: a curfew-time prompt is erased + butler message shown; `/plan`/`/shutdown` pass during curfew; an outside-curfew prompt passes. (Deny/allow also provable out-of-band via the NTP override.)
</verification>

<success_criteria>
- CURF-01 closed: the wired hook enforces the guard verdict globally and reproducibly.
- ROADMAP SC#1 (curfew blocks with butler message), SC#2 (outside-curfew + grace allow), SC#3 (reads canonical config, soft mode) all confirmed.
- The wiring survives SessionStart regeneration (generator-owned) and cannot be dodged per-project.
</success_criteria>

<output>
Create `.planning/phases/07.1-curfew-hook/07.1-03-SUMMARY.md` when done.
Record the live-enforcement confirmation (the three success criteria) for the phase verifier.
</output>
