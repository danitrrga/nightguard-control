---
phase: 07.1-curfew-hook
plan: 01
subsystem: infra
tags: [claude-code-hooks, userpromptsubmit, curfew, allowlist, probe]

# Dependency graph
requires:
  - phase: 07-root-wall
    provides: the working soft-mode guard.py verdict engine the future adapter fires
provides:
  - "Empirical resolution of RESEARCH Q1: the UserPromptSubmit prompt field carries RAW prompt text (fires before slash-command expansion) on Claude Code 2.1.186"
  - "Recorded matcher decision (MATCHER: leading-token) in 07.1-MATCHER-DECISION.md, consumed by Plan 02's allowlist task"
affects: [07.1-02 adapter allowlist, 07.1-03 hook wiring]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Wave-0 runtime probe: temporary, reversible instrumentation hook in ~/.claude/settings.json (exits 0, blocks nothing) to capture a runtime-unknown before committing to a design choice"
    - "Leading-token allowlist matcher (prompt.split()[0] membership) chosen over substring once the raw token was proven exposed"

key-files:
  created:
    - .planning/phases/07.1-curfew-hook/07.1-MATCHER-DECISION.md
  modified: []

key-decisions:
  - "MATCHER: leading-token — UserPromptSubmit exposes the raw /plan /shutdown leading token (verified live), so the tighter leading-token check is used instead of the substring fail-safe (no loosening hole where any prompt merely containing /plan would bypass curfew)"

patterns-established:
  - "Reversible probe hook with mandatory unwire-on-completion (T-071-01 mitigation: probe exits 0, Task 2 restores from .probe-bak and asserts no hooks block remains)"

requirements-completed: []

# Metrics
duration: 13min
completed: 2026-06-23
---

# Phase 07.1 Plan 01: UserPromptSubmit Matcher Probe Summary

**Live runtime probe proved the Claude Code `UserPromptSubmit` hook fires BEFORE slash-command expansion (raw `prompt` text, not an expanded `<command-name>` blob), so the curfew allowlist uses the tighter leading-token matcher — recorded as `MATCHER: leading-token` for Plan 02.**

## Performance

- **Duration:** ~13 min (wall clock incl. the human-action checkpoint pause)
- **Started:** 2026-06-23T10:06Z
- **Completed:** 2026-06-23T10:19:50Z
- **Tasks:** 2 auto-tasks + 1 human-action checkpoint
- **Files modified:** 1 created (repo); 1 runtime file instrumented then reverted (not git-tracked)

## Accomplishments
- Resolved RESEARCH Open Question Q1 (the phase's stated top integration risk) empirically against the live runtime (Claude Code 2.1.186).
- Captured the `UserPromptSubmit` `prompt` field for `/plan test`, `/plan test(...)`, `/shutdown`, and a plain control prompt — all slash captures expose the **raw leading token**.
- Recorded a single decisive `MATCHER: leading-token` line in `07.1-MATCHER-DECISION.md` with the verbatim captures + rationale, ready for Plan 02's adapter author.
- Fully removed the temporary probe instrumentation (settings restored from backup, backup deleted — no probe hook left live).

## Task Commits

1. **Task 1: Wire the staged probe into `~/.claude/settings.json`** — no repo commit (touches only the live runtime file `~/.claude/settings.json` + its `.probe-bak`, neither git-tracked; governed by the LifeOS shared agent-config system, not the repo)
2. **Checkpoint: Probe the live UserPromptSubmit prompt field** — human-action gate; resolved with verdict `raw-token` (cannot be auto-driven; only an interactive session can fire `UserPromptSubmit`)
3. **Task 2: Record the matcher decision and unwire the probe** — `7767996` (docs)

## Files Created/Modified
- `.planning/phases/07.1-curfew-hook/07.1-MATCHER-DECISION.md` — the recorded Q1 evidence (4 captured PROMPT_FIELD values) + the decisive `MATCHER: leading-token` line + rationale; consumed by 07.1-02.
- `~/.claude/settings.json` (runtime, NOT git-tracked) — temporarily gained a probe `UserPromptSubmit` hook in Task 1, restored to its exact pre-probe state in Task 2 (no hooks block).

## Decisions Made
- **MATCHER: leading-token.** The probe showed all three escape-valve prompts (`/plan test`, `/plan test(dont plan anything)`, `/shutdown`) expose the raw command as `prompt.split()[0]`. Per the plan's decision rule (leading-token ONLY if all three captures show the raw leading token; else substring fail-safe), the condition is satisfied. Leading-token is tighter than the Windows-adapter substring match — it closes the loosening hole where any prompt merely *containing* `/plan` would bypass curfew, while still letting D-12 free-form args ride along after the leading token.

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
- The capture log did not exist when the checkpoint was first reached (the probe had not fired). This is expected: `UserPromptSubmit` can only be driven by an interactive human-submitted prompt, so the agent correctly stopped at the `gate="blocking"` human-action checkpoint rather than fabricating evidence. Resolved when the probe captures were supplied (verdict `raw-token`).

## User Setup Required
None - no external service configuration required. (The probe instrumentation was added and removed within this plan; nothing persistent was installed.)

## Next Phase Readiness
- Plan 02 can read `MATCHER: leading-token` from `07.1-MATCHER-DECISION.md` and implement the allowlist as a leading-token membership check (`prompt.strip().split()[0] in config.yaml curfew.allow_commands`), with free-form args passing through.
- No live instrumentation hook remains on the runtime (T-071-01 satisfied) — Plan 03 installs the real curfew hook into a clean `~/.claude/settings.json`.
- No blockers. This plan deliberately does not block Plan 02 (substring was the documented fallback had the probe been inconclusive).

## Self-Check: PASSED
- FOUND: .planning/phases/07.1-curfew-hook/07.1-MATCHER-DECISION.md (contains `MATCHER: leading-token`)
- FOUND: commit 7767996
- VERIFIED: `~/.claude/settings.json` has no hooks block; `~/.claude/settings.json.probe-bak` deleted

---
*Phase: 07.1-curfew-hook*
*Completed: 2026-06-23*
