---
phase: 06-config-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
status: blocked
blocked_on: "Linux Python tooling absent — see Precondition. Do not execute until restored + watchdog quiescent."
files_modified:
  - LifeOS/nightguard/config.yaml          # instance (not in this repo)
  - LifeOS/nightguard/config.sanctioned.yaml
  - LifeOS/nightguard/guard.json           # re-signed via the control CLI
autonomous: false
requirements: [LXCF-01, LXCF-02]
must_haves:
  truths:
    - "The live LifeOS config.yaml + config.sanctioned.yaml use the new blocking: schema (no Windows uwp/package_id entry), curfew start 20:45"
    - "guard.json is re-signed (config_hmac over the new canonical config bytes; state_hmac via A3) so the running watchdog accepts the new config without reverting"
    - "Rust and the Python ngcommon produce byte-identical config_hmac over the new config; the Python yaml_load parses the new blocking.* keys"
  artifacts:
    - path: "LifeOS/nightguard/config.sanctioned.yaml"
      provides: "the canonical Linux-schema sanctioned snapshot (revert target)"
      contains: "blocking"
  key_links:
    - from: "LifeOS/nightguard/guard.json"
      to: "LifeOS/nightguard/config.yaml"
      via: "config_hmac = HMAC(file .guardkey, canonical config bytes)"
      pattern: "config_hmac"
---

<precondition>
## ⛔ BLOCKED — do not execute until this clears

As of 2026-06-22 ~15:13, the Linux Python tooling is **absent from disk** (only stale
`.pyc` under `scripts/nightguard/__pycache__/`), is **not in the repo**, and is **not
git-tracked in LifeOS** (so unrecoverable via git). The systemd user timer
`nightguard-watchdog.timer` (enabled, 60s) points at a `nightguard_watchdog.py` that does
not currently exist; the directory is being actively edited.

**Required before running 06-02:**
1. `scripts/nightguard/ngcommon.py` (shared `yaml_load` + `config_hmac`/`state_hmac` A3),
   `guard.py` (verify→revert), the signing **control CLI**, and `nightguard_watchdog.py`
   are present on disk and importable.
2. The watchdog is quiescent — `watchdog.log` shows a steady `tick: ok` with no revert loop.
3. (Recommended) those sources are committed somewhere recoverable before edits begin.

If the tooling is being rebuilt as part of Phase 7 instead, **move this plan into Phase 7**
(the watchdog→systemd-system work) rather than executing it here.
</precondition>

<curation_note>
**Curated 2026-06-22 (REVIEWS.md):** LXCF-02 narrowed to the **single Python stack** —
the load-bearing proof is that `ngcommon.py` parses + signs the new schema and the watchdog
accepts it (Task 1 + Task 3 revert proof). The Rust↔Python byte-parity check (Task 3 step 1)
is now **repo-hygiene / optional**, not a runtime gate, since Linux runs only the Python
crypto stack (the TUI app calls the control-CLI to sign — see PORT-02).
</curation_note>

<objective>
Re-baseline the author's live LifeOS nightguard instance onto the new Linux `blocking:`
schema and re-sign it via the Python control-CLI, then prove the Python stack parses + signs
it and tamper still reverts — closing the instance half of LXCF-01 and (single-stack) LXCF-02.

Purpose: the running watchdog auto-reverts any config.yaml whose HMAC ≠
`guard.json.config_hmac`. So the schema change on the live instance is not a hand-edit — it
must be written to the sanctioned snapshot and re-signed atomically (under the control CLI's
flock), or the watchdog reverts it. The `.guardkey` is already a plain file (no DPAPI), so
signing is pure HMAC over canonical bytes via the existing Python control CLI.
Output: rewritten `config.yaml` + `config.sanctioned.yaml`, a re-signed `guard.json`, and a
parity/revert proof.
</objective>

<context>
@.planning/phases/06-config-cleanup/06-CONTEXT.md
@.planning/phases/06-config-cleanup/06-01-SUMMARY.md   (the matching product schema)
@.planning/REQUIREMENTS.md   (LXCF-01, LXCF-02)

<canonical_config target="config.sanctioned.yaml — and config.yaml is written byte-identical">
timezone: Europe/Amsterdam

curfew:
  enabled: true
  start: "20:45"
  end: "05:30"
  allow_commands:
    - "/shutdown"
  block_when_offline: true
  message: "Mi señor, it is past {start} - the house is closed for the night. Pray run /shutdown to log the day, then retire. Jimmy will receive you again at {end}."
  tamper_message: "Mi señor, the clock appears to have been tampered with. Nice try. The house rules stand, retire for the night."
  offline_message: "Mi señor, I cannot verify the time (no network). For your own good, the house remains closed until I can confirm it is past dawn."

clock_protection:
  enabled: true
  max_offset_minutes: 5

# Revert-watchdog cadence (Phase 7 moves this to a systemd *system* unit).
watchdog:
  enabled: true
  check_interval_seconds: 60

# What gets blocked during curfew (Linux).
blocking:
  browser_extension:
    enabled: true
    # StayFree — Website Blocker (Chromium, force-installed via root managed policy).
    extension_id: elfaihghhjjoknimpccccmkioofjjfkf
  native_apps:
    enabled: true
    # Hyprland window classes killed during curfew (Phase 8 finalizes membership + kill mechanism).
    blacklist:
      - steam
      - discord
</canonical_config>
</context>

<tasks>

<task type="manual" tdd="false">
  <name>Task 1: Confirm the Python parity contract (ngcommon.py)</name>
  <files>scripts/nightguard/ngcommon.py (LifeOS), scripts/nightguard/guard.py (LifeOS)</files>
  <read_first>
    - ngcommon.py: `yaml_load` (the minimal parser — confirm it handles a 3-deep nested map
      with a list leaf: `blocking.native_apps.blacklist`), `config_hmac`, `state_hmac`
      (state with state_hmac blanked, compact JSON fixed field order, one trailing \n).
    - crates/trust-kernel/src/canon.rs + crates/mutation-engine/src/{sign.rs,state.rs} —
      the Rust side of the same contract (must agree byte-for-byte).
  </read_first>
  <action>
    Verify `yaml_load` parses the new `blocking:` block (nested map + list leaf) to the
    expected values. If the minimal parser cannot represent 3-deep nesting or a list under a
    2-deep map, STOP and surface it — the schema (D-1) may need flattening (fall back to the
    "Nest under watchdog:" or "Flat top-level" shape from discuss) before proceeding. Confirm
    `config_hmac`/`state_hmac` recipes match canon.rs/state.rs exactly (BOM/LF/trailing-\n;
    compact JSON field order).
  </action>
  <acceptance_criteria>
    - ngcommon `yaml_load` round-trips the new schema's blocking.* values.
    - config_hmac + state_hmac recipes are byte-for-byte identical to the Rust trust-kernel.
  </acceptance_criteria>
  <done>The Python parity contract is confirmed to handle the new schema, or a schema-shape blocker is surfaced.</done>
</task>

<task type="manual" tdd="false">
  <name>Task 2: Rewrite the sanctioned + live config and re-sign via the control CLI</name>
  <files>LifeOS/nightguard/config.sanctioned.yaml, LifeOS/nightguard/config.yaml, LifeOS/nightguard/guard.json</files>
  <read_first>
    - 06-CONTEXT.md §Decisions D-3/D-4 (re-baseline scope; curfew 20:45 is a sanctioned reset, not a token-charged loosen)
    - the control CLI's commit/sign/init path (holds .nightguard.lock flock; writes sanctioned → config → re-sign state)
  </read_first>
  <action>
    Using the Python **control CLI** (the signer — never a hand-edit), under its
    `.nightguard.lock` flock so the watchdog cannot revert mid-write:
    1. Write `config.sanctioned.yaml` = the canonical_config block above.
    2. Write `config.yaml` byte-identical to it.
    3. Re-sign `guard.json`: `config_hmac` = HMAC(file `.guardkey`, canonicalize(config bytes));
       `state_hmac` via A3. Preserve `weekly_spent`, `week_anchor`, `grace`, `ledger`.
    Record the curfew 20:30→20:45 change as a deliberate sanctioned re-baseline (audit log
    note), NOT a quota-charged loosen.
  </action>
  <acceptance_criteria>
    - config.yaml and config.sanctioned.yaml are byte-identical, carry the new `blocking:` schema, curfew 20:45, no `uwp`/`package_id`.
    - guard.json verifies: recompute config_hmac/state_hmac == stored, both Rust and Python.
    - Tokens/week-anchor/grace are preserved (no accidental quota change).
  </acceptance_criteria>
  <done>The live instance runs the new schema, re-signed and verifying.</done>
</task>

<task type="manual" tdd="false">
  <name>Task 3: Prove parity + revert (LXCF-02)</name>
  <files>LifeOS/nightguard/ (verification only)</files>
  <action>
    1. **Cross-language HMAC parity:** Rust and Python each compute config_hmac over the new
       config bytes — assert byte-identical (reuse the interop-gate shape, now Linux file-key).
    2. **Parse parity:** Python `yaml_load` returns the expected `blocking.*` values.
    3. **Revert proof:** hand-edit config.yaml (e.g. flip `blocking.native_apps.enabled` to
       false WITHOUT re-signing) → on the next watchdog tick it auto-reverts to sanctioned
       (HMAC-mismatch path works on the new schema). Confirm `watchdog.log` shows the revert,
       then a steady `tick: ok`.
    4. **Policy tick still green:** the watchdog's browser-policy check passes
       (`policies/chromium-nightguard.json` maps to `blocking.browser_extension.extension_id`).
  </action>
  <acceptance_criteria>
    - Rust config_hmac == Python config_hmac over the new config (byte-identical hex).
    - A hand-edit auto-reverts; no revert loop afterward (steady `tick: ok`).
    - Browser-policy verification stays green post-re-baseline.
  </acceptance_criteria>
  <done>LXCF-02 proven: Rust writer ↔ Python watchdog agree on parse + HMAC over the new schema; tamper still reverts.</done>
</task>

</tasks>

<threat_model>
## STRIDE notes
| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-06-03 | Tampering (re-baseline races the watchdog) | mitigate | Re-sign under the control CLI's `.nightguard.lock` flock; the guard skips revert while the lock is held (commit ordering: sanctioned → config → re-sign state). |
| T-06-04 | Elevation (curfew weakened under cover of "cleanup") | mitigate | Curfew 20:30→20:45 is the explicit, user-approved D-4 reset and is the ONLY curfew change; the diff is reviewed before signing; tokens/grace are preserved, not reset. |
| T-06-05 | DoS (revert loop bricks the instance) | mitigate | Task 3 verifies a steady `tick: ok` after re-baseline; if a loop appears, the sanctioned snapshot is the known-good fallback. |
</threat_model>

<verification>
- config.yaml ≡ config.sanctioned.yaml (byte-identical), new schema, curfew 20:45, no Windows entry.
- guard.json verifies in both Rust and Python; tokens/week-anchor/grace preserved.
- Rust↔Python config_hmac byte-identical; Python yaml_load parses blocking.*; hand-edit reverts; policy tick green.
</verification>

<success_criteria>
- LXCF-01 (instance half): the live config.yaml defines `browser_extension` + `native_apps`; the Windows `uwp`/`package_id` entry is gone.
- LXCF-02: the Rust writer and the Python watchdog parse + HMAC the new config identically; tamper still auto-reverts.
</success_criteria>

<output>
Create `.planning/phases/06-config-cleanup/06-02-SUMMARY.md` when done.
</output>
</content>
