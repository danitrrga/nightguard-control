---
phase: 03-enforcement-guard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/interop/run_state_interop_gate.ps1
  - crates/mutation-engine/tests/lock_probe.rs
autonomous: true
requirements: [GARD-03, GARD-05]
must_haves:
  truths:
    - "The runtime state-verify recipe (parse pretty guard.json -> compact ConvertTo-Json -> +0x0A -> HMAC) re-derives the exact state_hmac an untampered Rust-written guard.json carries."
    - "A PowerShell exclusive-open probe of .nightguard.lock returns held while a Rust process holds the fd-lock, and free once released."
  artifacts:
    - path: "scripts/interop/run_state_interop_gate.ps1"
      provides: "Added Check 4 that exercises the RUNTIME state-verify path (not the .signbytes path)"
      contains: "ConvertTo-Json -Compress -Depth 10"
    - path: "crates/mutation-engine/tests/lock_probe.rs"
      provides: "Rust integration test that holds .nightguard.lock so a spawned PowerShell probe can assert IOException-on-open"
      contains: "with_commit_lock"
  key_links:
    - from: "scripts/interop/run_state_interop_gate.ps1"
      to: "guard.json.state_hmac"
      via: "ConvertFrom-Json -> blank -> ConvertTo-Json -Compress -Depth 10 -> +0x0A -> Get-FileHmacHex"
      pattern: "ConvertTo-Json -Compress -Depth 10"
    - from: "crates/mutation-engine/tests/lock_probe.rs"
      to: ".nightguard.lock"
      via: "with_commit_lock held while a PowerShell [IO.File]::Open(...,'None') probe runs"
      pattern: "with_commit_lock"
---

<objective>
De-risk the TWO un-spiked cross-language mechanisms before any guard logic is built on top of
them: (1) the RUNTIME state_hmac re-derivation (parse the pretty on-disk guard.json, compact
re-serialize, canonicalize, HMAC) and (2) the fd-lock presence probe (exclusive-open
IOException, NOT file existence). Both are PowerShell-vs-Rust parity risks flagged MEDIUM in
RESEARCH Open Questions 1 & 2; everything in plans 02-04 assumes they hold.

Purpose: The state-verify recipe and the circuit-breaker are load-bearing for GARD-03 and
GARD-05. If the compact ConvertTo-Json form diverges from serde, the guard treats every
legitimate state as tampered (constant fail-closed). If the lock probe is wrong, the guard
either never skips (reverts legitimate commits) or always skips (never reverts). Prove both
with runnable gates first.

Output: Check 4 appended to run_state_interop_gate.ps1 proving the RUNTIME recipe equals the
on-disk state_hmac; a Rust integration test lock_probe.rs that holds .nightguard.lock and
spawns a PowerShell exclusive-open probe to confirm held/free detection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-enforcement-guard/03-CONTEXT.md
@.planning/phases/03-enforcement-guard/03-RESEARCH.md
@.planning/phases/03-enforcement-guard/03-PATTERNS.md

<interfaces>
<!-- The LOCKED state_hmac A3 recipe the runtime path must reproduce. From state.rs lines 49-56. -->
<!-- on-disk guard.json is serde_json::to_vec_pretty (indented); the tag signs the COMPACT
     serde_json::to_string form with state_hmac blanked + canonicalize_bytes (+ one trailing 0x0A). -->
GuardState field order (state.rs 26-39): config_hmac, state_hmac, weekly_spent, week_anchor, ledger, grace
compute_state_hmac (state.rs 49-56): clone -> state_hmac="" -> serde_json::to_string -> canonicalize_bytes -> sign_bytes -> tag_to_hex
canonicalize_bytes (trust-kernel canon.rs): no BOM, LF-only, exactly one trailing 0x0A

<!-- The lock the probe must detect. From commit.rs lines 61-81. -->
with_commit_lock (commit.rs 61-81): OpenOptions create+read+write (truncate false) on
  lock_dir/.nightguard.lock, then fd_lock::RwLock::write() = Windows LockFileEx + LOCKFILE_EXCLUSIVE_LOCK.
  File PERSISTS once created; existence is NOT the held-signal. Lock released on guard drop.

<!-- Existing PS primitives to REUSE (do not reimplement). From nightguard_interop.ps1. -->
Get-FileBytes (45-50): [IO.File]::ReadAllBytes only (never Get-Content)
Get-FileHmacHex (82-98): HMAC-SHA256 over raw file bytes -> lowercase hex
ConvertTo-LowerHex (62-67): byte[] -> lowercase hex matching Rust hex::encode
Unprotect-GuardKey (36-43): DPAPI CurrentUser, $null entropy, raw blob; caller asserts len==32
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add runtime state-verify Check 4 to run_state_interop_gate.ps1</name>
  <files>scripts/interop/run_state_interop_gate.ps1</files>
  <read_first>
    - scripts/interop/run_state_interop_gate.ps1 (the gate being extended — Add-Check pattern lines 68-73, Check 3 ends line 169, verdict block 171-186, shared-key setup 82-90)
    - scripts/interop/state_interop.ps1 (the .signbytes path this NEW check deliberately does NOT use — runtime path re-derives bytes itself)
    - scripts/interop/nightguard_interop.ps1 (Get-FileBytes 45-50, Get-FileHmacHex 82-98, ConvertTo-LowerHex 62-67 — reuse, never reimplement)
    - crates/mutation-engine/src/state.rs (compute_state_hmac lines 49-56 + field-order struct 26-39 — the recipe to mirror byte-for-byte)
  </read_first>
  <action>
    Insert a new Add-Check block "state_hmac RUNTIME re-derive" AFTER Check 3 (after line 169,
    before the Verdict block at line 171). It must prove the RUNTIME recipe (parse pretty
    guard.json, re-serialize compact) equals the on-disk guard.json.state_hmac for the untampered
    Rust-written $guardJson already produced by Check 1's emit-state-hmac call. Concrete steps:
    read the pretty guard.json via Get-FileBytes then [Text.Encoding]::UTF8.GetString | ConvertFrom-Json;
    capture $expectedTag = $obj.state_hmac; set $obj.state_hmac = ''; $compact = $obj | ConvertTo-Json -Compress -Depth 10
    (the -Depth 10 is MANDATORY — default 2 truncates ledger[].fields to "System.Object[]");
    $bytes = [Text.Encoding]::UTF8.GetBytes($compact) (UTF-8, NO BOM); build $canon = $bytes with
    exactly one 0x0A appended (allocate byte[$bytes.Length+1], Array.Copy, set last = 0x0A);
    HMAC $canon with the gate's existing $knownKey via a [HMACSHA256]::new($knownKey) ComputeHash
    then ConvertTo-LowerHex; assert the re-derived tag -eq $expectedTag via Add-Check. Reuse the
    gate's existing $knownKey and $guardJson; do NOT depend on the .signbytes file (that is the
    gate-only path this check replaces). Keep ASCII-only (WinPS 5.1 -File chokes on em-dashes).
    This closes the RESEARCH Wave 0 gap: "prove the runtime recipe (not .signbytes) before the phase gate."
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/interop/run_state_interop_gate.ps1; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - run_state_interop_gate.ps1 exits 0 with the new check listed PASS in the === Summary === block.
    - The new check's re-derived tag equals guard.json.state_hmac WITHOUT reading the .signbytes file (source assertion: the added block references $guardJson, never $signBytes).
    - The block uses `ConvertTo-Json -Compress -Depth 10` (grep: the literal string appears in the added check).
    - File is ASCII-only (no bytes > 0x7F in the added lines): `[IO.File]::ReadAllBytes('scripts/interop/run_state_interop_gate.ps1') | Where-Object { $_ -gt 127 }` returns nothing.
  </acceptance_criteria>
  <done>Gate is green and the runtime (pretty->compact) state_hmac re-derivation provably matches the on-disk tag for an untampered Rust-written guard.json.</done>
</task>

<task type="auto">
  <name>Task 2: Spike the fd-lock presence probe with a Rust lock-holder + PowerShell exclusive-open</name>
  <files>crates/mutation-engine/tests/lock_probe.rs</files>
  <read_first>
    - crates/mutation-engine/src/commit.rs (with_commit_lock lines 61-81 — the exact lock acquisition the probe must detect; note the file persists, existence is NOT the signal)
    - crates/mutation-engine/tests/commit_order.rs (existing integration-test conventions in this crate: temp dirs under target/, CommitPaths construction)
    - crates/mutation-engine/src/state.rs (MutationError — with_commit_lock body returns Result<_, MutationError>)
    - .planning/phases/03-enforcement-guard/03-PATTERNS.md (Test-LockHeld probe shape lines 84-93 — [IO.File]::Open($p,'Open','ReadWrite','None') -> IOException = held)
  </read_first>
  <action>
    Create a Rust integration test that proves the PowerShell circuit-breaker probe (D-06)
    correctly distinguishes a held vs free .nightguard.lock. In the test: create a temp dir under
    target/; call with_commit_lock(&dir, || { ... }) and, INSIDE the closure while the fd-lock is
    held, spawn `powershell -NoProfile -ExecutionPolicy Bypass -Command` running an inline probe
    that attempts [IO.File]::Open(<lockpath>,'Open','ReadWrite','None') wrapped in try/catch
    [IO.IOException] — print "HELD" on IOException, "FREE" on successful open (then Close).
    Assert the spawned probe printed HELD while inside the lock. Then, AFTER with_commit_lock
    returns (lock released, file still present), spawn the same probe again and assert it prints
    FREE. Use std::process::Command to run powershell and capture stdout. The lock path is
    dir.join(".nightguard.lock"). This is the one runtime mechanism not yet proven cross-language
    (RESEARCH Open Question 1) — exit code 0 from `cargo test` proves the exact probe
    nightguard_guard.ps1 will use. Do NOT add the probe to the guard script here — this is the
    spike only; plan 02 lifts the proven [IO.File]::Open('None') shape into Test-LockHeld.
  </action>
  <verify>
    <automated>cargo test -p mutation-engine --test lock_probe -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mutation-engine --test lock_probe` exits 0.
    - The test asserts the PowerShell probe prints "HELD" while with_commit_lock holds the lock (behavior assertion via captured stdout).
    - The test asserts the probe prints "FREE" after the lock drops, with the .nightguard.lock file still present on disk (proving existence != held).
    - The probe shape used is `[IO.File]::Open(<path>,'Open','ReadWrite','None')` catching `[IO.IOException]` (source assertion: that exact share-mode 'None' open appears in the test's inline PowerShell).
  </acceptance_criteria>
  <done>`cargo test -p mutation-engine --test lock_probe` is green; the exclusive-open IOException probe is proven to report held-while-locked and free-after-release against a live Rust fd-lock holder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust writer -> PowerShell reader (guard.json contract) | The guard re-derives state_hmac in PS; a serializer divergence is a silent fail-open-OR-fail-stuck risk |
| App commit-lock -> guard probe | The guard reads a lock it does not own; a wrong probe defeats the GARD-05 race protection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | state_hmac runtime re-derivation | mitigate | Task 1 gate proves the compact ConvertTo-Json -Depth 10 + 0x0A recipe re-derives the on-disk tag for an untampered Rust-written guard.json; any future schema drift turns the gate red (RESEARCH A2). |
| T-03-02 | Tampering (TOCTOU) | fd-lock presence probe (D-06) | mitigate | Task 2 proves exclusive-open IOException == held against a live Rust lock holder, so the guard skips revert only during a real in-progress commit (closes RESEARCH Open Question 1). |
| T-03-03 | Repudiation | ASCII-only gate scripts | accept | WinPS 5.1 -File parse failure on non-ASCII is caught by the acceptance grep; not a security threat, a correctness guard. |
</threat_model>

<verification>
- run_state_interop_gate.ps1 exits 0 with the runtime state-verify check PASS.
- cargo test -p mutation-engine --test lock_probe exits 0.
- Both un-spiked mechanisms (RESEARCH Open Questions 1 & 2 / planner un-spiked list) are now empirically proven before plan 02 builds on them.
</verification>

<success_criteria>
The runtime state_hmac recipe and the fd-lock presence probe are each proven by a green automated
gate, removing the two MEDIUM-confidence cross-language risks before the guard core is written.
</success_criteria>

<output>
Create `.planning/phases/03-enforcement-guard/03-01-SUMMARY.md` when done
</output>
