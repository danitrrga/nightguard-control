# Phase 12.2.1: Omarchy Native Panel - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-13
**Phase:** 12.2.1-omarchy-native-panel
**Areas discussed:** none selected — user chose to proceed on Claude's judgment

---

## Discuss — area selection

| Option | Description | Selected |
|--------|-------------|----------|
| What the panel calls (CLI surface) | Reuse `ngtui/backend.py`'s read/preview logic as a headless CLI, or write a fresh script | |
| How editing happens in the panel | Inline expanding rows vs. a popup dialog per key | |
| What "parity" means before retiring `ngtui` | Full feature match vs. core actions first | |
| None of these — proceed with my judgment | | ✓ |

**User's choice:** "None of these — proceed with my judgment."
**Notes:** No follow-up given; decisions D-04 through D-07 in CONTEXT.md were made by Claude
under this instruction and recorded as decisions rather than open questions.

---

## Claude's Discretion

- CLI surface: extend the existing `ngtui` CLI router with `preview --from <path>`
  (unprivileged) and `commit --from <path>` (thin `pkexec` wrapper), keeping
  `ngtui/backend.py` alive past the terminal app's retirement (D-04, D-05).
- Editing layout: inline rows in the existing panel column using the shell's own
  `ToggleSwitch`/`Toggle`/`TextField` components, staged locally, gated by a single
  `ConfirmDialog` before commit (D-06).
- Retirement gate: full BLK-01..05 parity including the informational surface (BLK-03) —
  not watered down to "core actions first" (D-07). This was already locked by the
  requirements this phase absorbs, so it was confirmed rather than freely decided.
- Whether to define a scoped polkit `.policy` action vs. rely on the built-in
  `org.freedesktop.policykit.exec` action — deferred to planning (see CONTEXT.md).

## Deferred Ideas

- A custom, phase-specific polkit action name/message in the auth dialog.
- Scoped polkit action icon/branding.
