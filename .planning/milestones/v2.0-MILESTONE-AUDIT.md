---
milestone: v2.0
milestone_name: Linux Port
audited: 2026-06-24
re_audited: 2026-06-24 (after B1/B2 resolution)
status: tech_debt
scores:
  requirements: 10/10 in-scope satisfied (3 descoped, 2 parked — intentional)
  phases: 5/6 verified (Phase 8 descoped, no VERIFICATION.md)
  integration: 9/9 seams resolved (2 prior blockers fixed)
  flows: 2/3 E2E flows complete (1 intentionally descoped)
blockers_resolved:
  - id: "B1"
    seam: "Phase 10 TUI backend.live_verdict -> guard.curfew_verdict"
    was: "guard.curfew_verdict() existed only as an uncommitted LifeOS working-tree diff; a git restore would break the TUI at import."
    fix: "Committed a standalone curfew_verdict() to LifeOS (a523c43). git HEAD now contains it — verified restore-safe. Zero runtime change (the on-disk file was already deployed)."
    affects: ["PORT-02"]
  - id: "B2"
    seam: "Phase 8 tracking vs live code"
    was: "REQUIREMENTS/ROADMAP/08-02-SUMMARY claimed NBLK-01/02/03 shipped, but the native-kill was reverted (LifeOS 3e64c81)."
    fix: "Marked NBLK-01/02/03 descoped (checkboxes + traceability), annotated ROADMAP Phase-8 entry/section, added a DESCOPED banner to 08-02-SUMMARY, reworded TRAK-01/02 as parked. Commit 840b873."
    affects: ["NBLK-01", "NBLK-02", "NBLK-03"]
descoped:
  - ids: ["NBLK-01", "NBLK-02", "NBLK-03"]
    reason: "Native-app kill built then reverted (LifeOS 3e64c81); the Claude Code curfew hook (CURF-01) was deemed sufficient enforcement. Intentional. Plan 08-01's curfew_verdict() refactor survives and backs the Phase-10 TUI."
parked:
  - ids: ["TRAK-01", "TRAK-02"]
    reason: "StayFree records 0 sessions on Hyprland/Wayland (X11-only detection) — the gating spike FAILED. Live tracking is non-functional. Offline blocklist/config recovery (config.db is clean SQLite) is a viable FUTURE-PHASE candidate, not v2.0 scope. 09-02 plan never completed (spike-PASS branch not taken)."
tech_debt:
  - phase: "08-native-blocker"
    items:
      - "No 08-VERIFICATION.md (phase descoped; requirements no longer claim shipped, so this is now cosmetic — a verifier was never run because the work was reverted)."
  - phase: "07-root-integrity-wall"
    items:
      - "ROOT-02 text overstates the watchdog: it does config-revert + browser-policy presence, NOT clock-tamper (that's the hook's job via guard.decide())."
      - "Browser policy JSONs are user-owned 0644 in a 0777 dir; watchdog logs but does not restore. User-removable without sudo. Known/accepted (STATE.md)."
  - phase: "all (6,7,7.1,8,9)"
    items:
      - "Nyquist: only Phase 10 has a VALIDATION.md (Nyquist enabled 2026-06-24, after these phases shipped). Backfill optional via /gsd:validate-phase <N>."
  - phase: "09-stayfree-desktop"
    items:
      - "09-VERIFICATION.md says passed while TRAK is parked — it verified the offline-recovery analysis, not the original tracking intent. 09-02 plan has no SUMMARY."
verification_anomalies:
  - phase: "07.1-curfew-hook"
    status: "human_needed"
    note: "CURF-01 wired + live (integration WIRED); formal human re-confirmation outstanding."
  - phase: "10-linux-app-omarchy-tui-was-tauri-port"
    status: "human_needed"
    note: "D-06 ordering + D-05 repaint approved LIVE during the 10-03 walkthrough; doc re-confirmation only."
---

# Milestone v2.0 · Linux Port — Audit (re-audit after B1/B2 resolution)

**Status: `tech_debt`** — no critical blockers remain. Every in-scope requirement is satisfied; the two prior blockers are fixed; the unmet requirements (NBLK, TRAK) are now honestly tracked as intentional descope/park rather than silent failures. What's left is reviewable tech debt, not a wall to merging.

## The milestone deliverable is met

The Linux trust spine — **one root key → one root watchdog → one signer (control-CLI via sudo) → one schema → one HMAC → the omarchy TUI** — is wired and working end-to-end:

- **Phase 6 (LXCF-01/02):** Linux `blocking.{browser_extension,native_apps}` schema flows consistently through signer, guard, and the TUI editable surface.
- **Phase 7 (ROOT-01/02/03/04):** root-owned `.guardkey` (0600) + sanctioned config; watchdog as systemd system service reverts tampered config; the TUI commits only via the exact sudoers argv; hand-forging infeasible.
- **Phase 7.1 (CURF-01):** `guard.py` is live as the Claude Code curfew hook; a `deny` verdict blocks the session.
- **Phase 10 (PORT-01/02/03):** the omarchy Textual thin client — 56 tests, ASVS-L1 secured, Nyquist-compliant — now standing on **committed** LifeOS code.
- **E2E Flow B (hand-edit → revert):** proven.

## Blockers — both RESOLVED

### B1 ✅ `guard.curfew_verdict` committed to LifeOS (`a523c43`)
The verdict extractor Phase 10 depends on is now in git HEAD (verified: `git show HEAD:…guard.py | grep curfew_verdict` matches). A `git restore` can no longer break the TUI. Zero runtime change — the on-disk file was already the deployed version the live walkthrough used.

### B2 ✅ NBLK descope reconciled (`840b873`)
REQUIREMENTS.md (checkboxes + traceability), ROADMAP Phase-8 entry/section, and `08-02-SUMMARY.md` now state plainly that the native-kill was reverted and NBLK-01/02/03 are descoped. The docs no longer claim reverted work shipped.

## Out-of-scope by decision (not failures)

| REQ | Disposition | Why |
|-----|-------------|-----|
| NBLK-01/02/03 | **Descoped** | Native-kill reverted; curfew hook deemed sufficient. Intentional. |
| TRAK-01/02 | **Parked** | StayFree tracking dead on Wayland (spike failed). Offline config-import is a future-phase candidate. |

## Remaining tech debt (review before/after completing the milestone)

1. **Phase 8 has no VERIFICATION.md** — now cosmetic (requirements descoped, nothing falsely claimed).
2. **Nyquist backfill** — only Phase 10 has a VALIDATION.md; 6/7/7.1/8/9 predate the enablement. Optional: `/gsd:validate-phase <N>`.
3. **ROOT-02 wording** overstates the watchdog (no clock-tamper there — that's the hook).
4. **Browser policy** files are user-removable without sudo (known/accepted, STATE.md).
5. **Phase 9** verification passed contradicts the parked TRAK status; 09-02 incomplete.
6. **7.1 + 10 VERIFICATION** are `human_needed` (10 live-approved; doc re-confirmation only).

None of these block completing v2.0 — they're deferrable with eyes open.
