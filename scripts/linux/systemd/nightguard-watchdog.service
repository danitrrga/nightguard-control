[Unit]
Description=Nightguard watchdog (root) — revert tampered config + verify browser policies
# System (root) unit: no %h. Absolute ExecStart + Environment=NIGHTGUARD_DIR (Phase 7 D-2).
# Runs as root so it can read the root-owned .guardkey (Phase 7 ROOT-01).
After=network-online.target
Wants=network-online.target

[Service]
Type=oneshot
Environment=NIGHTGUARD_DIR=/home/danitrrga/.local/share/nightguard
ExecStart=/usr/bin/python3 /home/danitrrga/dev/Projects/nightguard-control/scripts/linux/nightguard_watchdog.py
