# Build output
/target
src-tauri/target
src-tauri/gen

# Frontend toolchain
node_modules
dist

# Runtime secret/state artifacts — MUST never be committed (T-01-04)
*.guardkey
guard.json
config.yaml
config.sanctioned.yaml
tamper.log
.playwright-mcp
.claude
.antigravitycli
.gemini
AGENTS.md
CLAUDE.md
.claude/AGENT-SYSTEM.md

# Compiled Python — a stale .pyc next to a deployed source can shadow it
__pycache__/
*.pyc
