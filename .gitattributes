# Defend HMAC byte-stability against Git autocrlf rewriting.
# The config and key files are signed over raw bytes — Git must never normalize
# them, or a fresh checkout on a machine with autocrlf on would produce a file
# whose bytes no longer match the signature over them.
config*.yaml -text
*.guardkey -text
